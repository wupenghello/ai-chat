# 迭代复盘 — iter-22 真实天气工具（CHG-016 / REQ-053）

> **状态：已关闭（G4 过，2026-08-23）**——QA 审计有条件符合 2 NCR 整改闭环（retros/qa-audit-iter-22.md，CEO「整改」）；Code Review 三问全过无 NCR 级缺陷 + 6 非阻塞取舍留档，CEO 过目认可落痕（2026-08-23「认可」）；复盘四问 CEO 批准「全批」（改进 A/B 落制度 v1.4.21）。
>
> 迭代范围：CHG-016 真实天气工具（T0 取证 + T2 后端，零 UI 无设计稿——定夺④）+ 附带上线后缺陷当轮修两枚（DEF-041 流式滚动 / DEF-042 设置分区样式）+ v1.1.0 发布上线（2026-08-23 CEO G3 批准）。

## 1. 做对了什么（可固化为习惯）

1. **凭据先行验证**：CHG-016 起草期即完成和风三端点冒烟（now/3d/坐标 200、GeoAPI 404）——定夺⑥（GeoAPI 启用或回退）长在实测数据上，T0 零等待径行，实现期零返工。外部 API 依赖的 CHG 值得延续此模式。
2. **静态数据资产机器校验**：148 城表以 scripts/gen_weather_cities.py 逐城真调用校验生成（148/148 零失败），零手抄零猜测——铁律 5 精神延伸到静态数据资产面。
3. **回退路径预授权**：定夺⑥明文「不可启用则回退（表 + 坐标兜底）」使外部依赖（CEO 控制台动作）不阻塞开发——预授权回退值得作为外部依赖项的通用定夺写法。
4. **门禁三次立功实证**：.env 凭据击穿（pre-commit 全量测试拦截）、前端 flaky、跨午夜 `_TODAY` 竞态——均被机检拦下或暴露，机检价值三连证。
5. 上线后 CEO 反馈两枚缺陷（DEF-041/042）均当轮「登记 + 修复 + 真实浏览器取证 + 契约断言补面」闭环，且各带可迁移教训（交互时序类 jsdom 不设防 / 跨组件复用 class ≠ 复用样式）。

## 2. 哪里卡住了（根因）

1. **.env 凭据击穿测试——同型第三次**（iter-18 search key → 本次 weather key，v1.1.0 基线提交被拦）：根因 = 「config 字段先于 .env 实值」顺序纪律只存在于教训记录、从未入制度，每轮靠记忆 → **改进 A 承载**。
2. **台账计数手写失实**（NCR-iter22-001：19 vs 机器 20，净 +19 误记为新增 19）：testing §4b（v1.4.16 C）条款已有，但落笔时无「必须跑机器计数」的触发动作、且沿用了计划预估语义 → 执行失误为主，条款补强为动作化要求 → **改进 B 承载**。
3. 发布记录时序（NCR-iter22-002）：v1.0.0 两段式先例未沿——已定夺下轮恢复两段式（要件版先行 → 执行 → 回填实录），无需再动制度。
4. 走查脚本自身三轮迭代（登录后视图刷新/结果文本仅展开态渲染/累积历史卡定位）：e2e 脚本对真实 DOM 行为的假设需以首轮实跑校准——属正常调试成本，未显著超余量。

## 3. 流程改进（CEO 批准「全批」，落制度 v1.4.21）

- **A**：`process/development.md` §1 新增第 11 条——**新增配置变量的顺序纪律**（config 字段 + `.env.example` 占位先合入 → `.env` 实值后写；测试/走查凭据进程环境注入不落本地 `.env`）。治同型三次复发。
- **B**：`process/testing.md` §4b 补强——**测试计数落笔来源标注**（当次机器输出为据，禁计划预估/既有文档转写；新增/退役并存写三段「新增 N − 退役 M = 净 +K」）。治 NCR-iter22-001（v1.4.16 C 首次复发）。
- 无第三条（宁缺毋滥）。代码级小活不入制度，留候选清单：`_TODAY` 跨午夜调用时求值加固（QA OBS-6）、DEF-041 亚阈值回拽残留（CR 取舍①——意图后短窗忽略位置重挂）、weather 出网治理流级用例两枚（CR 取舍④）。

## 4. 估算校准

- 计划 Σ4（任务 Σ3 = T0 S1 + T2 M2 + 余量 1）vs **实际 Σ4 零偏差**——**连续十七轮**。
- 余量 1 实际吸收三件计划外工作：走查脚本三轮迭代、两次提交门禁拦截处置（.env 凭据撤出重提 / 跨点 flaky 重试）——余量设计目的的正面样本。
- 容量上限 Σ≤10 维持；「不顶格小迭代」第二例正面证据（继 iter-19 Σ4）——零 UI 纯后端小迭代以 T0 技术基线承担串行职能运转良好（v1.4.15 适配口径第二次验证）。

## 5. 交付与质量摘要（机器口径）

- 交付：weather 工具上线（v1.1.0）+ demo_weather 移除（echo 保留）；pytest 363→382（20 新增 − 1 退役 = 净 +19）+ ruff clean + vitest 423→428（DEF 批 +5）+ 走查 walkthrough-22 9 PASS/0 FAIL（真凭据）+ DEF-041/042 当轮修（各带真实 Chrome 取证）。
- QA：有条件符合 2 NCR 全闭环；CR：三问全过、无 NCR 级缺陷、6 取舍留档。
- 线上：v1.1.0 部署冒烟全过（含真实天气回合端到端）。
