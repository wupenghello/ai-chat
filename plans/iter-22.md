# 迭代计划 — iter-22（2026-08-22 ~ 2026-09-04）

> **状态：已批准（2026-08-22 CEO「批准」——Σ4 = S1+M2+余量 1 不顶格，T0→T2 严格串行，按本稿落盘）。**
>
> **变更依据**：requirements/changes.md **CHG-016**（已批准 2026-08-22，CEO「全部按推荐」= 整体批准 + 六定夺全按推荐定案，基线 req-baseline-v13，70f5392 + tag req-baseline-v13 已推 GitHub）。
>
> **本迭代定位**：真实天气工具（和风天气接入，第二个生产出网工具）。排入 REQ-053（P1）+ demo_weather 移除（定夺①，REQ-030 验收 6 改写映射承载）+ 全局回归基线天气面收口。
>
> **前置就绪**：工具网关六项校验（tools.py）、search 生产工具范本（search.py：bind/unbind/白名单/DNS 核验/ToolError 降级/逐字文案先例）、gate 注册过滤（tools_for_user + proxy._tool_gates）、kind='tool' 遥测行与 admin 工具用量聚合——全部在线零改动。**和风凭据 2026-08-22 复验已完成**（专属 Host /v7/weather/now 与 /v7/weather/3d 均 200 真实数据、坐标直查 200；GeoAPI 404 未启用——定夺⑥承载；复验实录登记 changes.md CHG-016 现状取证第 3 条）。
>
> **迭代时长**：名义标准 2 周（2026-08-22 ~ 2026-09-04，项目 tailoring.md「迭代长度：2 周」适用，无偏离需登记）；沿近期单日迭代节奏（iter-13~21 均 1~2 天内收口），实际周期以开发 → 测试 → QA 审计 → Code Review → 复盘全部收口为准。
>
> **头部自检（v1.4.18 B：当周 ISO 周机器核实）**：2026-08-22 属 **W34**（08-17~08-23，周一起算）——计划起点周次与当前周报 weekly-W34 一致；若任务提交跨入 W35（08-24 起）周报落 weekly-W35，以提交时实际周次为准。
>
> **定夺状态**：CHG-016 六项定夺已**全部定案**（①demo_weather 移除 echo 保留 ②key∧Host 配置即启用零 admin 开关 ③实时+三日一次返回 ④零 UI 无设计稿 ⑤iter-22 独立小迭代 Σ3~4 ⑥GeoAPI 控制台启用、不可启用回退百城表+坐标兜底）——本计划**无开放定夺项**；定夺⑥含一个 **CEO 侧前置动作**（见风险①）。

## 迭代目标

用户问「北京今天天气怎么样」，AI 自动调用真实天气工具，给出实时天气与三日预报的真实数据（真凭据端到端走查留档）；demo_weather 同时从注册表移除——admin 自问天气不再命中假数据；存量 pytest 363 + vitest 423 复跑全绿零回退（test_agent_tools 4 处断言面改写映射逐条登记）。

## 任务总览

| 任务 | 对应需求 | 复杂度 | 一句话 |
|------|---------|------|------|
| T0 前置取证与文案定稿 | REQ-053（验收 1/6 前置） | S（1） | GeoAPI 可用性确认（定夺⑥）+ 回退路径验证 + 逐字文案定稿 + 额度口径核实 |
| T1（无） | — | — | 零 UI 无设计任务（CHG-016 定夺④），空缺如实呈现 |
| T2 后端实现 + 天气面收口 | REQ-053（验收 1~6）+ REQ-030 验收 6 改写 | M（2） | weather.py 模块 + 注册与门控 + demo_weather 移除 + 测试 + 走查 + 收口 |

> 任务排序纪律（v1.4.15，定夺④适配口径——沿 iter-19 先例）：设计基线是全部开发任务的前置——本迭代零 UI、无设计稿任务，**T0 技术基线承担「基线先行」职能：T0 留档前不派 T2 开发任务，T0→T2 严格串行**，无未登记并行。
> 含 UI 的任务验收标准须含走查清单留档（v1.4.4）——本迭代零 UI（spec REQ-053「涉及页面 = 不涉及」）；e2e 走查为数据链路取证（3~5 条，非设计走查清单面），如实登记。

### T0 前置取证与文案定稿（S）

**工作项**（脚本/文档级，零产品代码改动）：

1. **GeoAPI 可用性确认（定夺⑥，T0 首步）**：CEO 在和风控制台为凭据（CMGTQUUCJA）启用 GeoAPI 服务后复验 `/v2/geo/city/lookup`（中文/ASCII/热门城市三变体）——2026-08-22 复验 404 未启用；启用成功 → 两步链定案；**CEO 未启用或控制台不可启用 → 回退定案径行**（定夺⑥已预授权：内置约百城 LocationID 静态表 + 模型直传坐标兜底，坐标端点已验证 200），不构成计划变更。
2. **回退路径验证（若走回退）**：百城表数据源定稿（覆盖口径 = 直辖市/省会/计划单列市 + GDP 前百城市，LocationID 以和风公开城市清单为准）+ 坐标兜底实测（小地名样例 3~5 个）+ 表内查无 → 模型降级链路验证。
3. **逐字文案定稿**（沿 EMPTY_RESULT_TEXT 先例，后端拥有、verify 登记）：城市查无 error 文案「未找到该城市」体例定稿；结果组装模板（城市/天气现象/温度/体感/风/湿度/三日概览 + 数据更新时间）定稿；降级标注文案核对（沿 REQ-035 体例复用则零新文案）。
4. **免费额度口径核实**：控制台读数登记（免费订阅天气 API 日限额；响应无 X-RateLimit 头，额度可见性仅控制台——2026-08-22 复验实录）；额度尽自愈链路复述登记（API 返错 → tool error → 模型降级直答）。
5. **机制写实留档**：定位路径定案（两步链 or 回退）+ 城市解析边界 + 文案全量 + 额度口径，登记 `plans/iter-22-verify.md` T0 段。

**判定依据**：

- CHG-016 定夺⑤（T0 余量 = 逐字文案定稿 + 定夺⑥回退路径验证；冒烟三项已随 2026-08-22 凭据复验前置完成）+ 定夺⑥（GeoAPI 启用/回退定案路径）；spec REQ-053 描述（逐字文案 T0 定稿登记 verify）。
- 复杂度取 S 的类比：iter-14~19 T0 单点取证均定 S（脚本级、零产品代码改动）；本次为可用性确认 + 文案定稿 + 额度核实，性质同档。

**验收标准**：

- 定位路径定案结论 + 依据留档 verify T0 段（两步链复验数据或回退表数据源与样例实测）。
- 逐字文案全量定稿登记（结果模板/查无/降级）；额度口径登记。
- 零产品代码改动、无未登记变更（沿 iter-14~19 T0 先例）。

### T2 后端实现 + 天气面收口（M，取 2）

**工作项**：

1. **backend/app/weather.py 新模块**（沿 search.py 体例，预估 120~180 行）：
   - bind/unbind 运行时绑定（main.create_app lifespan，key 与 Host 从 Settings 读入）
   - 定位路径按 T0 定案：两步链（GeoAPI lookup → LocationID）或回退（内置百城表 dict + 坐标兜底直传）
   - `/v7/weather/now` + `/v7/weather/3d` 调用与结果归一化（实时：obsTime/temp/feelsLike/text/windDir/windScale/humidity；三日：fxDate/textDay/textNight/tempMax/tempMin）→ 纯文本组装（T0 定稿模板逐字）
   - 出网治理：白名单 = 配置 Host 单元组（ensure_egress_allowed 传参化）+ `_assert_public_resolution` DNS 解析核验（复用 search 体例）；请求 Host 与配置 Host 不一致 → 拒绝
   - 失败统一 ToolError 机器可读上抛（沿 tavily_search 体例）
2. **tools.py**：weather 注册（gate="weather"，非 admin_only，timeout=10s，location 单必填参数 maxLength 50）；**demo_weather 移除**（定义/假数据表/注册三段删除；echo 保留）。
3. **config.py +2 字段**（weather_key / weather_host，沿 search_key 体例）+ .env.example 占位；**.env 实值写入在 config 字段合入之后**（iter-18 key 击穿 + 本次基线提交 Settings extra_forbidden 拦截 = 同型第三次，顺序纪律登记 verify）。
4. **proxy.py**：`_tool_gates` 增 weather 键（key∧Host 均配置）+ gates 下发字典加 "weather"（定夺②：不新增 admin 开关、不改 settings 端点形状）。
5. **tests/test_weather.py 新增**（预估 10~15 用例，承载 REQ-053 验收 2~5）+ **test_agent_tools.py 4 处断言面改写**（L19 枚举/L39 执行/L119·L125 注册表与 payload——demo_weather 退役映射逐条登记，沿 test_proxy 16 例先例）。
6. **e2e-walkthrough-22 天气链路走查**（3~5 条真凭据：查北京天气北极星链路 / 三日预报问答 / 查无城市降级——沿 iter-13 北极星形态留档截图）。
7. **存量复跑**：pytest 363 + vitest 423 复跑（vitest 零触达背书）+ ruff clean。
8. **天气面收口**：RTM REQ-053 行 + 全局回归基线天气面行同批收口（台账五件套）；CHG-016 后续动作面（生产服务器部署）呈报 CEO 批准后执行。

**零改动面**：db.py / telemetry.py / quota.py / admin.py / 前端全部零改动；零新表零迁移（SCHEMA_VERSION 维持）；search 工具与 research 门控零触碰。

**判定依据**：

- planning.md §2 M 定义「一个模块内的完整功能（含测试与异常分支）」命中：核心 = weather.py 单新模块 + 注册/门控两处小改 + demo_weather 删除 + 测试；复用面全在（search.py 范本逐条对照，CHG-016 现状取证在案）。
- CHG-016 影响评估「T2 后端与走查 M2」定级；L 档「跨模块协作/新技术」不命中（出网工具为既有模式第二实例）。

**验收标准**（REQ-053 验收 1~6 逐条）：

1. **真凭据端到端**：真实 Chrome「查北京天气」→ 工具卡（weather 完成）+ 综合回答含实时与三日真实数据（走查留档）。
2. **降级不崩**：城市查无/假端点失败 → error result → 模型降级直答、回合不崩（pytest）。
3. **门控**：key/Host 任一缺失 → 注册表无 weather、上游 tools 定义不含 weather（pytest 断言 payload）。
4. **出网白名单**：目标改内网/他域 → 零连接（pytest）。
5. **白名单动态化 SSRF**：请求 Host ≠ 配置 Host → 拒绝（pytest 专项）。
6. **demo_weather 移除映射**：test_agent_tools 4 处改写逐条登记（旧断言 → 新断言），功能性删除为决策驱动已批准变更。
- 另：pytest 全绿 + ruff clean + vitest 423 复跑背书；RTM 两行同批收口。

## 携带项追溯

（planning.md §1 映射规则：无独立 REQ 承载的子项填合法来源——v1.4.8）

- **T0 五项**：出处 changes.md CHG-016 定夺⑤（T0 余量定义）+ 定夺⑥（GeoAPI/回退）+ spec REQ-053 描述（逐字文案 T0 定稿）+ 现状取证第 3 条（2026-08-22 复验实录）。
- **T1 无**（零 UI 无设计任务——定夺④）：任务编号保留 T0/T2 与 CHG-016 定夺⑤拆解编号一致，空缺即零 UI 迭代的如实呈现，非漏排（沿 iter-19 同型）。
- **缺陷/观察项携带核查**：plans/defects.md DEF-001~040 全部闭环（DEF-040 v1.0.0 首发冒烟当轮修 a7c6c88）——**本迭代无未闭环缺陷需携带**（v1.4.8）。

## 串行口径（v1.4.15 适配——CHG-016 定夺④）

- v1.4.15：「设计基线是全部开发任务（含纯后端）的前置——严格串行」。
- 本迭代零 UI、无设计稿任务（T1 无；spec REQ-053「涉及页面 = 不涉及」），**T0 技术基线承担「基线先行」职能**（沿 iter-19 定夺⑧先例）：定位路径定案 + 文案定稿留档 `plans/iter-22-verify.md` T0 段之前，不派 T2 开发任务——**T0→T2 严格串行，无未登记并行**。
- 该适配为 CHG-016 定夺④明文定案（2026-08-22 CEO「全部按推荐」），**不构成 tailoring 偏离**。

## 复杂度与容量

**复杂度合计：任务表 Σ3 = S1（T0）+ M2（T2）；迭代排期口径 Σ4 = Σ3 + 余量 1（CHG-016 定夺⑤「Σ3~4 不顶格」，沿 iter-19 同型拆法）**

- **口径对齐**：S=1 / M=2 / L=3~4（iter-13~21 定案数值口径）；容量上限 Σ≤10 维持（连续十六轮零偏差，见 retros/iter-21.md）。
- **算术**：CHG-016 影响评估 = T0 S(1) + T2 M(2) → 任务合计 Σ3；定夺⑤区间上沿 4 中的 1 为**余量**（承载 QA 审计整改 / 当轮 DEF / CEO 控制台动作等待期计划外工作）。本计划算术与定夺⑤一致，无新决策。
- **不顶格理由**：
  1. 小迭代范围窄——单一 REQ-053、零 UI 零迁移零前端；
  2. 复用面全在——search.py 范本逐条对照（CHG-016 现状取证在案），weather 为出网工具模式第二实例；
  3. 凭据可用性不确定已由 2026-08-22 复验前置关闭（now/3d/坐标 200），仅 GeoAPI 一点由 T0 首步定案。

容量校准表：

| 迭代 | 计划 Σ | 实际 Σ | 备注 |
|------|--------|--------|------|
| iter-20（移动端） | Σ6（不顶格） | Σ6 | 零偏差，连续十五轮 |
| iter-21（用量面板） | Σ6（不顶格） | Σ6 | 零偏差，连续十六轮 |
| **iter-22（天气工具，本计划）** | **Σ4（任务 Σ3 + 余量 1，不顶格）** | — | CHG-016 定夺⑤推荐口径 |

- **膨胀处置**：余量 1，新增范围超 30% 触发 planning.md §3 砍范围不延期。
- **备砍序**（任一回退属范围变更，报 CEO 批准后执行并登记）：
  1. 三日预报收窄为仅实时（定夺③范围内收窄，Σ−0.5）
  2. 回退路径百城表收窄至 top50（定夺⑥回退范围内收窄，Σ−0.5）
  3. e2e 走查收窄至北极星 1 条（不推荐——验收 1 留档面单薄）
- **底线** = weather 工具注册 + 白名单/SSRF 全链 + 降级不崩 + demo_weather 移除（echo 保留）。

## 全局回归基线（天气面收口）

（沿 CHG-007 定义与历期延展的验收门槛，RTM 独立行跟踪）

- 迭代关闭时：后端 pytest 363 存量全绿（计划时点口径引 v1.0.0 终态 + DEF-040 修复后实测；启动时以 pytest --collect-only 实测核实——v1.4.16 C）+ tests/test_weather.py 全绿。
- **test_agent_tools 4 处断言面改写映射逐条登记**（demo_weather 退役，决策驱动例外——沿 test_proxy 16 例先例）；除此外功能性删除为零（weather 纯加法）。
- 度量数据全部机器采集（铁律 5）；前端 vitest 423 复跑背书（零触达、无 UI 走查面）；e2e-walkthrough-22 为增量取证面。
- RTM 全局回归基线行天气面收口随 T2 承载收口提交同批更新（v1.4.16 A / v1.4.17 B）。

## 四件套与台账义务（本迭代映射）

- **任务完成四件套（v1.4.13 A / v1.4.14 B 机检）+ 台账五件套（v1.4.17 B）随 T2 代码提交同批强制**：
  1. 代码（weather.py + tools/config/proxy 改动 + 测试）
  2. verify 登记 = `plans/iter-22-verify.md`（T0 段机制写实 + T2 段验收对照）
  3. 周报节 = 当前周 weekly-W34；跨周（08-24 起）落 weekly-W35，以提交时实际 ISO 周为准（v1.4.18 B 周次自检）
  4. 缺陷账 = 当轮如有 DEF 发现同批登记 defects.md
  5. RTM REQ-053 行 + 全局回归基线天气面行同批收口
- **零面如实登记**：无设计稿任务（T1 无，定夺④）、无前端面（vitest 复跑背书即可）；e2e-walkthrough-22 为数据链路走查面（真凭据，非设计走查）。
- 测试计数全部机器采集（v1.4.16 C）。

## 本迭代不做

| 需求/事项 | 原因 |
|------|------|
| 空气质量（/v7/air/now）/ 气象预警（/v7/warning/now） | CHG-016 定夺③定案不入本期；需求浮现入池走 CHG |
| weather admin 开关 / admin 设置面任何改动 | CHG-016 定夺②定案：key∧Host 配置即启用、不新增 admin 开关（额度尽自愈降级 + 部署级下线） |
| 天气专属卡片形态（前端渲染增强） | CHG-016 定夺④定案：零 UI，工具卡折叠结果承载；若做需 design 基线前置（未取备选） |
| 生产服务器部署（.env 两变量 + 容器重建 + 线上冒烟） | CHG-016 后续动作面：随 T2 交付后呈报 CEO 批准执行（发布性质动作，不占迭代容量；G3 类决定归 CEO） |
| 暂缓池其余条目（RAG/知识库、搜索供应商对比评估、A2 深化①②③、webhook、管理后台移动化等） | 暂缓池全部条目维持不排（spec §4；纳入须走变更流程——铁律 1） |
| v1.0.x 版本发布 | 本迭代交付后是否出 v1.0.1 随部署动作由 CEO 定夺（G3 归 CEO），不预设 |

## 风险清单

**风险① GeoAPI 启用依赖 CEO 控制台动作（唯一外部前置）**

定夺⑥推荐路径的前提是 CEO 在和风控制台为凭据启用 GeoAPI 服务；未启用时两步链不可用。

应对：

- T0 首步即确认：CEO 启用后复验三变体；**未启用/不可启用 → 回退定案径行**（定夺⑥已预授权百城表 + 坐标兜底，不构成计划变更、不新增定夺项）；回退实现已含在 T2 工作项 1 拟稿内（dict 常量 + 坐标分支），Σ 不变。
- 回退口径下的边界（表外小地名靠坐标兜底，模型坐标可能偏差）在 verify T0 段如实登记为已知边界。

**风险② 和风免费订阅额度**

凭据为免费档，日限额口径待 T0 核实（响应无 X-RateLimit 头，可见性仅控制台）；额度尽可能影响线上可用性。

应对：

- 额度尽自愈已内建：API 返错 → ToolError → 网关 error result → 模型降级直答或转 search，回合不崩（REQ-053 异常分支）。
- admin 遥测工具用量视图自动涵盖 weather 行（kind='tool' 聚合零改动），额度异常可观测。
- 部署级止血 = .env 移除 key 重启（定夺②口径）。

**风险③ 白名单动态化的 SSRF 新模式**

白名单域取自配置而非固定常量（与 search 的差异点），配置错误或注入场景下的出网面需要专项保障。

应对：

- 验收 5 专项断言：请求 Host ≠ 配置 Host → 拒绝（pytest）；字面 IP 与 DNS 解析期核验沿用 search 全口径（零逻辑变化）。
- Host 配置缺失/非 https → 工具不注册 + 启动 warning（防御性兜底沿 search「理论不可达」体例）。
- config 字段合入先于 .env 实值写入（同型第三次教训的顺序纪律，verify 登记）。

## 实际结果（复盘时回填）

- 完成任务：T0 + T2 全交付（T1 无——零 UI 无设计任务，定夺④空缺如实呈现）；附带交付：上线后缺陷当轮修两枚（DEF-041/042）与 v1.1.0 发布上线（迭代范围外，CEO G3 批准 2026-08-23）
- 复杂度：计划 Σ4（任务 Σ3 + 余量 1）vs 实际完成 Σ4（零偏差，连续十七轮；余量 1 实际吸收走查脚本三轮迭代 + 两次提交门禁拦截处置，零延期）
- 砍掉的范围与原因：无（备砍序三条未动用）
