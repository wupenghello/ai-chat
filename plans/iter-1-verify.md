# iter-1 集成验证与走查记录（T7）

日期：2026-08-15 · 验证人：主会话（开发工程师）+ 浏览器实测

## 自动化验证

- 单元测试：**26/26 通过**（api/client 8、settings 6、sessions 6、settings-form 3、composer 3）
- 类型检查 `vue-tsc -b`：通过
- 生产构建 `npm run build`：通过（gzip 后 41 KB JS / 2.8 KB CSS）

## 浏览器实测（dev server，逐项）

| 项 | 结果 |
|----|------|
| 未配置密钥发送 → toast + "前往设置"引导，消息不发出 | ✅ |
| 设置页三配置项：校验、保存持久化、清除密钥 | ✅（单测+实测） |
| 真实流式对话（DeepSeek，deepseek-chat） | ✅ 逐字流式渲染，回复完整 |
| 多轮上下文记忆（第二轮正确答出第一轮的名字） | ✅ |
| 刷新后会话全部恢复（2 会话 6 消息，active 正确） | ✅ |
| 错误路径：GLM 429=余额不足 → 错误气泡 + 供应商原因透传 | ✅ |
| 视觉抽查：主色 #3370FF、按钮 13px/36px、侧栏 264px | ✅ 与 design-iter-1 一致 |

## 冒烟发现并修复的缺陷（3 个）

1. IndexedDB 无法结构化克隆 Pinia 响应式代理 → persist 深拷贝（DataCloneError，会话曾整段丢失）
2. 设置页无返回对话入口 → 侧栏新建/选择会话切回 chat 视图
3. 429 错误一律显示"请求频繁"误导用户 → 透传供应商具体原因（GLM 1113 余额不足）

## 未尽事项（如实记录）

- **GLM 真实流式未验证**：key 余额不足（429/1113），待充值后补验（协议同为 OpenAI 兼容，风险低）
- 视觉走查按 design-iter-1 状态矩阵 11 行逐项核对未全部执行，仅抽查关键令牌；完整走查在发布 checklist（G3）前补齐
- 首屏 < 2 秒：dev 模式下首帧约 0.5s（构建产物 41KB gzip），发布前用构建版实测留档
