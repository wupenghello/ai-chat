# 迭代计划 — iter-18（2026-08-21 ~ 2026-09-04）

> 状态：**已批准（2026-08-21，CEO「全部批准」= 计划整体批准 Σ9 不顶格 + research.phase 维持不提案〔按推荐〕，按草案落盘）。**
> 变更依据：requirements/changes.md **CHG-012**（已批准 2026-08-21，CEO「批准」= 整体批准 + 十定夺全按推荐，基线 req-baseline-v9，9aefb9f + tag 已推 GitHub）——本迭代 = 七期路线第六期 **D1「deep-research 子代理 + SSE 心跳」**，排入 REQ-045（P0）/ REQ-046（P0）/ REQ-047（P1）+ REQ-030/036 改写实现面 + 波及 9 项（REQ-001/014/025/031/032/033/034/035/037）复验。A1/A2/B1/B2/C（REQ-030~043）已于 iter-13~17 全交付并关闭 G4，D1 复用面全在线：run_turn 循环（参数化基座）、工具网关与 search（Tavily）、SourceCard 引用卡、telemetry 行基建、nginx Compose 部署形态（iter-9 T3）。
> 迭代时长：名义标准 2 周（2026-08-21 ~ 2026-09-04，项目 tailoring.md「迭代长度：2 周」适用，无偏离需登记）；沿近期单日迭代节奏（iter-13~17 均 1~2 天内收口），实际周期以开发 → 测试 → QA 审计 → Code Review → 复盘全部收口为准。
> 定夺状态：CHG-012 十项定夺已**全部定案**（②编号自 045 起 / ③受控 ReAct 变体 / ④心跳 SSE 注释帧 20s 全回合生效——审核稿点名「T0 必须定夺」项关闭 / ⑤1 发起 = 1 回合 / ⑥16 步 + 900s / ⑦输入框开关 + mode 字段同会话混用发送后复位 / ⑧三与门承载不新增独立开关 / ⑨报告 ≤3000 字引用复用 research.phase 首版不加 + reason 'time_limit' 加法 / ⑩断连沿现行取消口径）——本计划**无开放定夺项**；定夺⑥护栏参数由 T0 取证在 ±4 步 / ±300s 授权内校准（取证即定死，登记不走变更），心跳间隔 20s 由 T0 反代实测定档（定夺④附属）。

## 迭代目标

丢一个开放问题给「深度研究」模式：AI 自动拆解问题、多轮联网搜索、边做边给进度，交付一份带引用来源的综合报告（≤3000 字）；多步长回合与普通回合的连接都不再被反代静默超时掐断（SSE 心跳，前端零改动）；普通对话与既有功能零回退（前端 364 + 后端 312 存量用例回归全绿）。

## 任务表

> 设计基线是**全部开发任务（含非 UI 的纯后端/数据面任务）的前置**，严格串行（v1.4.15）：T1 design-iter-18 未基线前不派任何开发任务；T0 为取证（零产品代码改动），不是开发任务，排在设计前（沿 iter-14/15/16/17 T0 先例）。
> 含 UI 的任务，验收标准必须包含「design-iter-N 走查清单留档」（v1.4.4：走查是任务级门槛，不是迭代末补做）。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 前置取证（research prompt 真实冒烟 + nginx 心跳实测 + 护栏初校）：① research 指令 prompt 真实 DeepSeek 冒烟（统一 key + 真 Tavily search）——预置开放性问题素材，实测「计划（子问题分点）→ 逐项检索（每子问题 ≥1 次 search 或明示无需）→ 综合（带引用标注 + ≤3000 字）」方法论遵循度与报告质量，定稿 research 指令 prompt 逐字登记（后端拥有、逐字断言面，沿 EXTRACT_PROMPT R1 / SUMMARY_PROMPT R2 定稿先例）② nginx 反代心跳实测：本地 Compose 形态（iter-9 T3，nginx 默认 proxy_read_timeout 60s）下静默 SSE 流不断连验证（REQ-045 验收 4：90s 静默流）+ 心跳间隔 20s 定档校准（约 1/3 余量口径核对）③ 护栏参数初校：真实研究回合步数分布与总耗时实测，16 步 / 900s 在 ±4 步 / ±300s 授权内的校准结论（定夺⑥）④ 沿 iter-14~17 T0 取证模式留档 | REQ-046（描述「文案后端拥有、T0 定稿」；内容 3.2「框架定死，文案 T0 定稿」）+ REQ-045（验收 4 反代实测 T0 取证留档；定夺④间隔实测定档）+ 定夺⑥（T0 校准授权） | S | 类比：iter-14/15/16/17 T0 单点取证均定 S（路径明确、零产品代码改动）；本次为一次真实研究回合冒烟 + 一次反代实测 + 一组护栏初校，性质同档取 S（若冒烟暴露缺陷按登记纪律另立 DEF，不预先吃余量） | research 指令 prompt 定稿文案逐字登记（逐字断言面）+ 心跳间隔定档与 90s 静默不断连实测结论留档 `plans/iter-18-verify.md` T0 段 + 步数/耗时分布与护栏校准结论留档（授权内调整回填 changes.md CHG-012 定夺⑥字段，不走变更）；零产品代码改动、无未登记变更 |
| T1 design-iter-18 设计基线：输入区「深度研究」模式开关与进度/到顶标注——① 模式开关形态（ComposerBox 加法：候选输入框角落 toggle + 模式提示标签，定夺⑦；开启态/关闭态/**禁用态**（research_available=false 时禁用 + 提示文案）+ **发送后复位**（防高成本模式误连发）② 进度与报告呈现锚点（零新增渲染面：计划/报告经 text.delta 打字机、搜索经既有 ToolStepCard、来源经既有 SourceCard——设计稿承载布局叙事与既有组件复用口径；**research.phase 阶段条提案权**：设计稿可提案（提案则后端加法事件 + 前端消费，帧格式 T0 定），不提案则工具步骤卡密度天然呈现进度——定夺⑨附属）③ 步数/时长到顶标注态（沿 maxSteps 标注体例，含 reason='time_limit' 新终态文案）+ 断连态（沿 REQ-001 既有分支）④ 入口可用性判定源口径章节随稿（GET /api/quota 加法字段 research_available = 三与门判定，开关禁用态数据面）⑤ 走查清单随稿（亮/暗双主题 + 开关态/禁用态 + 进度态 + 步数与时长到顶标注态 + 断连态；几何断言面 v1.4.11 B、行为断言面适用项 v1.4.13 B；= REQ-047 验收 6 断言面）⑥ REQ-001 发送/停止、REQ-010 停止生成、暗色主题既有口径零回退锚点逐条映射既有走查编号 ⑦ **卫生项：spec REQ-047「涉及页面」指针随基线零滞后回填（v1.4.10 A 防线，核对清单第 8 项）** | REQ-047（主）+ REQ-001/010（既有口径零回退锚点） | M | 类比：iter-15 design-iter-15 为 1 遥测视图触点 + API 口径章节定 M、iter-17 design-iter-17 为 1 页级分区七态 + API 口径章节定 M；本次为 1 个输入区开关触点（含禁用/复位/标签三态）+ 进度叙事锚点 + 到顶标注 + API 口径章节——触点面积小于 iter-17 页级分区、大于 iter-16 菜单项，取 M | CEO 批准基线（tag design-iter-18）；开关形态与样件文案定死（逐字断言面——v1.4.10 B）；research.phase 提案/不提案定案；到顶标注文案定死；research_available 口径定稿；走查清单随稿；REQ-001/010 零回退映射；spec「涉及页面」指针零滞后回填 |
| T2 后端核心：REQ-045 心跳 + REQ-046 编排——① 心跳 watchdog（proxy.py stream() 协程内周期补发 SSE 注释帧 `: ping\n\n`，间隔 heartbeat_interval 默认 20s .env 可覆盖、非法值 ≤0 兜底默认；watchdog 异常不杀流；注释帧不进事件序、不落遥测；**对全部回合生效**——定夺④⑩）② turn 端点 mode 加法可选字段（缺省 'chat' 行为零变化；TurnRequest 加法校验，非法值 422）+ 三与门校验（不满足受理即 422 research_unavailable 零上游调用零事件流——与 search 下发门同源一处读两用，proxy.py L218-222 现状）③ research.py 薄模块（研究指令文案〔T0 定稿逐字〕+ 护栏参数 + 编排配置，预估 100~200 行）+ **run_turn 参数化承载 research 回合**（CHG-012 内容 3.1 单实现优先：可选 research 配置 = 独立 max_steps=16 / 总时长 deadline=900s / reason 枚举扩展；仅当参数化污染成本高于分叉时允许薄复制，实现级决策登记 verify 不走变更）④ 注入位：research 指令 system 消息挂载 system[1] 动态尾区之后、记忆消息之前（六层注入序，REQ-036 改写承载；普通回合不含，注入序零变化）⑤ 双护栏：步数到顶 reason=max_steps（沿 REQ-030 体例）+ 时长到顶 reason='time_limit'（turn.end reason 加法）——已生成内容保留落库 ⑥ 计费沿 REQ-034 既有点位零改动（1 发起 = 1 回合，quota.py 零改动）⑦ 遥测 llm/tool 行 endpoint='research' 加法值（kind 枚举与行形状零变化）⑧ GET /api/quota 加法字段 research_available（= 三与门判定，REQ-047 禁用态数据面）⑨ config 三参数（max_research_steps / research_total_timeout / heartbeat_interval，T0 校准值，.env.example 占位） | REQ-045（主）+ REQ-046（主）+ REQ-030/036 改写实现面 + REQ-001/014/025/031/033/034/035/037 波及复验（REQ-032 波及为前端承载归 T3） | L（取 4） | planning.md §2 L 定义「跨模块协作」命中：六模块协作（proxy.py 心跳+mode+三与门 / research.py 新模块 / agent.py run_turn 参数化 / telemetry.py endpoint 加法 / quota 端点 / config.py）；对比 iter-17 T2（七模块 + 首次常驻任务）取 4，本次无常驻任务与新存储面（零新表零迁移），但**心跳触及既有全部回合流**（回归面最宽）+ run_turn 参数化为循环核心改造，不确定性面不小，取 4 不取下沿 3 | REQ-045 验收 1~4 逐条（①静默保活原始字节断言：假上游 45s 无 delta → ≥2 个 `:` 注释帧、间隔 ≤ heartbeat_interval+5s / ②前端零感知：parseSse 零事件产出（vitest 联动归 T3，后端字节断言本任务承载）/ ③普通回合零回退：delta 密集回合事件序不变，REQ-030 验收 1 复跑全绿 / ④反代实测 T0 留档 + 走查脚本一条）+ REQ-046 验收 1~8 逐条（①帧级断言：规划+2×search+综合事件序逐帧 + 首步请求体含 research 指令消息（位置 = system[1] 之后记忆之前、内容 = T0 定稿逐字）+ system[0] 人设/时间行口径不回退（含记忆预置用例）/ ②步数硬上限 max_research_steps=3 注入 → turn.end(max_steps) 已生成保留不悬挂 / ③时长护栏 → turn.end('time_limit') 无孤儿任务 / ④计费：一回合并 5 次上游调用 → turns+1、tokens=5 次之和数值断言 / ⑤门控拒绝：三与门不满足 → 422 零上游零事件流；mode 缺省普通回合零影响 / ⑥网关复用：非法入参 search → error 回填回合继续；tool 行 endpoint='research' 落库断言 / ⑦断连取消：取消后零新增上游调用、工具协程终止、无孤儿任务 / ⑧卫生：指令文案/事件流/日志/遥测检索不到 key）；mode 缺省回合与既有验收复跑零回退；pytest 全绿 + ruff clean |
| T3 前端模式入口 + 全局回归收口：① ComposerBox 模式开关加法（开启/关闭/禁用态 + 模式提示标签 + **发送后复位**，按 design-iter-18 定稿，样件文案逐字）② client.ts runChatTurn 加法可选 mode 参数（缺省不传 = 现状零变化）+ sessions.ts 事件消费适配（进度经既有 text.delta/ToolStepCard 渲染；research.phase 若 T1 提案则消费加法事件，未提案零改动）③ time_limit 标注态（沿 maxSteps 体例，design-iter-18 文案）④ 前端零感知断言（REQ-045 验收 2：注释帧经 parseSse 零事件产出）⑤ 全局回归基线 D1 面收口走查 | REQ-047（主）+ REQ-032（波及：渲染复用不新增 block 类型）+ REQ-045 验收 2（前端面） | M（取 2，M 下沿） | 类比：iter-16 T3「端点 + 菜单项 + 懒回填 + 聚合 + 卡」定 M2；本次为输入区开关（三态+复位）+ client 参数加法 + 事件消费适配（**进度呈现纯复用既有渲染面，零新增组件**——CHG-012 Σ9 推荐的口径：T3 取 M 下沿 2，进度呈现纯复用渲染） | REQ-047 验收 1~6 逐条（①开关与载荷：开启发送请求体含 mode='research'、关闭不含、发送后复位（vitest 载荷形状与复位断言）/ ②进度渲染：假事件序 → 工具步骤卡序列 + 文本段累积、blocks 定型含 text/tool_call/tool_result 段 / ③报告引用：sources → 引用卡渲染 = REQ-035 复用断言、SourceCard 既有用例零回退 / ④time_limit 标注呈现 / ⑤前向兼容：未知 research 加法事件静默跳过零崩 / ⑥design-iter-18 走查清单留档：亮/暗双主题 + 开关态/禁用态 + 进度态 + 步数与时长到顶标注态 + 断连态）；REQ-045 验收 2（parseSse 零感知 vitest）；vitest 全绿 + guard:style + 生产构建；全局回归基线 D1 面收口：pytest 312 + vitest 364 存量全绿（REQ-030 验收 1 事件序断言排除心跳帧等改写用例逐条登记映射），功能性删除为零 |

**携带项追溯**（planning.md §1「对应 REQ」映射规则：无独立 REQ 承载的子项填合法来源，来源编号已回查出处文档核对、不凭记忆——v1.4.8）：
- **T0 取证三项**：出处 changes.md CHG-012 内容 3.2「文案后端拥有、T0 定稿、逐字断言面登记 verify——沿 SUMMARY_PROMPT R2/EXTRACT_PROMPT R1 先例」+ 内容 3.3「间隔以本地 nginx 实测定档（T0 取证，不凭文档臆断，沿 iter-14 T0 模式）」+ 定夺⑥「T0 校准授权 ±4 步/±300s 登记不走变更」。
- **心跳与编排并入 T2**（不另立任务）：REQ-045/REQ-046 均为 T2 描述既有承载（连接层与编排层同批交付——心跳为 deep-research 前置且作用域为全部回合，拆开交付反而产生中间态回归面），属 REQ 内任务归位拆分，非 REQ 外携带。
- **缺陷/观察项携带核查**：plans/defects.md 截至 DEF-037 全部已修复/已销账/已接受闭环（DEF-037 Esc 双重消费 iter-17 当轮修复登记）；qa-audit-iter-17 的 2 NCR 当轮整改闭环、6 OBS 已处置（OBS-2 提交规范四处字面边界已随制度 v1.4.16 B 澄清收口）——**本迭代无未闭环缺陷需携带**（v1.4.8）。

复杂度合计：**Σ9 = S1 + M2 + L4 + M2（容量 Σ≤10，不顶格，余量 1）**

- 口径对齐（沿 iter-13~17 定案数值口径：S=1 / M=2 / L=3~4，process/planning.md §2 只定义质性档位）；容量上限 Σ≤10 维持（iter-6~17 连续十二轮零偏差校准，见 retros/iter-17.md）。
- CHG-012 影响评估拆解预估 Σ9~10，**推荐 Σ9 不顶格**（沿「默认不顶格、顶格需三条理由」口径——retros/iter-17 §4；CHG-012 在案理由：若 design-iter-18 提案 research.phase 阶段条等增强则回 Σ10 顶格，三条理由已备）。本计划按 Σ9 起草：T3 取 M 下沿 2（进度呈现纯复用既有渲染面，零新增组件）；research.phase 默认不提案（定夺⑨附属，不提案零成本）。
- 膨胀处置：余量 1，新增范围超 30% 触发 planning.md §3 砍范围不延期。备砍序（任一回退属范围变更，报 CEO 批准后执行并登记；CHG-012 内容 5 在案）：a) T3 收窄至开关 + 载荷 + time_limit 标注（进度呈现纯复用，Σ−1）；b) research.phase 增强项不提案（默认路径，零成本）；c) 心跳作用域收窄仅 research 回合（Σ−0.5，**不推荐**——普通回合静默风险同在，连接层修复不分模式的取证结论在案）。**REQ-046 编排核心与 REQ-045 心跳为底线不回退**（审核稿 D1 主体 + 「不定会返工」点名项已定案落地）。
- 若 CEO 定夺 research.phase 提案（T1 设计稿层面）：Σ 回 10 顶格（CHG-012 三条顶格理由在案：审核稿原定级 Σ10（七期仅 A1/D1 同级）/ D1 含首个「单回合多阶段长时编排」+ 连接层修复双主线 / 心跳触及既有回合流回归面）。

**全局回归基线（D1 面收口）**（沿 CHG-007 定义、CHG-009/010/011/012 延展的验收门槛，RTM 独立行跟踪）：迭代关闭时前端 364 + 后端 312 存量用例全绿（计划时点口径引 iter-17 终态收口记录，启动时以 pytest --collect-only / vitest list 实测核实——v1.4.16 C）+ 本轮新增全绿；REQ-030 验收 1 事件序断言排除心跳注释帧等改写用例逐条登记映射（旧断言 → 新断言，CHG-012 测试基线段既有口径）；功能性删除为零；度量数据全部机器采集（铁律 5）；RTM 全局回归基线行 D1 面收口为全期达成（v1.4.16 A：随承载收口任务提交同批更新）。

## 本迭代不做

| 需求/事项 | 原因 |
|------|------|
| research.phase 阶段条（「规划中/检索中/综合中」显式阶段指示） | CHG-012 定夺⑨定案首版不加：非基线验收范围（REQ-047 验收 1~6 均不依赖它），进度可见性由工具步骤卡密度承载（验收 2）；归宿 = REQ-047 描述内「可提案增强」选项——若 T1 design-iter-18 提案则 Σ 回 10 顶格（CHG-012 三条理由在案），本轮不提案则留池，后继迭代纳入**无需走 CHG**（基线文本已含该选项口径） |
| deep-research 独立 admin 开关与用量可见性 | CHG-012 定夺⑧/3.7 定案：可用性由 search 三与门承载、遥测 endpoint='research' 已区分、admin 聚合天然涵盖；独立开关/专属面板入暂缓池，纳入走 CHG |
| 异步研究任务（断连续跑/后台执行/完成通知） | CHG-012 定夺④⑩定案：心跳方案定案（in-process 断连取消口径），后台续跑路径（research_jobs 任务表轮询方案）连同拟稿存档 changes.md CHG-012 内容 3.3 留池，需求浮现走 CHG |
| D2 hooks（生命周期事件回调） | 七期路线第 7 期（暂缓池，候选非承诺，纳入走 CHG；可与移动端搭班）——D1 交付后为下一候选 |
| 移动端主界面适配 | CHG-007 定序：让位至 agent UI 形态稳定后（可与 D2 搭班）——CEO 2026-08-17 批准；纳入走 CHG |
| A2 体验深化主题包 ①引用上标/角标联动 ②搜索质量与时效调优 ③搜索过程可视化增强 | CHG-009 定夺⑦定案不整包搭班：①②③ 留暂缓池（②建议与「搜索供应商对比评估」迭代合并评估） |
| 搜索供应商对比评估（Tavily vs 博查横向专项） | 暂缓池条目（CEO 2026-08-18 指示）：以真实运行数据为主要输入，D1 的多轮搜索将新增一批真实数据，积累后择机排期，纳入走 CHG |
| 真实天气工具（和风天气类专用 API） | 暂缓池条目（iter-14~17 计划定案不纳入）：凭据 + 专属 Host 已验证备存服务端 .env，择机再评估 |
| GLM 自填端点多 system 兼容随测 | DEF-002 口径：GLM 429 余额不足，CEO 不充值；充值恢复后随测（CHG-009 T0 遗留，非本迭代承诺） |
| v0.5.0 发布 | 独立卡服务器（VPS/域名/HTTPS）就绪，不属本迭代范围；发布决定（G3）由 CEO 批准（CHG-012 发布影响：不受本变更影响） |

## 风险清单

| 风险 | 应对 |
|------|------|
| ① 真实研究回合行为不确定：上游对「长指令 + 多轮搜索 + 综合报告」的方法论遵循度、步数分布与耗时直接决定体验与护栏参数合理性（审核稿 §九「自动拆解、多轮搜索、边做边给进度」的承诺面） | T0 真实 DeepSeek + 真 Tavily 冒烟先行（方法论遵循度与报告质量实测为实现输入，不凭文档臆断，沿 EXTRACT_PROMPT R1 定稿先例）；护栏参数带 ±4 步 / ±300s 校准授权（定夺⑥，取证即定死不走变更）；检索全部失败降级直答（沿 REQ-035 体例，回合不崩）；报告超长属 prompt 纪律问题，T0 冒烟校准 |
| ② 心跳触及既有全部回合流的回归面：注释帧与事件流的交错、断连取消传播、普通回合事件序断言（REQ-030 验收 1 为全局回归锚点） | 注释帧非事件的取证结论在案（parseSse 只认 data: 行，client.ts L95-107）；双向断言承载：REQ-045 验收 1/3（心跳存在但事件序不变）+ REQ-030 验收 1 复跑（事件序断言排除心跳帧，改写映射登记）；watchdog 异常不杀流（心跳为尽力而为保活，不构成回合成败要件）；心跳对全部回合生效避免双路径（定夺⑩） |
| ③ T2 为本期最重任务（L=4，六模块协作，run_turn 参数化为循环核心改造——参数化污染 vs 薄复制的实现级分叉） | 单实现优先原则定死（CHG-012 内容 3.1：首选 run_turn 参数化，仅当参数化污染成本高于分叉时允许薄复制，实现级决策 T2 登记 verify 不走变更）；六子步骤逐项验收（心跳 → mode/三与门 → research.py → 注入位 → 双护栏 → 遥测/端点），**mode 缺省回合零变化 = 逐步验收的恒等基准**（每步回归锚点）；备砍序 a 预备；开发纪律沿 v1.4.15 串行 + v1.4.14 B 亲跑核验采信 |

## 实际结果（复盘时回填）

- 完成任务：T0~T3 全部（S1+M2+L4+M2）；新增范围 = QA 整改（2 NCR + 2 OBS）+ DEF-038 处置 + 真 Tavily 随测三连收口（零容量占用，余量 1 吸收）
- 复杂度：计划 Σ9 vs 实际完成 Σ9（连续十三轮零偏差；不顶格决策第二例正面证据）
- 砍掉的范围与原因：无（备砍序未触发）
- 回归：pytest 312→332 / vitest 364→378 / 走查 41 PASS/0 FAIL（DEF-038 改规格处置后）；QA 有条件通过 2 NCR 当轮整改；Code Review CEO 认可（5 非阻塞取舍留档）；G4 于 2026-08-21 关闭（复盘 A/B 落制度 v1.4.17：周报门禁仓库定位修复 + 台账五件套）
