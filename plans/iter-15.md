# 迭代计划 — iter-15（2026-08-19 ~）

> 状态：**已批准（2026-08-19，CEO 原话「批准」，未提修改，按草案原文落盘）**。
> 变更依据：requirements/changes.md **CHG-009**（已批准 2026-08-18，CEO「全部按推荐批准」，基线 req-baseline-v6）——本迭代 = 七期路线第三期 **B1「prompt 静态/动态分割 + 请求级遥测 + admin 遥测面板」**，排入 REQ-036（P0）/ REQ-037（P0）/ REQ-038（P1）+ REQ-033/008/025 改写实现面 + 波及 4 项（REQ-002/030/031/034）复验 + 定夺④旧透传端点下线执行；A1/A2（REQ-030~035）已于 iter-13/14 全交付并关闭 G4。
> 迭代时长：标准 2 周（2026-08-19 起算；项目 tailoring.md「迭代长度：2 周」适用，无偏离需登记）。
> 定夺状态：CHG-009 八项定夺已**全部定案**（②策略一两段式分区 / ③工具说明不文本化 / ④旧端点方案 A 随 B1 直接下线 / ⑤保留期 90 天 / ⑥成本口径仅统一 key + .env 单价三变量 / ⑦主题包④⑤收编、①②③留池 / ⑧人设授权 T0 中性默认稿、CEO 保留否决）——本计划**无开放定夺项**，T0 仅执行取证两项 + 人设稿（非定夺项）。

## 迭代目标

部署者可在管理后台按日查看真实成本估算、缓存命中率与工具用量（全部机器采集，缺失如实标缺失、不造数）；AI 对话的上下文组装完成静态/动态分区固化（上游前缀缓存受益降本，B2 压缩与 C 记忆有明确挂载点），旧透传端点下线、回合端点成为唯一对话入口；用户侧体验零感知零回退（前端 305 + 后端 224 存量用例回归全绿）。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-N` tag）才能开始开发，设计任务排在开发任务之前。
> 含 UI 的任务，验收标准必须包含「design-iter-N 走查清单留档」（v1.4.4：走查是任务级门槛，不是迭代末补做）。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 前置取证 + 产品人设中性默认稿：① 真实 DeepSeek（统一 key）usage 缓存字段形状冒烟——字段名/数值语义取证留档，作为 REQ-037 字段映射的**唯一实现输入**（审核稿 B1 验收口径「指标定义先核上游字段可得性」，不得凭文档臆断）② 自填端点多 system 消息兼容性冒烟 + 字段矩阵同取（沿 iter-14 T0 取证模式；若实测某端点明确拒绝多 system → 档案级「合并 system 单段」回退开关按 REQ-014「支持工具」开关同哲学另登记，CHG-009 不预设，冒烟未见问题则不做）③ 产品人设中性默认稿供 CEO 审（定夺⑧，CEO 保留否决权；若否决当 T0 重拟，S 级返工不阻塞开发）——定稿后以 `backend/.env` 变量注入形态登记、`.env.example` 占位 | REQ-037（验收 2 取证条款）+ REQ-036（人设，定夺⑧；异常分支自填多 system 冒烟） | S | 类比：iter-14 T0 单点取证（S 档，路径明确）；本次两项冒烟 + 一份文案稿，零产品代码改动，性质同档略宽仍取 S（若发现缺陷按登记纪律另立 DEF） | 两项冒烟结论与人设稿（CEO 审签）留档 `plans/iter-15-verify.md` T0 段；字段形状结论同步 T2 字段映射口径；人设稿定稿文案逐字登记（静态前缀内容物，跨请求字节恒定的源）；零产品代码改动、无未登记变更 |
| T1 design-iter-15 设计基线：REQ-038 遥测视图形态——每日成本估算卡（仅统一 key 计成本、自填模式列 token 不计成本并明示「用户自带密钥」；单价 admin 只读）/ 缓存命中率（缺失时段缺失标注）/ 工具用量视图（按工具名×状态聚合，暂缓池主题包⑤由本视图天然承载）；API 口径章节随稿定案（遥测聚合端点形状、缺失与未配置语义）；缺失态/单价未配置态两异常形态；**卫生项：spec REQ-038「涉及页面」指针随基线零滞后回填（v1.4.10 A 防线）** | REQ-038（主）+ REQ-025/029（既有口径零回退锚点） | M | 类比：design-iter-8/11/12/13/14 同规模单域原型一次基线均定 M（iter-14 为 4 触点章节 + 定夺登记定 M）；本次 1 个遥测视图触点 + API 口径章节 + 三态（正常/缺失/未配置），规模同档偏轻 | CEO 批准基线（tag design-iter-15）；遥测视图形态与 API 口径定死；缺失标注与「单价未配置不估算」语义定稿（样件文案逐字断言面——v1.4.10 B）；走查清单随稿（亮/暗双主题、几何断言面——v1.4.11 B、行为断言面适用项——v1.4.13 B）；REQ-025/029 零回退组逐条映射既有走查编号；spec「涉及页面」指针零滞后回填 |
| T2 后端 B1 核心：① prompt 两段式分区组装——assemble_context 单点改造（system[0] 静态前缀=人设 .env 注入、跨请求字节恒定；system[1] 动态尾区=用户提示词如有 + 时间行；空配置回退基线 v5 单 system 形态；tools 字段不文本化；20 轮窗口与 user 锚定截断零变化）② 迁移 v8 telemetry 明细表 + 双端采集（turn 端点 llm 行/tool 行 + 旧端点 legacy 行，turn_id=NULL）③ usage 分项与缓存字段映射（以 T0 取证为输入；上游不返回记 NULL，铁律 5）④ 遥测写失败不阻塞主路径（warning 日志不补造）⑤ 90 天按自然日惰性清理（清理失败不阻断）⑥ **旧透传端点下线执行（定夺④，先 legacy 行取流量证据 → 后端点删除 + test_proxy 16 例退役映射登记 + proxy_smoke 迁 turn，落地核对清单第 9 项勾验）**⑦ 人设/单价 config 变量（`.env.example` 占位）⑧ 卫生：表与日志零 key、零消息内容、零工具结果全文 | REQ-036（主）+ REQ-037（主）+ REQ-033/008 改写实现面 + REQ-030/031 波及复验 + 定夺④执行（决策驱动，追溯见任务表下注记） | L（取下沿 3） | planning.md §2 L 定义「跨模块协作」命中：agent.py 组装 / 工具网关执行点 / proxy.py legacy 行 / db.py 迁移 v8 / config.py 五模块协作 + 既有测试改写面；取 3 不取 4 的理由：全部改造在既有 agent 运行时框架上（组装器已单点收敛、迁移体系 v6/v7 先例、采集点均挂既有调用路径），无本项目未用过的新技术；分割（M 口径）与下线执行（S 口径）为任务内携带子项——沿 iter-13 T1 单任务携带多 REQ 先例，不推高 Σ | REQ-036 验收 1~5 逐条（system[0] 跨用户跨时刻逐字节相同 / 动态尾区隔离：system[0] 检索不到时间串与用户提示词 / 第 30 轮仍仅最近 20 轮 / 「组装等价」类用例改写逐条登记映射 / 空配置回退与基线 v5 逐字段等价，均 pytest + MockTransport 捕获）；REQ-037 验收 1~6 逐条（3 调用恰 3 行逐值一致 / 缓存字段如实性含缺失 NULL / test_quota 全套零改动复跑全绿 / 故障注入主路径隔离 turn.end 正常 / 卫生断言 / search·echo 各一次 tool 行与网关日志四字段一致）；REQ-033 验收 1 新口径 + REQ-008 验收 3（超 20 轮系统提示词仍在动态尾区段首位）复验；旧端点已删（请求 404 取证）+ 16 例退役映射登记 + proxy_smoke 迁 turn 取证；quota.py 与 usage_daily 数据面零改动验证；pytest 全绿 + ruff clean |
| T3 admin 遥测面板（前后端）与全局回归收口：① admin.py 遥测聚合端点（加法扩展，沿 iter-12/14 overview 先例；成本=Σtokens×单价三项分项、命中率=Σhit/(Σhit+Σmiss)、工具用量按 tool_name×status 聚合——全部机器聚合无手工修正入口）② AdminView 遥测视图（按 design-iter-15；单价未配置 → 成本卡不估算并提示）③ 403 门禁沿 get_admin_user；④ 全局回归基线 B1 面收口走查 | REQ-038（主）+ REQ-025/029（口径零回退） | M | 类比：iter-12 T1（后端聚合 M）+ T2（前端视图 M）之合并轻量版——本次仅一个聚合端点 + 一个视图，且数据层只读 T2 已交付的 telemetry 表、无新数据模型；拆解：触点单一、无主对话面触达（REQ-001~019 零触达） | REQ-038 验收 1~4 逐条（既有六端点形状零变化或仅加法字段 + 既有 admin pytest 用例零改动复跑全绿 / 造数聚合数值断言：成本=tokens×单价精确值、命中率数值、缺失→缺失标注 / 普通用户访问遥测端点 403 零泄露 / design-iter-15 走查清单留档：亮暗双主题 + 缺失态 + 单价未配置态）；vitest 全绿 + guard:style + 生产构建；全局回归基线 B1 面收口：前端 305 + 后端 224 存量全绿（改写/退役映射逐条登记口径），功能性删除为零（决策驱动 16 例退役例外登记除外） |

**携带项追溯**（planning.md §1「对应 REQ」映射规则：无独立 REQ 承载的子项填合法来源，来源编号已回查出处文档核对、不凭记忆——v1.4.8）：
- **定夺④下线执行**（T2 携带，S 级子项不推高 Σ）：出处 `requirements/changes.md` CHG-009 4.5 定夺表第④行「方案 A 随 B1 直接下线」+ 落地核对清单第 9 项「端点删除 + test_proxy 16 例退役映射 + proxy_smoke 迁 turn（随 B1 任务提交勾验）」+ spec §3 架构行（基线 v6 已落「随 B1 下线、回合端点为唯一对话入口」）；16 例退役属**决策驱动的功能性移除例外登记**（非口径迁移删除，CHG-009 内容 4 既有口径）。
- **人设中性默认稿**（T0 携带）：出处 CHG-009 4.5 定夺表第⑧行「授权 T0 采中性默认稿，CEO 保留否决权」。
- **T0 取证两项**：出处 CHG-009 4.5 表「T0 取证项（非定夺，随 B1 T0 执行）」行 + REQ-037 验收 2 明文「冒烟结论为字段映射的实现输入，不得凭文档臆断」。

复杂度合计：**Σ8 = S1 + M2 + L3 + M2（容量 Σ≤10，余量 2，不顶格）**

- 口径对齐（沿 iter-13/14 定案数值口径：S=1 / M=2 / L=3~4，process/planning.md §2 只定义质性档位）；容量上限 Σ≤10 维持（iter-6~14 连续九轮零偏差校准，见 retros/iter-14.md，本期为第十轮验证）。
- CHG-009 影响评估预估核心 Σ7~9（分割 M + 遥测 L + admin M + 下线 S），本计划取 **Σ8**——与审核稿中位一致且留 20% 余量。**不顶格的理由**：iter-13 顶格（Σ10）的代价是当轮缺陷修复 + QA 整改吃掉全部余量（复盘在案）；本期含真实上游字段形状取证与自填端点兼容性两项外部不确定性，宁可留余量。**携带说明**：分割（M 口径）与下线执行（S 口径）并入 T2 单任务承担，定级 L 下沿 3 已含携带幅度（iter-13 T1 单任务承载四 REQ 先例），不重复计入 Σ。
- 膨胀处置：新增范围 ≤2 可由余量吸收；触顶后继续膨胀按 planning.md §3 砍范围不延期。备砍序（任一回退属范围变更，报 CEO 批准后执行并登记）：a) REQ-038 工具用量视图简化——仅保留按工具名聚合行，状态细分与耗时均值延后；b) 下线执行顺延——旧端点改返回 410 Gone + 命中观察（降级为 CHG-009 方案 B 口径，定夺④时点调整重新报批）。

**全局回归基线（B1 面收口）**（沿 CHG-007 定义、CHG-009 延展的验收门槛，RTM 独立行跟踪）：迭代关闭时前端 305 + 后端 224 存量用例全绿（**计划时点实测核实：pytest collect 224 / vitest list 305，与 iter-14 末口径一致**）+ 本轮新增全绿；「组装等价」类改写用例与 test_proxy 16 例退役均逐条登记映射（旧断言 → 新断言/退役去向，随 verify 登记）；功能性删除为零（定夺④ 16 例退役为决策驱动例外登记）；度量数据全部机器采集（铁律 5）；RTM 全局回归基线行 B1 面收口为全期达成。

## 本迭代不做

| 需求/事项 | 原因 |
|------|------|
| A2 体验深化主题包 ①引用上标/角标联动 ②搜索质量与时效调优 ③搜索过程可视化增强 | **CHG-009 定夺⑦定案不整包搭班**（2026-08-18 CEO 批准）：整包搭班预估另 +Σ5~8 必触容量上限，①②③ 为纯体验层与 B1 无依赖，留暂缓池（②建议与「搜索供应商对比评估」迭代合并评估）；④⑤ 已由 REQ-037/038 收编（遥测工具维度 + 工具用量视图即搜索用量面板），不另立 REQ |
| 搜索供应商对比评估（Tavily vs 博查横向专项） | 暂缓池条目（CEO 2026-08-18 指示「后期专门弄个迭代测试」）：以 A2 上线后真实运行数据为主要输入，数据积累后择机排期，纳入走 CHG；主备切换随时可用（S 级），本评估为正名性验证而非依赖解锁 |
| B2 三级上下文压缩 / C 五层记忆 / D1 deep-research / D2 hooks | 七期路线后续各期（暂缓池登记，候选非承诺）；纳入各走 CHG（基线是承诺不是路线图）。本期只为其固化挂载点（分区边界 + 遥测基建） |
| 真实天气工具（和风天气类专用 API） | 暂缓池条目（iter-14 计划定案不纳入）；和风凭据 + 专属 Host 已冒烟验证备存服务端 .env，B1 后排期时再评估 |
| 移动端主界面适配 | CHG-007 定序：让位至 agent UI 形态稳定后（约 B1 后，可与 D2 搭班）——CEO 2026-08-17 批准 |
| v0.5.0 发布 | 独立卡服务器（VPS/域名/HTTPS）就绪；发布决定（G3）由 CEO 批准 |

## 风险清单

| 风险 | 应对 |
|------|------|
| ① 上游缓存字段可得性不确定：DeepSeek usage 缓存字段形状需 T0 实测（字段名/数值语义可能与文档预期不一致）；自填端点字段矩阵各家不一（GLM 类无缓存概念） | 字段映射口径以 T0 取证为**唯一实现输入**，不凭文档臆断（REQ-037 验收 2 口径）；若实测上游不返回缓存字段 → 命中率恒显缺失标注（铁律 5 不造数），分割降本效果改经上游账单侧复核，功能交付不受阻；自填端点缓存列 NULL、聚合显缺失 |
| ② 自填端点多 system 消息兼容性：多数 OpenAI 兼容端点支持，但个别端点可能明确拒绝多条 system 消息 | T0 冒烟前置取证，不阻塞开发；若实测某端点拒绝 → 档案级「合并 system 单段」回退开关按 REQ-014「支持工具」开关同哲学**另登记**（属新增范围，报 CEO 批准后走登记、吃余量，CHG-009 不预设）；冒烟未见问题则不做 |
| ③ legacy 行采集与端点下线的时序耦合：下线先于遥测上线则流量证据缺失，遥测上线后忘删端点则遗留双轨维护面 | 下线执行整体在 T2 内按固定序列完成（legacy 行采集上线 → 预览环境流量取证 → 端点删除 → 16 例退役映射登记 + proxy_smoke 迁 turn），落地核对清单第 9 项随 T2 提交勾验（QA 复核点）；16 例退役按决策驱动例外口径登记，避免与「功能性删除为零」措辞冲突 |

## 实际结果（复盘时回填）

- 完成任务：{{}}
- 复杂度：计划 Σ8 vs 实际完成 Σ{{}}（新增范围注明）
- 砍掉的范围与原因：{{}}
