# 迭代计划 — iter-3（2026-08-15 ~ 2026-08-29）

> 容量依据：iter-2 复盘维持 Σ ≤ 10（S=1/M=2/L=3），拍脑袋值，本迭代末复盘校准。
> 本迭代为"打磨"迭代：清空 P1 剩余项（REQ-009/011）+ 两个 P2 会话管理项（REQ-012/013）。CEO 确认全排（2026-08-15，Σ9）。

## 迭代目标

用户可以让会话自动命名、AI 回复的 Markdown 与代码块正确渲染并可一键复制，还能手动重命名会话、把会话导出成文件。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-3` tag）才能开始开发，设计任务排在开发任务之前。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 design-iter-3 设计稿并基线 | REQ-009 / REQ-011 / REQ-012 / REQ-013 | M | 拆解：4 个 UI 触点（列表项重命名交互态、消息区 Markdown/代码块样式、导出入口、自动命名展示），复用 design-system 现有组件与 iter-1/2 已基线模式，无新令牌 | 设计稿经 CEO 批准并打 `design-iter-3` tag |
| T1 会话自动命名 | REQ-009 | S | 单模块小改：stores/sessions.ts 新建会话标题初始化 + 首条用户消息后截取前 20 字更新；SessionListItem 已展示 title，无 UI 改动 | 发送"帮我写一封请假邮件…"后列表标题为其前 20 字；并发多会话标题与内容对应正确；手动改名后不再被自动命名覆盖（与 T3 交互） |
| T2 Markdown 渲染与代码块复制 | REQ-011 | L | 新技术档：引入 Markdown 渲染库（markdown-it/marked）+ DOMPurify XSS 防护（AI 输出不可信）+ 代码块语言标签与复制按钮；改造 MessageBubble | 标题/列表/表格/代码块渲染结构正确；复制后剪贴板与代码块原文逐字一致；恶意 HTML/脚本被净化不执行；纯文本回复按普通段落渲染不报错 |
| T3 会话重命名 | REQ-012 | S | 单组件交互：SessionListItem 双击/菜单进入编辑，回车保存 Esc 取消；复用 store 既有更新方法，持久化自动生效 | 重命名后刷新仍生效；Esc 取消不改动标题 |
| T4 导出会话 | REQ-013 | M | 一个模块内完整功能：导出 util 遍历消息转 Markdown/文本 + Blob 下载 + 入口按钮 + 空会话/特殊字符边界 + 测试 | 导出文件含该会话全部消息（区分用户/AI），普通文本编辑器可打开 |

**合计 Σ9**（M×2 + L×1 + S×2）。

## 本迭代不做

| 需求 | 原因 |
|------|------|
| 暂缓想法池全部（账号/RAG/暗色主题/移动端/搜索等） | 后期边界，纳入须走需求变更流程 |
| （无剩余基线需求） | 本次做完 REQ-009/011/012/013 后，REQ-001~014 全部达成 |

## 风险清单

| 风险 | 应对 |
|------|------|
| REQ-011 Markdown 库 + XSS 防护选型与适配（新技术、安全敏感） | T2 第一步先做选型评估（markdown-it + DOMPurify 主流组合）并验证 XSS 用例；不可行则降级为"转义纯文本 + 代码块手工处理"并记入 tailoring.md |
| 自动命名与手动重命名冲突（改完标题被首条消息覆盖） | 明确规则：仅"新建后未手动改名的会话"才随首条消息自动更新，手动改名后自动命名不再覆盖 |
| 设计稿 4 个触点覆盖不全导致开发返工 | T0 把重命名交互态（编辑/确认/取消）与 Markdown 渲染样式逐项列清，CEO 一次定夺 |

## 实际结果（复盘时回填，2026-08-15）

- 完成任务：5/5（T0~T4 全部完成）
- 复杂度：计划 Σ9（M=2/L=3/S=1）vs 实际完成 Σ9（偏差 0%，无新增范围、无砍范围）
- 砍掉的范围与原因：无
