# 迭代计划 — iter-14（2026-08-18 ~）

> 状态：**已批准（2026-08-18，CEO「批准，全部按推荐」；范围经澄清确认 Σ8——真实天气工具不纳入本迭代）**；定夺记录见文末。
> 变更依据：requirements/changes.md **CHG-007**（已批准 2026-08-17，基线 req-baseline-v5）——本迭代 = 七期路线第二期 **A2「联网搜索」**，排入 REQ-035（P1，首个生产工具）+ REQ-032 A2 面（引用来源卡）+ REQ-031 出网治理实例化 + REQ-025/014 开关承载与复验；A1（REQ-030~034）已于 iter-13 全交付并关闭 G4（2026-08-18）。
> 迭代时长：标准 2 周（2026-08-18 起算；项目 tailoring.md「迭代长度：2 周」适用，无偏离需登记）。

## 迭代目标

用户问时效性问题时（今天的热点、最新版本号）AI 自动联网搜索并在消息流中展示搜索工具步骤与来源引用卡（回答可核验），admin 可在管理后台整体关闭搜索（关闭后模型直答、用户无感降级），存量体验零回退（前端 273 + 后端 182 存量用例回归全绿）。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-N` tag）才能开始开发，设计任务排在开发任务之前。
> 含 UI 的任务，验收标准必须包含「design-iter-N 走查清单留档」（v1.4.4：走查是任务级门槛，不是迭代末补做）。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 前置取证（QA 遗留 OBS-2 补账）：自填端点真实回合联调取证——iter-13 计划风险①应对承诺「真实上游联调取证（统一 key DeepSeek + 至少一个自填端点）」的未竟半项（统一 key 已在案）；兼为 A2 铺路：真实自填端点在回合路径 + tools 下发（echo / demo_weather）下的行为差异取证（流式 tool_calls 分片行为各家不一——iter-13 风险①点名的差异面），差异结论同步 T2 实现口径 | QA OBS-2（流程遗留项，合法来源；出处已回查核对，见任务表下追溯注记） | S | 类比：iter-10 T1 技术债小任务（S 档，单点取证路径明确）；拆解：单次真实联调 + 留档，无产品代码改动（若发现缺陷按登记纪律另立 DEF） | 真实自填档案（自填 DeepSeek 或 GLM 三要素）经回合端点完成 ≥1 次完整回合（含 ≥1 次工具调用），事件流与终态取证留档 `plans/iter-14-verify.md` T0 段；iter-13 风险①「双端点取证」补齐、OBS-2 闭环登记（RTM 备注 + 审计观察项处置留痕）；上游分片行为差异（若有）登记 verify 并同步 T2 |
| T1 design-iter-14 设计基线：① 引用来源卡形态（4.8-6，「原型即需求」——搜索结果之来源列表呈现、与既有工具步骤卡的关系、回答内引用标记形态、折叠默认态）② 搜索降级与空结果文案定稿（「搜索未成功，以下为模型直接回答」类 + 「未搜到相关内容」如实回答口径）③ admin 搜索开关入口（AdminView 形态 + API 口径章节：开关状态存取、运行时生效）④ 自填档案「支持工具」开关 UI（高级设置档案表单——design-iter-13 定夺④联动定案「开关 UI 随 A2/design-iter-14 落地」兑现）⑤ 4.8-10 选型与 key 归属定夺值随稿登记；**卫生项：spec REQ-035/032「涉及页面」指针随基线回填 + REQ-033 措辞注记同步（QA OBS-3）** | REQ-035（主）+ REQ-032（引用卡，验收 4）+ REQ-014（档案开关 UI，定夺①）+ REQ-025（admin 开关入口）+ QA OBS-3（卫生项，合法来源） | M | 类比：design-iter-8/11/12/13 同规模单域原型一次基线均定 M（iter-13 为 9 项定夺 + 协议契约章节定 M）；本次 4 触点章节 + 定夺登记，规模同档偏轻 | CEO 批准基线（tag design-iter-14）；4.8-6 形态定死、4.8-10 定夺值与微参数（search 单工具超时/结果条数）随稿登记；降级文案定稿（样件文案逐字断言面——v1.4.10 B）；走查清单随稿（引用卡亮/暗双主题、几何断言面——v1.4.11 B、加载态一帧——iter-12 OBS-5 配套）；spec「涉及页面」指针零滞后回填（v1.4.10 A 防线）+ REQ-033 措辞注记同步（OBS-3 闭环登记，无需 CHG） |
| T2 后端搜索工具接入：① search 工具静态注册（入参 JSON Schema `{query}`；出网=true、固定域白名单 = 选定搜索 API 域；结果上限 32 KiB 沿网关默认）② 第三方搜索 API 客户端（按 4.8-10 定夺选型实现；key 经 backend/.env 注入、`.env.example` 占位，admin 可见开关状态、不可见 key）③ 结果组装：网关截断 + 整理「结果摘要 + 来源列表（标题/URL/片段）」（REQ-035 主流程 3）④ admin 整体开关（状态落库运行时生效、默认开；关闭 → 工具不注册、上游 tools 定义不含 search——模型不知其存在）⑤ REQ-035 验收 2/3/4/5 pytest（假搜索端点全 mock，不依赖真实 key）⑥ 网关安全日志四字段沿用；注入防护随真实搜索结果复核（CHG-007 内容 4.5 第 6 项 A2 动作） | REQ-035（主）+ REQ-031（出网治理实例化，验收 2 A2 面收口）+ REQ-025（admin 开关后端） | L（取下沿 3） | planning.md §2 L 定义两条均中：跨模块协作（tools 注册表 / 网关 / admin 路由 / 配置注入）+ 引入本项目未用过的第三方搜索 API；取 3 不取 4 的理由：网关六项校验链与回合链路 A1 已落地，本任务为既有框架上实例化首个出网工具，对比 iter-13 T1 从零建四子系统（取上沿 4）轻一档 | REQ-035 验收 2（假搜索端点失败/超时 → 回合降级直答不崩：pytest 帧级断言）/ 验收 3（admin 关闭 → 注册表无 search、MockTransport 捕获上游请求 tools 定义不含 search——payload 断言）/ 验收 4（出网仅搜索 API 域：目标内网/环回/他域 → 假传输层零连接；REQ-031 验收 2 A2 面实例化收口）/ 验收 5（假端点超大结果 → 截断标注、截断后 ≤ 32 KiB）；**开关断言矩阵**（admin 开关 × 档案「支持工具」开关 × key 模式三态组合：admin 关·档案开 → tools 无 search；admin 开·档案关 → 无 tools；admin 开·档案开/统一 key → 含 search，统一 key 恒开不参与变化）；key 卫生（.env 不入 git、日志/响应体零 key、git 全量历史检索不到）；pytest 全绿 + ruff clean；真实搜索 API 冒烟 1 例（key 到位后结果结构取证留档 verify，不作阻塞门） |
| T3 前端引用卡与开关 UI：① 引用来源卡渲染（按 design-iter-14；tool.call/tool.result 事件消费 A1 已在位，本任务为搜索结果引用呈现与回答内引用标记）② 搜索失败/超时/空结果的降级与如实呈现（文案按基线逐字）③ 自填档案「支持工具」开关 UI（默认开；上游明确报不支持 tools 时经 REQ-007 既有映射引导关闭，不新建映射）④ admin 搜索开关入口（AdminView 状态展示与操作）⑤ 存量零回退复验（blocks 混流 / branches / 导出简明标记行 / 搜索索引文本段拼接，沿 CHG-007 改写口径） | REQ-032（引用卡，验收 4）+ REQ-035（验收 1 前端侧 + 用户所见）+ REQ-014（开关 UI，定夺①）+ REQ-025（开关入口与复验） | M | 类比：iter-11 T1/T3 单域 UI 任务均定 M；拆解：三个触点均为局部增量 + 一个新卡组件，blocks 数据模型与事件流消费 A1 已交付、无数据层改造 | REQ-035 验收 1（时效性问题预置样例：回答含可见工具步骤与引用来源卡——真实 Chrome + 真实搜索 API 走查留档）；REQ-032 验收 4（引用来源卡 design-iter-14 走查清单留档：亮/暗双主题、几何断言、样件文案逐字、加载态一帧）；REQ-025 复验（admin 开关操作生效；关闭后普通用户直答降级、无错误提示——「模型不知其存在」口径走查）；REQ-014 开关 UI 走查（默认开/关闭态/引导路径）；vitest 全绿 + guard:style + 生产构建；新增用例映射登记（全局回归基线） |

**前置项与卫生项追溯**（planning.md §1「对应 REQ」映射规则：无 REQ 承载的任务填合法来源，来源编号已回查出处文档核对、不凭记忆——v1.4.8）：
- **QA OBS-2**（T0 承载）：出处 `retros/qa-audit-iter-13.md` 观察项表原文「风险①应对承诺『自填端点真实联调取证』无记录（仅统一 key DeepSeek 在案）→ 建议 A2 T0 前补自填端点真实回合或 tailoring 登记裁剪理由」；iter-13 复盘遗留清单已定「随 A2 T0 前补」（CEO 认可复盘），故取补做路径、不裁剪。
- **QA OBS-3**（并入 T1，S 级顺带不推高 Σ）：出处同上——「spec REQ-033 措辞与实现（可选 system_prompt 第三字段）差异……下次 spec 触点同步一句，无需补 CHG」；T1 基线回填指针即 spec 触点，顺带收口。

复杂度合计：**Σ8 = S1 + M2 + L3 + M2（容量 Σ≤10，余量 2，不顶格）**

- 口径对齐（沿 iter-13 计划定案的数值口径：S=1 / M=2 / L=3~4，process/planning.md §2 只定义质性档位）；容量上限 Σ≤10 维持（iter-13 复盘 CEO 拍板，连续八轮零偏差校准）。
- CHG-007 影响评估预估 A2 Σ6~8，本计划取上沿 8——相对容量仍留 20% 余量。**不顶格的理由**：iter-13 顶格（Σ10）的代价是当轮 4 个缺陷修复 + QA 整改吃掉全部余量（复盘已记录）；本轮含真实第三方 API 联调与首个出网工具，外部不确定性高于均值，宁可留余量。
- 膨胀处置：新增范围 ≤2 可由余量吸收；触顶后继续膨胀按 planning.md §3 砍范围不延期。备砍序（任一回退属范围变更，报 CEO 批准后执行并登记）：a) 引用卡联动简化——回答内引用标记降级为纯来源列表卡；b) 档案开关引导路径精简（开关本体保留，上游报错的映射引导文案延后）。

**全局回归基线（A2 面收口）**（CHG-007 定义的 iter-13/14 统一验收门槛，RTM 独立行跟踪）：迭代关闭时前端 273 + 后端 182 存量用例全绿（iter-13 收口基线数）+ 本轮新增全绿；A2 新增/改写用例逐条登记映射（旧断言 → 新断言，随 verify 登记）；功能性删除为零（措辞沿全局回归基线定义——QA iter-13 OBS-4 口径）；度量数据全部机器采集（铁律 5）；RTM 全局回归基线行收口为全期达成。

## T0 定夺参数清单（CHG-007 4.8 表 A2 相关项）

| #（4.8 原编号） | 参数 | 候选 | 推荐 | 备注 |
|---|---|---|---|---|
| 10 | 搜索 API 选型与 key 归属 | Tavily / 博查 / SearXNG | SaaS + .env | **已定夺（2026-08-18，见文末定夺记录第 1/2 项）：Tavily 起步 + backend/.env**；定夺值随 design-iter-14 稿登记 |
| 6 | 引用来源卡形态 | 随原型 | — | 即 design-iter-14 本体（「原型即需求」），T1 基线定死 |
| 随稿微参数（非 4.8 表项） | search 单工具超时 / 结果条数 / 引用卡折叠默认 | 随原型 | — | 随 design-iter-14 基线登记（候选区间：超时 8~15s——交互式搜索等待，高于演示工具 2s 先例；结果条数量级 5） |

（4.8 表其余项不属本轮：#11 旧透传端点下线时点 = B1 排期时；A1 相关 9 项已于 design-iter-13 全部定死。）

## 本迭代不做

| 需求/事项 | 原因 |
|------|------|
| 真实天气工具（和风天气类专用 API） | **已定案不纳入（CEO 2026-08-18 范围确认 Σ8）**：① 容量——核心范围 Σ8 已近上限，纳入（M2）即 Σ10 顶格，iter-13 复盘顶格代价在案；② 价值重叠——通用搜索（REQ-035）已覆盖时效性天气问答，专用 API 增益（结构化实时数据）非 A2 主路径必需；③ demo_weather 保留 admin 自检。留暂缓池，B1 排期时再评估。**补记（2026-08-18）**：CEO 已提供和风凭据（凭据ID CMGTQUUCJA，API KEY 鉴权）+ 专属 API Host，冒烟验证通过（专属 Host 下 /v7/weather/now code 200 真实数据；通用 devapi.qweather.com 返回 403 Invalid Host——该账户为新版专属 Host 制度）；已存 backend 侧 .env 备用（不入 git），B1 启用时直接可用，零等待 |
| REQ-030/033/034 新开发 | A1 已全交付并验证（iter-13 关闭 G4）；本迭代仅涉关联复验面（REQ-031 出网实例化 / REQ-032 引用卡 / REQ-025 开关复验），无新开发 |
| B1 prompt 分割+遥测 / B2 三级压缩 / C 五层记忆 / D1 deep-research / D2 hooks | 七期路线后续各期（CHG-007 暂缓池登记，候选非承诺）；纳入各走 CHG（基线是承诺不是路线图） |
| 移动端主界面适配 | CHG-007 定序：让位至 agent UI 形态稳定后（约 B1 后，可与 D2 搭班）——CEO 2026-08-17 批准 |
| 旧透传端点下线（4.8-11） | B1 排期时评估；本迭代窗口期口径不变（保留零改动、1 请求 = 1 回合计、配额同源） |
| GET 兼容序列化（v2 → string 降级输出） | CHG-007 定夺②附带的 A2/B1 可选项、非必选（iter-13 已登记不做，维持） |
| v0.5.0 发布 | 独立卡服务器（VPS/域名/HTTPS）就绪；发布决定（G3）由 CEO 批准 |

## 风险清单

| 风险 | 应对 |
|------|------|
| ① 第三方搜索 API 依赖：key 申请/审核时滞、免费额度与计费（429/额度尽）、结果质量（尤其中文时效性场景）不确定 | 选型定夺（本计划批准）后立即注册取 key；T2 pytest 全部以假搜索端点承载（MockTransport 既有夹具模式），开发不阻塞在 key/额度；真实 API 仅冒烟 1 例 + 验收 1 走查取证；质量或行为不达预期触发备选切换（Tavily ↔ 博查——工具封装在网关后，切换成本 S 级，白名单域与客户端适配层隔离）；计费/限流错误一律走网关 error 降级路径（回合不崩，REQ-035 异常分支既有口径） |
| ② 引用卡与存量 blocks 兼容：引用呈现触达渲染/导出简明标记行/搜索索引文本段拼接/branches 混流，老会话 v1 归一化零回退面广 | 承载形态（tool_result 结构化扩展 vs 新 block 类型）T1 随 design-iter-14 定死、影响面随稿评估；老会话零回退用例先行复跑；「既有用例零删除 + 逐用例映射登记」按全局回归基线措辞执行（QA iter-13 OBS-4 口径，避免计划措辞张力）；导出/搜索口径沿 CHG-007 改写复验 |
| ③ admin 开关 × 档案「支持工具」开关 × key 模式三态叠加，tools 定义下发的 payload 断言面易漏（统一 key 恒开、自填档案可关、admin 总开关再乘一层） | T2 验收内置三态断言矩阵（REQ-035 验收 3 为主锚）；T3 走查复验用户侧形态（admin 关闭后直答、无错误提示——「模型不知其存在」口径）；关闭态对回合端点的请求载荷断言纳入 pytest（MockTransport 捕获） |

## 实际结果（复盘时回填）

- 完成任务：{{}}
- 复杂度：计划 Σ8 vs 实际完成 Σ{{}}（新增范围注明）
- 砍掉的范围与原因：{{}}

## 定夺记录（2026-08-18 CEO 批准「批准，全部按推荐」+ 范围澄清确认 Σ8）

| # | 定夺项 | 结果 |
|---|--------|------|
| 1 | 搜索 API 选型（4.8-10 前半） | **Tavily 起步**——面向 LLM 的搜索 API，返回结构化摘要 + 引用来源（CHG-007 4.7 对比表），与 REQ-035「结果摘要 + 来源列表」组装口径和引用来源卡直接对口。备选博查：中文源覆盖与国产合规更优，若 Tavily 中文质量实测不足、或部署环境出境调用受限即切换（网关封装后 S 级）。SearXNG 不做（CHG-007 已定倾向：运维与质量不稳，与单人公司容量冲突） |
| 2 | 搜索 key 归属（4.8-10 后半） | **backend/.env 注入**（`AI_CHAT_SEARCH_KEY`，与统一 key 三变量同法，`.env.example` 占位）；admin 可见开关状态、不可见 key——与 REQ-014 受保护存储同口径（不进 git/日志/响应体）；admin 开关状态落库运行时生效（API 口径随 design-iter-14 定案），key 与开关分离 |
| 3 | 真实天气工具纳入与否 | **不纳入本迭代**（留暂缓池，B1 排期时再评估）——2026-08-18 CEO 范围澄清确认（「天气下个迭代」口径与 PM 原推荐一致）；spec §4 暂缓池条目授权计划流程定案，无需新 CHG。和风凭据 + 专属 Host 已验证并存 .env 备用（见「本迭代不做」表补记），B1 启用零等待 |
| 4 | 迭代计划整体批准 | **批准 Σ8 方案**（T0 前置取证 + T1 设计基线 + T2 后端 + T3 前端）；定夺 1/2 值随 design-iter-14 稿登记 |

**变更流程提示**：本计划排入任务全部有已基线需求或合法来源承载（REQ-035/032/031/025/014 + QA OBS-2/OBS-3 + 全局回归基线，req-baseline-v5），无未登记变更诉求、无需走变更流程；真实天气工具纳入与否已随本计划批准定案为不纳入（spec §4 授权计划流程定夺）。
