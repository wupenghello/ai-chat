# 迭代计划 — iter-7（2026-08-16 ~ 2026-08-30）

> **已批准——2026-08-16 CEO 批准，Σ10 方案（T0~T3；全链路 Compose 确认挪 iter-8，与 v0.5.0 发布形态同批收口）。**
>
> 容量依据：iter-6 复盘（retros/iter-6.md Q4）上限维持 **Σ ≤ 10**（S=1/M=2/L=3）；后端任务继续按 L 起评但**置信度上调**（iter-6 实测：T1=L 吻合、T2/T3 偏保守，校准基线已建立，新栈 ramp-up 未阻塞）。
>
> v1.4.4 新规生效（iter-6 复盘 A/B 条）：含 UI 的任务，验收标准必须包含「design-iter-7 走查清单留档」，实现先对照设计基线自查再提交——本计划 T0/T2 已按此落。
>
> **范围调整说明（相对 iter-6 已批准的跨迭代切分）**：原切分将「全链路 Compose 一键起」排在 iter-7，本计划建议**挪至 iter-8**，理由：① 主线四件自然拆解 Σ12 超容量上限 20%，守 Σ10 需砍一条；② Compose 全链路（前端构建产物 + backend + DB）的验收价值绑定 v0.5.0 发布形态与自部署文档，与 iter-8 收口同批一次到位，可避免高级设置重设计落地前后两轮容器联调；③ dev 路由按环境裁剪（QA 观察项 6）与 auth_sessions 过期清理（Code Review 遗留）随部署收口同批处理更顺。iter-7 开发联调用既有环境（Vite dev server + 后端容器，iter-6 已就绪）。**CEO 已确认（2026-08-16）：采用 Σ10 方案，全链路 Compose 挪 iter-8**（备选方案 A——Σ12 一次性突破上限、T4 加回任务表——未采纳，此处留档备查）。

## 迭代目标

用户不做任何密钥配置即可经自建后端流式对话（统一 key 模式），自填的供应商密钥与档案只存服务端且多设备一致，断网期间的会话更改在恢复网络后自动同步。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-7` tag）才能开始开发，设计任务排在开发任务之前。
> 含 UI 的任务，验收标准必须包含「design-iter-7 走查清单留档」（v1.4.4：走查是任务级门槛，不是迭代末补做；无 UI 变更的任务须注明不适用）。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 design-iter-7 设计稿并基线 | REQ-014（高级设置双模式重设计）+ REQ-018（档案迁服务端后的设置页）+ REQ-007（错误映射扩展口径） | M | 类比 iter-4/6 T0（多触点 M）：1 个设置页重设计（统一 key 零配置态 + 自填档案列表/添加/切换/清除），复用现有令牌与 components.md 表单/列表模式；错误文案映射为表格口径非新触点 | 设计稿经 CEO 批准并打 `design-iter-7` tag，**含 §7.x 走查清单（v1.4.4 硬要求）**；T0 设计阶段定夺的 4 项技术项（随设计基线批准由 CEO 拍板）：① 统一 key 及默认上游的存放与环境变量口径（.env 不入 git）② 自填 key 服务端受保护存储形式 ③ 上游错误经代理透传后的 REQ-007 文案映射表 ④ 存量本地档案上云方式（登录静默自动 vs 手动导入） |
| T1 REQ-023 流式代理端到端（后端转发 + 前端切端点 + REQ-001/002 复验） | REQ-023 + REQ-001/002（NCR-iter6-005 过渡态注记销账）+ REQ-007（错误透传） | L | 跨模块跨栈：httpx AsyncClient SSE 实时转发 + 鉴权/档案解析 + 错误分支 + 性能验收 + 前端 client 切端点；SSE 形态已经 echo 端点验证、认证与会话基线就绪，类比 iter-6 T1（新域首任务 L 实测吻合）。置信度中（有校准基线，真实上游组合未验证） | 统一 key 模式：新注册用户零配置完成一次真实流式对话（DeepSeek）；自填 key 模式：DeepSeek 与 GLM 各完成一次经代理流式对话；代理首块额外延迟 ≤500ms（同环境直连对比取证，非功能「性能」条款）；上游 401/403/429/5xx/超时错误类型透传或映射，前端按 REQ-007 提示并可重试；token 失效返回 401 沿用既有跳转钩子；quota 检查位预留不实现（REQ-024 排 iter-8）；浏览器网络面板/响应体/服务端日志均检索不到任何 API key；pytest + 前端测试全绿；REQ-001/002 验收在代理架构下复验通过（流式渲染、空输入、断网、中断标注、20 轮截断与 system 首位请求体可观测）；**无 UI 变更，走查不适用（v1.4.4 注明）** |
| T2 密钥与供应商档案迁服务端（后端受保护存储 + 前端 settings 换源 + 高级设置重设计 UI） | REQ-014 + REQ-018 | L | 拆解：后端 profiles CRUD + 受保护存储（类比 iter-6 sessions CRUD，实测 <L）+ 前端 settings 换源（同接口适配层模式，iter-6 已证明有效）+ SettingsForm 高级设置重设计（类比 iter-5 T3 档案 UI = M）；合计 L。置信度中偏高（各半侧均有 iter-5/6 类比） | 自填三要素存服务端受保护存储，浏览器 localStorage/IndexedDB 检索不到任何 key 与档案；另一设备登录后档案列表与当前生效档案一致；一键切换即时生效，生成中切换当前回复用旧档案跑完、下一次请求用新档案（CHG-002 语义保持）；清除自填配置后自动回退统一 key 模式；自填 key 无效（上游 401 经代理透传）提示并引导回高级设置；存量本地档案按 T0 定案方式上云且本地清除；git 全量历史（前后端）检索不到任何真实 key；**design-iter-7 走查清单留档（v1.4.4）**；前后端测试全绿 |
| T3 会话断网暂存与自动重试 | REQ-022（异常分支细项，iter-6 Code Review 登记口径） | M | 单模块完整功能含异常分支：persistence 层暂存队列 + 恢复网络自动重试 + 连续失败提示；现状已有失败 toast 兜底（iter-6 交付），增量明确。无新 UI（提示复用 AppToast——走查不适用，注明） | 断网状态完成一轮对话，恢复网络后自动同步到服务端、另一设备可见（REQ-022 验收原文）；连续失败提示「部分更改未同步」；重试不阻塞对话主流程；暂存队列按序重放，LWW 语义不被乱序破坏（PUT 整档按提交顺序覆盖）；前端测试覆盖上述分支 |

**合计 Σ10**（M×2 + L×2）。容量守恒（Σ ≤ 10）；范围调整与 CEO 决策见文件头说明。

## 本迭代不做

| 需求 | 原因 |
|------|------|
| REQ-024 用量配额与滥用防护（P0） | 排 iter-8 主线（与 REQ-025 管理后台的配额调整联动）；本迭代 T1 仅预留 quota 检查位；配额初始默认值（CHG-004 后续待定）随 iter-8 计划定 |
| REQ-025 管理员后台（P1） | 依赖用户与用量数据积累，排 iter-8 |
| REQ-021 账号管理（P1） | 排 iter-8 |
| REQ-022 其余细项：LWW 多设备并发冲突用例、存量会话迁移入口（含只读 30 天清除） | LWW 机制已随 iter-6 PUT 整档落地，并发验收用例与迁移入口随 iter-8 多设备/迁移批次统一取证 |
| 全链路 Compose 一键起 + 自部署文档 + dev 路由按环境裁剪（QA 观察项 6） | 挪 iter-8 与 v0.5.0 发布形态同批收口（CEO 已确认 2026-08-16，理由与留档见文件头）；iter-7 联调用 Vite dev server + 后端容器既有环境 |
| auth_sessions 过期行清理、useTheme 单测（iter-5 观察项 4） | 容量不足不排，iter-8 候补；若执行中主线提前完成且余量充足，按计划外新增登记（复盘补账） |
| 云部署（VPS/域名/HTTPS） | CHG-004「后续待定」，本地跑通后再议 |

## 风险清单

| 风险 | 应对 |
|------|------|
| SSE 真实上游代理转发：echo 端点只验证了流式形态，真实上游 + 认证 + 档案解析 + 错误分支的组合未验证；首块延迟 ≤500ms 的性能验收存在不确定性 | T1 第一步先做统一 key 直通冒烟（DeepSeek 真实流式打通全链），再补自填模式与错误分支用例；性能验收同环境直连对比取证；阻塞 >2 天按流程升级 CEO |
| 密钥安全边界扩大：自填 key 集中存服务端，泄露面从浏览器移到后端 DB 与日志 | 受保护存储形式列为 T0 待澄清项 CEO 定案后实施；密钥不进日志（日志脱钩）、不出现在任何 API 响应；git 全量历史与浏览器端检索双重验证；Code Review 重点审密钥路径 |
| 高级设置重设计动既有设置页（与 REQ-008 系统提示词、REQ-017 主题入口共存），设计偏差前科（NCR-iter6-002 登录页 8 处） | 执行 v1.4.4 新规全链：T0 设计稿带 §7.x 走查清单 → 实现对照清单逐条自查再提交（development.md §1.4）→ T2 任务验收含走查清单留档，偏差要么修正要么先登记 |

## 实际结果（复盘时回填）

- 完成任务：T0~T3 全部完成（2026-08-16，提交 50ef5d1/3fad772/ad8fe6a/dcfafcd + 审计整改 56f4eeb）
- 复杂度：计划 Σ10 vs 实际完成 Σ10（偏差 0%；T0/T3 偏松、T1/T2 吻合，详见 retros/iter-7.md Q4）
- 砍掉的范围与原因：无砍范围。口径修正 2 处（均 spec/设计基线定案优先，tailoring 登记）：存量本地档案上云提示条 iter-8 落地；GLM 自填模式经代理不补验（CEO 决策 2026-08-16，DEF-002 延续）
- 过程缺陷：DEF-015（严重，iter-6 遗留偶发 500）/ DEF-016（一般，走查偏差）/ DEF-017（严重，Code Review 发现竞态）全部修复；QA 审计 3 NCR 全整改；复盘产出制度 v1.4.5（文档随任务收尾 + 登记位置唯一）
