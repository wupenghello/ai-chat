# 缺陷登记 — ai-chat

> 编号前缀为 DEF（制度模板示例为 BUG，偏离已登记 tailoring.md，2026-08-15）。
> DEF-003~009 为 QA 审计 NCR-001 整改补录（2026-08-15）：原散记于 plans/iter-1-verify.md，修复先于登记，特此补录并标注原修复提交。

| 编号 | 日期 | 描述 | 严重度 | 状态 | 处置 |
|------|------|------|--------|------|------|
| DEF-001 | 2026-08-15 | `metrics/scripts/collect.sh` 以退出码 129 中止，终止点在 csv 写入之前；手动逐行复刻相同命令可正常写入。影响：度量采集需人工绕行，违反铁律 5 的自动化初衷 | 中 | 已修复 | 根因（iter-2 T4 排查）：129 是 git **用法错误退出码**而非 SIGHUP——`git for-each-ref` 无 `--since` 选项，`set -e` 使脚本在 tags 赋值行中止；"环境异常"为误诊。修复：改用 `--format='%(creatordate:short)'` + awk 日期过滤；验证 exit=0、csv 正常写入、tag 计数 4 正确（company-os 提交） |
| DEF-002 | 2026-08-15 | GLM 供应商真实流式未验证（key 余额不足 429/1113） | 低 | 已关闭（不验证） | CEO 决策（2026-08-15）：不充值 GLM、不补验，接受现状。依据：协议层按 OpenAI 兼容实现且 DeepSeek 实测通过，REQ-014 的多供应商可配置性由配置项与协议实现承载；GLM 实测留待日后有需要时再做。REQ-014 验收中"分别配置 DeepSeek 与 GLM 各完成一次流式对话"一项按 CEO 决策接受为部分达成。**iter-7 T1 复验同口径（CEO 决策 2026-08-16）：经代理的 GLM 自填模式不补验，DeepSeek 实测通过即接受** |
| DEF-003 | 2026-08-15 | （补录）IndexedDB 无法结构化克隆 Pinia 响应式代理，persist 报 DataCloneError，会话曾整段丢失。冒烟发现 | 严重 | 已修复 | 修复：persist 前深拷贝（提交 be69e97）；回归：刷新恢复 2 会话 6 消息实测通过 |
| DEF-004 | 2026-08-15 | （补录）设置页无返回对话入口，进入后卡死在设置视图。冒烟发现 | 一般 | 已修复 | 修复：侧栏新建/选择会话切回 chat 视图（提交 be69e97） |
| DEF-005 | 2026-08-15 | （补录）429 错误一律显示"请求频繁"误导用户（GLM 1113 实为余额不足）。冒烟发现 | 一般 | 已修复 | 修复：透传供应商具体原因（提交 be69e97） |
| DEF-006 | 2026-08-15 | （补录）发送消息后 AI 回复必须刷新页面才显示：`send()` 中 `aiMsg` 未经过 Pinia 响应式代理，流式追加不触发视图更新。CEO 试用发现 | 严重 | 已修复 | 修复：push 后从 `session.messages` 取回代理对象再变更（提交 061746f）；回归用例 + 浏览器实测（不刷新 3 秒 DOM 已 107 字） |
| DEF-007 | 2026-08-15 | （补录）chat 区布局宽度混乱：用户消息未右对齐（`.row-wrap` 缺 `justify-content`）。CEO 试用反馈"布局乱"走查发现 | 一般 | 已修复 | 修复：消息区布局重构为 DeepSeek 模式（提交 53c0343）+ 用户气泡单行显示（提交 3eee183）；DOM 实测走查表全过 |
| DEF-008 | 2026-08-15 | （补录）中文输入法选词按 Enter 会误发送（未判断 `e.isComposing`）。自查发现 | 一般 | 已修复 | 修复：选词回车不再发送（提交 061746f） |
| DEF-009 | 2026-08-15 | （补录）同一会话生成期间重复发送无防护；删除有后台生成的会话未终止其请求。自查发现 | 一般 | 已修复 | 修复：`send()` 生成中忽略；删除会话同步终止其 AbortController（提交 061746f） |
| DEF-010 | 2026-08-15 | （iter-2 遗留，iter-3 生产构建暴露）`PersistedMessage.status` 缺 `'stopped'`，致 `Session extends PersistedSession` 类型冲突（TS2430）。iter-2 未跑 `vue-tsc` 生产构建故未暴露，属构建期类型缺口而非运行时缺陷 | 低 | 已修复 | 修复：`src/db/idb.ts` 补 `'stopped'`（iter-3 提交 740c6a1）。根因：迭代准出未含生产构建（`npm test`=vitest 不做类型检查）；建议后续把生产构建（vue-tsc）纳入迭代准出固定动作（QA 审计观察项 2） |
| DEF-011 | 2026-08-15 | ComposerBox 输入区实现偏离已基线设计稿 iter-1（4.6 节）：布局由「textarea 在上 + 底部操作栏（hint + 发送按钮 36px）」改为「textarea 与按钮并排单行 + align-items:flex-end」；textarea 字号 15px（基线 14px/1.6）；容器 padding 10px 10px 10px 16px（基线 12px 12px 8px）；缺 shadow-1 阴影；缺底部「Enter 发送 · Shift+Enter 换行」hint。现象：单行输入文字偏上、与发送按钮不对齐，CEO 反馈「输入框输入内容位置歪歪扭扭」 | 一般 | 已修复 | 处置（回基线设计稿 iter-1 4.6 节）：布局改回两行结构（textarea 上 + 底部 bar：hint + 发送按钮 36px）；textarea 字号回 14px/1.6；容器 padding 回 12px 12px 8px（内边距由容器提供，iter-1 基线 textarea 无自身 padding，无需补）；补 shadow-1 阴影。关联 CHG-002。修复提交 c934641（iter-4 T1）：62/62 测试、vue-tsc 生产构建、DOM 实测走查（712px 容器 / 686px textarea / 36px 按钮 / hint 12px 左对齐）全过。注：原根因「textarea 无自身内边距」系误诊——iter-1 基线 textarea 本就无自身 padding，已按 design-iter-4 待澄清第 6 项定夺修正措辞。**CHG-003 覆盖（2026-08-15）**：CEO 试用反馈后最终态改为「发送/停止按钮与 textarea 同排、顶部对齐首行（padding-top 7px）+ hint 移底部」，原「两行结构回基线」处置作废，本行 DOM 走查数据（712px/686px/36px/hint 左对齐）为中间态已失效，以 CHG-003 最终实现为准。关联 CHG-003 |
| DEF-012 | 2026-08-15 | iter-5 设置页「外观/供应商档案/模态」三块样式完全失效：①追加样式被写到 SettingsForm `</style>` 之外成死文本；②T1 清扫脚本误将 App.vue :root/[data-theme] 内 14 处令牌定义替换为自引用（--c-mask: var(--c-mask) 等），遮罩/焦点环/悬停底等失效。CEO 试用发现 | 严重 | 已修复 | 修复提交 064e393：样式闭合标签移至文件末尾（CSS 产物 24.1→26.5kB 死样式复活）；14 处令牌恢复字面值并正则校验无自引用。根因：脚本式批量编辑绕过结构校验 + 验证只查 DOM 类名未查 computed style。教训转 iter-5 复盘 |
| DEF-013 | 2026-08-15 | 暗色模式 toast 不可读：AppToast 底色用 var(--c-text-1)（浅色下恰为深值的取巧写法），暗色下 --c-text-1=#E6EAF0 配白字 ≈1.1:1；动作链接 #A3BCFF 为裸色值无令牌。QA 审计 NCR-iter5-001 发现 | 严重 | 已修复 | 修复：底色改 var(--c-toast-bg)（tokens v1.3 既有）+ 新增 --c-toast-border（亮 #23272E 同底不可见 / 暗 #33363E 描边分层，tokens v1.3「暗色加描边」定稿）+ 新增 --c-primary-on-dark #A3BCFF（深底动作链接）；两令牌入 tokens.md v1.3.1 |
| DEF-014 | 2026-08-16 | 登录页网络/5xx 错误未提供「重试」入口：实现只对 fetch 网络异常（status 0）挂重试，经 Vite 代理的后端不可达实际返回 500，走通用错误分支无重试按钮（design-iter-6 §1.2/走查清单 5）。iter-6 完整走查中发现 | 一般 | 已修复 | 修复提交 3529ece：status 0 与 ≥500 均显示 danger banner + 内嵌「重试」，恢复后实测重试登录成功；新增用例。**复盘补登记（2026-08-16）：当场修复时未走登记，违反 testing.md §3「发现即登记」——制度 v1.4.4 改进 C（当场修复不豁免登记）即源于此** |
| DEF-015 | 2026-08-16 | 真实 uvicorn（容器）下偶发 `PUT /api/sessions` 500：`sqlite3.ProgrammingError: SQLite objects created in a thread can only be used in that same thread`——FastAPI 将 sync 依赖（get_db 建连接）与 sync 路由提交线程池时可落不同线程，sqlite3 默认线程绑定触发。iter-7 T1 浏览器验收时经网络面板发现（TestClient 单线程模型未暴露，属 iter-6 遗留）。影响：会话云同步保存随机失败 | 严重 | 已修复 | 修复（提交 **3fad772**）：`connect()` 传 `check_same_thread=False`（连接每请求独立 + WAL，无跨请求共享）；pytest 52/52、容器重启后浏览器复测 PUT 全 200。iter-7 T1 交付 |
| DEF-016 | 2026-08-16 | 上游 401 错误气泡仍渲染「重试」按钮——design-iter-7 §3.1 映射表明确 401/403「不提供重试（不修复必然再失败）」，实现遗漏该分支。iter-7 T2 走查 15（QA 审计 NCR-iter7-003 指出当场修复时未落 DEF 登记，v1.4.4 C 条口径：verify 表留痕≠defects.md 登记，本条补登） | 一般 | 已修复 | 修复（含于提交 **ad8fe6a**，审计主会话补证核验）：ErrorBubble 重试按钮改 `v-if="kind !== 'auth'"`；浏览器实测 401 气泡仅「前往高级设置」。**补登记**：QA 审计 NCR-iter7-003 整改（2026-08-16） |
| DEF-017 | 2026-08-16 | 暂存队列重放出队用 `slice(1)` 存在竞态：重放某操作的 `await` 期间若同 id 新操作入队（enqueue 按同 id 压缩重排队列），`slice(1)` 会错删队头之后的其他待同步操作——触发窗口窄但真实（断网恢复瞬间再操作同一会话）。iter-7 G4 Code Review（NCR-iter7-002 整改执行时）发现 | 严重（数据丢失类） | 已修复 | 修复（整改提交，2026-08-16）：PendingOp 加单调 `seq` 身份标识，重放出队改 `filter(o => o.seq !== op.seq)` 按身份精确移除；回归用例「重放期间同 id 新入队不错删他项」加入 pending-sync.spec（141/141 全绿）。教训：出队/入队共享可变队列时，索引式移除（slice(1)）在 await 间隙不安全 |
| DEF-018 | 2026-08-16 | design-iter-8 §4.1 暗色锁定帧（及暗色态交互演示）管理后台表格主体为白底：设计稿文档外壳全局 `table{background:#FFFFFF}` 渗入演示组件，`.adm-table` 未自声明背景。CEO 走查发现（本迭代第 1 个外部走查缺陷）。实现侧 AdminView 自带 `background:var(--c-surface)` 不受影响，仅设计稿演示层缺陷 | 一般 | 已修复 | 修复提交 **4538a79**（基线后修订 R1，设计稿头部已登记变更）：`.adm-table` 补 `background:transparent`，表面底由 `.tbl-wrap` 承载；无头 Chrome 计算样式复核两帧表格背景 rgba(0,0,0,0)，视觉复检无白色异常块。**补登记说明**：修复时仅登记了设计稿头部修订，defects.md 登记滞后于修复提交——按 v1.4.5 B 条「登记位置唯一」口径本应修复提交时同步落此表，留作审计检视 |
| DEF-019 | 2026-08-16 | MigrationBanners 在 setup 时一次性捕获 store 状态对象引用（`banner: mig.sessions`），而 store 的 dismiss/knowDone 是整对象替换（freshBanner）——替换后组件持有旧对象，**提示条不从界面消失**（store 状态已变但 DOM 滞留）；同页会话内此后再次导入，进度也不刷新（action 内重新捕获新对象，模板读旧对象）。iter-8 G4 全量 Code Review 发现；既有测试只断言 store 状态漏过 DOM 层 | 一般（数据无损，刷新页面即恢复；影响迁移条交互） | 已修复 | 修复（随本条同提交）：kinds 改 computed 持有——store 替换触发重算，模板引用始终是当前对象；回归断言补 DOM 层（暂不导入/知道了后 `.mig-banner` 数为 0），185/185 + 构建通过 |
| DEF-020 | 2026-08-16 | iter-9 设置页「账号」分区 label 字号偏离 design-iter-9：design 为 13px/600 text-2（settings-section-label），实现沿用既有 `.section-label`（14px/600 text-1）。QA 审计观察项 3 指出 | 仅视觉 | 已接受（按既有实现） | 与设置页既有分区（外观/供应商档案）label 字号保持一致，避免同页面分区 label 字号不一致；design-iter-9 该触点标注为示意口径 |
| DEF-021 | 2026-08-16 | iter-9 改密成功 toast 形态偏离 design-iter-9：design 为「✓ 密码已更新」（绿）+「其他设备已退出登录」（白）双色，实现简化为整条 success 绿「✓ 密码已更新，其他设备已退出登录」。QA 审计观察项 3 指出 | 仅视觉 | 已接受（按既有实现） | 与注销成功 toast「✓ 账号已删除，再见」整条 success 绿形态一致，统一 toast 视觉；design 双色为文案级示意 |
