# 迭代计划 — iter-12（2026-08-16 ~）

> 批准：2026-08-16 CEO 批准（草案原样通过 Σ8 = M+M+L；移动端主界面适配 CEO 定夺不纳入，留暂缓池作 iter-13 候选）。
> 变更依据：requirements/changes.md CHG-006（基线 req-baseline-v4，tag 已推送）。

## 迭代目标

管理员进入后台即见概览统计卡（总用户/今日请求/今日 token/统一 key 用量），能搜索定位用户、流畅翻页浏览，观感对齐现代管理后台——REQ-029 全量达成，REQ-025 功能口径零回退。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-N` tag）才能开始开发，设计任务排在开发任务之前。
> 含 UI 的任务，验收标准必须包含「design-iter-N 走查清单留档」（v1.4.4：走查是任务级门槛，不是迭代末补做）。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 design-iter-12 设计稿：概览统计卡（四指标）+ 用户搜索框与分页控件形态 + 视觉重构方向（信息层级/卡片/留白/状态徽标）+ 后端分页 API 口径（limit/offset 参数与响应结构、统计口径） | REQ-029 | M | 类比 design-iter-8（同规模单页原型，四项定夺一次基线）；通用下拉菜单组件已存在（iter-11），无新交互规格章节 | CEO 批准基线（tag design-iter-12）；「今日」界定/缺失时段处理（不估算补齐，铁律 5）/分页参数与响应结构随稿定案 |
| T1 后端分页与搜索：GET /api/admin/users 与 /api/admin/usage 增 limit/offset 分页 + 用户名搜索过滤 + 概览统计聚合（总用户/今日请求/今日 token/统一 key 用量进度，复用既有 usage_daily 数据，无新表迁移）；六端点功能口径零回退 | REQ-029 | M | 拆解：单模块（admin router）三组改动（分页/搜索/聚合）各含测试与异常分支；数据已落库（iter-8 T1 usage_daily + stream_options.include_usage），无新技术 | pytest 新增用例全绿：分页（含越界/空页）/搜索（命中/空态/大小写）/统计口径（跨零点归属）；REQ-025 test_admin 19 用例零回退；ruff clean |
| T2 AdminView 前端重构：概览统计卡区 + 用户搜索 + 用户/用量双列表分页控件 + 全页视觉重构（卡片/留白/状态徽标）；封禁/调配额/403 门禁/全站配额条流程沿用 | REQ-029 | L | 拆解：AdminView 全页重排（三个新块：卡区/搜索/分页 × 双列表）+ 既有封禁/调配额模态与全站条适配；类比 iter-8 T2 AdminView 初建（L）与 iter-11 T1 单区块整体重建；既有 AdminView.spec 15 用例回归 | design-iter-12 走查清单留档（亮/暗双主题）；REQ-025 既有走查口径（403/封禁/调配额/全站条/在线被封禁跳登录）复验不回退；分页翻页/越界/空页、搜索空态走查；vitest 全绿 + guard:style + 生产构建 |

复杂度合计：**Σ8 = M + M + L**（大迭代容量上限 Σ ≤ 10，iter-6~11 连续六轮零偏差校准值；本迭代留余量）。

## 本迭代不做

| 需求/事项 | 原因 |
|------|------|
| 移动端主界面适配 | 暂缓池维持（CHG-006 登记，纳入须走变更）；2026-08-16 CEO 定夺不纳入 iter-12，留作 iter-13 候选（届时走 CHG-007 变更基线） |
| 联网搜索 / 工具调用 | 暂缓池维持（CHG-004 CEO 决策：待后端就绪后走下一次需求变更） |
| v0.5.0 发布 | 待服务器（VPS/域名/HTTPS）就绪走 /mm-release，与本迭代无耦合 |
| 用量趋势图表 | CHG-004 定案「图表不做」；REQ-029 统计卡为数值卡，不含趋势图 |

## 风险清单

| 风险 | 应对 |
|------|------|
| 统计聚合口径（「今日」自然日界定、统一 key 用量进度分母、缺失时段）易出理解分歧 | T0 设计稿定死口径随基线批准；实现沿用 usage_daily 既有 day 维度（本地自然日，iter-8 T1 先例）；缺失时段「不估算补齐」（铁律 5） |
| 分页/搜索引入后既有 AdminView.spec 15 用例与 REQ-025 走查清单回归面广 | REQ-025 既有走查清单（plans/iter-8-verify.md T2 段）作为复验门槛；分页越界/空页/搜索空态列为新增走查条目；先全量基线再改动 |
| REQ-025「六端点零回退」是硬验收，分页改造动响应结构时易破坏既有消费方（AdminView 既有逻辑/测试） | T0 定案响应结构兼容策略（如默认 limit 下既有响应形状不变）；test_admin 19 用例零改动通过为第一道门槛，需改用例即触发兼容设计回稿 |

## 实际结果（复盘时回填）

- 完成任务：{{}}
- 复杂度：计划 Σ8 vs 实际完成 Σ{{}}（新增范围注明）
- 砍掉的范围与原因：{{}}
