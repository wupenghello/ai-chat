# 迭代计划 — iter-6（2026-08-15 ~ 2026-08-29）

> **已批准——2026-08-15 CEO 批准，按草案 Σ10 原样通过**。T0 技术待澄清项同日定案：登录态 token 传递方式 = **HttpOnly Cookie**（同源部署前提，防 CSRF 措施随设计基线说明，前端不做 token 管理）。
>
> 容量依据：iter-4/5 复盘维持 Σ ≤ 10（S=1/M=2/L=3）。**本迭代引入 Python FastAPI 新技术栈，后端任务（T1/T3 的后端侧）无任何校准数据，估算置信度低，初值均为拍脑袋值**；iter-6 实际完成情况将作为后续后端任务的校准基线。另按 iter-5 复盘注记，容量外预留 1S 缺陷修复缓冲（新栈首迭代，建议照此预留）。
>
> 仓库布局：按 **CHG-004 决策（仓库布局 monorepo，CEO 2026-08-15 拍板）**——后端代码放本仓库新增 `backend/` 目录（FastAPI 独立依赖与 lockfile），前端代码保持根目录不动，`docker-compose.yml` 放仓库根（引用以 changes.md 补录落盘为准）。

## 跨迭代切分建议（CHG-004 整体 L~XL，分三段）

- **iter-6（本次）——后端地基 + 注册登录闭环**：FastAPI 骨架（auth/sessions/proxy 模块位起步）+ SQLite、REQ-020 注册登录全量、REQ-022 服务端会话存储核心（登录后读写走服务端）、登录/注册 UI 与登录态路由守卫。对话请求本迭代仍走前端直连上游（过渡态）。
- **iter-7——流式代理与密钥双模式**：REQ-023 OpenAI 兼容 SSE 代理（前端切到后端端点）、REQ-014/018 改写落地（密钥与供应商档案迁服务端、高级设置重设计）、仓库根 `docker-compose.yml` 扩为全链路一键起（前端构建产物 + `backend/` + DB）、REQ-022 断网暂存重试与 LWW 多设备细项。
- **iter-8——治理与收尾**：REQ-024 配额与滥用防护（注册限频/每用户配额/统一 key 总量保护）、REQ-025 管理员后台（首个注册用户为管理员、封禁/配额/用量列表）、REQ-021 账号管理（改密/注销）、REQ-022 存量迁移入口（一次性导入 + 本地只读 30 天清除）、自部署文档，收口 v0.5.0 发布（发布决定 G3 由 CEO 批准）。

## 迭代目标

用户可以注册账号并登录，登录后创建和产生的会话保存在服务端、重新登录（含换设备）后完整恢复。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-6` tag）才能开始开发，设计任务排在开发任务之前。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 design-iter-6 设计稿并基线 | REQ-020（登录/注册页原型）+ REQ-006（未登录应用框架说明） | M | 类比 iter-4 T0（2 触点 M）：2 个新 UI 面（登录/注册，可单页切换），复用现有令牌与 components.md 表单模式；另附「未登录只见登录页、登录后进主界面」的应用框架说明（CHG-004 影响评估要求） | 登录/注册原型 + 应用框架说明经 CEO 批准并打 `design-iter-6` tag；token 传递方式已定案：**HttpOnly Cookie**（2026-08-15 CEO 定；同源部署前提，防 CSRF 措施随设计基线说明，前端不做 token 管理） |
| T1 FastAPI 后端骨架（`backend/`）+ SQLite + 注册/登录/登出 API | REQ-020 + 非功能「架构/数据/安全/部署」 | L | 引入本项目未用过的全新技术栈（FastAPI + SQLite + pytest + 容器化），满足 L 定义「引入新技术」；后端代码落仓库新增 `backend/` 目录，独立依赖与 lockfile，与前端根目录互不污染（CHG-004 决策（仓库布局 monorepo））；内含 REQ-020 后端全量（argon2/bcrypt 哈希、token 签发与校验、401、登出失效）。**拍脑袋值，无校准数据** | 注册→登录→携 token 访问受保护端点→登出后 401 全链路通；数据库无密码明文（密码字段全为哈希）；重复用户名返回「用户名已存在」且用户数不增；错误登录统一「用户名或密码错误」；pytest 覆盖上述分支；仓库根 `docker-compose.yml` 经 `docker compose up` 可起后端容器（全链路 Compose 随 iter-7 扩齐）；`backend/` 依赖与 lockfile 独立（不引入前端 node 依赖，前端构建流程不受影响）；存储结构带版本号（非功能「数据」条款） |
| T2 登录/注册页 UI + 登录态路由守卫 | REQ-020 + REQ-006（未登录门禁部分改写条款） | M | 前端为已有校准栈：类比 iter-2 SettingsForm（表单+校验+错误提示）M；新增 vue-router 路由守卫（beforeEach），路径明确无分支 | 新用户名注册 3 秒内进主界面，刷新保持登录态；用户名已存在/密码不达 8 位/两次不一致均前端拦截并提示；密码错误 3 秒内出统一提示；未登录访问任何功能页一律跳登录页，登录后回跳原页面；token 失效后操作收到 401 并跳登录（REQ-006 验收子集「未登录只看得到登录页」达成） |
| T3 服务端会话存储核心（登录后读写走服务端） | REQ-022（核心子集）+ REQ-006（数据层切换） | L | 跨模块且跨技术栈边界：`backend/` sessions CRUD API + 前端三处换源（stores/sessions 读写、api/client、db/idb.ts 降级为一次性迁移读取源），满足 L 定义「跨模块协作」。**含新栈部分，拍脑袋值** | 登录后新建/重命名/删除会话、发消息及 branches 版本切换均写回服务端；刷新与重新登录（含另一浏览器模拟换设备）会话列表与内容逐字恢复；浏览器 IndexedDB 不再产生新的会话数据写入（REQ-006 验收子集）；既有 85 条前端测试在切源后全绿（交互逻辑不变，仅数据层换源） |

**合计 Σ10**（M×2 + L×2）。其中 T1 全部与 T3 后端半侧为新栈拍脑袋值；容量沿用 Σ ≤ 10，置信度说明见文件头。

## 本迭代不做

| 需求 | 原因 |
|------|------|
| REQ-023 流式代理与密钥双模式（P0） | 依赖账号体系先落地；代理与配额检查同批实现，排 iter-7 主线。本迭代对话请求仍走前端直连上游（过渡态，iter-7 收口） |
| REQ-024 配额与滥用防护（P0） | 与 REQ-023 同批（配额检查在代理层逐次执行），排 iter-7/8；iter-6 注册不限频的暴露窗口由「本地部署、尚未公开」缓解 |
| REQ-025 管理员后台（P1） | 依赖用户与用量数据积累（按用户按日用量列表），排 iter-8 |
| REQ-021 账号管理（P1） | 依赖 REQ-020 落地后的账号设置 UI 与多设备 token 失效机制，排 iter-8 |
| REQ-022 细项：断网暂存重试、LWW 多设备冲突用例、「导入本地会话到云端」迁移入口（含只读 30 天清除） | 本迭代只做存储核心；异常分支细项排 iter-7，存量迁移排 iter-8（旧版本本地数据本迭代原样保留，不影响使用） |
| REQ-014/REQ-018 改写落地（密钥与供应商档案迁服务端、高级设置重设计） | 依赖 REQ-023 代理与密钥代管框架，随 iter-7；本过渡迭代供应商档案仍存浏览器本地，属已登记的过渡态，iter-7 收口 |
| 联网搜索 / 工具调用 | 暂缓池（CEO 2026-08-15 决策），纳入须走需求变更流程 |
| useTheme 单测补齐（iter-5 复盘 QA 观察项 4，iter-6 候选） | 容量优先让给后端主线，转 iter-7 候选 |

## 风险清单

| 风险 | 应对 |
|------|------|
| 新技术栈 ramp-up：FastAPI/Python 生态、pytest、依赖与容器化均无项目内经验，估算置信度低（T1/T3 初值为拍脑袋值） | T1 采用 FastAPI 官方标准模式（tutorial 架构 + Pydantic + pytest），先立测试基线再堆功能；后端任务阻塞超过 2 天即按流程升级 CEO 决策（简化实现或砍范围）；iter-6 复盘建立后端复杂度校准基线 |
| SSE 流式代理转发（iter-7 主线，但 iter-6 骨架若不预留将返工） | T1 骨架预留 proxy 模块位并做一个 async 流式 echo 端点验证技术形态（httpx AsyncClient + StreamingResponse），确认连接中断可向前端传递后再定骨架；不在本迭代实现真实代理 |
| Docker Compose 联调环境：`backend/`（仓库根 `docker-compose.yml`）与前端 dev server 跨进程联调，CORS、端口、SQLite 数据卷；token 已定案 HttpOnly Cookie，对跨域凭证（credentials）与同源部署有约束 | iter-6 只需仓库根 compose 起 backend 容器 + Vite dev server（proxy 转发 API，同源规避跨域凭证问题），全链路 Compose 随 iter-7 扩齐；token 方案已定（2026-08-15 CEO 定），剩余 CORS/端口/数据卷细节随 T0 设计基线与 T1 技术说明定 |
| 过渡态一致性：本迭代对话直连上游、档案仍在本地，与基线 v3 终态不符 | 已在「本迭代不做」与任务表明确登记为过渡态，QA 审计按本计划验收口径执行；iter-7 收口后补验终态条款 |

## 实际结果（复盘时回填，2026-08-16）

- 完成任务：T0~T3 全部完成（4/4）+ QA 审计 6 NCR 整改同迭代消化 + compose 容器化验收收口
- 复杂度：计划 Σ10 vs 实际完成 Σ10（新栈校准基线：T1=L 吻合；T2 实际 < M；T3 实际 < L——估算偏保守；容量上限维持 Σ≤10，后端任务 L 起评置信度上调）
- 砍掉的范围与原因：无砍范围、无延期；追加（已批准）：侧栏登出入口基线增量（design-iter-6 定案 5）、5xx 重试缺口修复（DEF-014，走查发现当场修复）
