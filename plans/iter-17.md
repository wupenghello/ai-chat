# 迭代计划 — iter-17（2026-08-20 ~ 2026-09-03）

> 状态：**已批准（2026-08-20，CEO 定夺「Σ10 顶格」，按草案落盘）。迭代已关闭（G4 过，2026-08-21：QA 2 NCR 当轮整改 + 复盘 A/B/C 全批落制度 v1.4.16，见 retros/iter-17.md）**。
> 变更依据：requirements/changes.md **CHG-011**（已批准 2026-08-20，CEO 原话「全部按推荐批准」，基线 req-baseline-v8）——本迭代 = 七期路线第五期 **C「五层记忆体系」**，排入 REQ-042（P0）/ REQ-043（P1）+ REQ-002/008/028/036 改写实现面 + 波及 5 项（REQ-039/021/034/037/038）复验；REQ-044 按定夺⑩不立项（遥测落行并入 REQ-042，admin 可见性入暂缓池）。A1/A2/B1/B2（REQ-030~041）已于 iter-13/14/15/16 全交付并关闭 G4，B2 已提供 C 的复用面：compress.py 摘要调用基建（调用器形态/超时护栏/三终态降级/转写渲染/包裹哲学/遥测落行模式；context_summary 与水位语义为会话级口径不在复用面）。
> 迭代时长：名义标准 2 周（2026-08-20 ~ 2026-09-03，项目 tailoring.md「迭代长度：2 周」适用，无偏离需登记）；沿近期单日迭代节奏（iter-13~16 均 1~2 天内收口），实际周期以开发 → 测试 → QA 审计 → Code Review → 复盘全部收口为准。
> 定夺状态：CHG-011 十项定夺已**全部定案**（②抽取触发 = 服务端常驻静默窗口扫描 / ③抽取不计回合、tokens 仅落 telemetry / ④注入动态尾区之后摘要之前（五层注入序）/ ⑤自由文本 ≤200 字 × 上限 50 整体替换 / ⑥整体一键停用、单条停用不做 / ⑦设置弹窗第六分区 / ⑧跟随当前模式失败降级 / ⑨页内注入逐字预览、回合内不提示 / ⑩REQ-044 不立项）——本计划**无开放定夺项**；定夺②的微参数 N/X/扫描间隔精确值由 T0 取证回填（非定夺项，取证即定死），定夺⑤上限 T0 可 ± 微调并登记不走变更。

## 迭代目标

新会话说「按我平时的偏好来」，AI 记得我是谁、喜欢什么：长期记忆随对话自动沉淀（会话后异步抽取）、新会话自然注入；设置里多一个「AI 的记忆」分区，可看到 AI 记住了什么、可编辑/删除/一键停用，且能逐字看到实际注入对话的内容；无记忆或停用时基线 v7 口径零回退（前端 345 + 后端 282 存量用例回归全绿）。

## 任务表

> 设计基线是**全部开发任务（含非 UI 的纯后端/数据面任务）的前置**，严格串行（v1.4.15）：T1 design-iter-17 未基线前不派任何开发任务；T0 为取证（零产品代码改动），不是开发任务，排在设计前（沿 iter-14/15/16 T0 先例）。
> 含 UI 的任务，验收标准必须包含「design-iter-N 走查清单留档」（v1.4.4：走查是任务级门槛，不是迭代末补做）。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 前置取证（抽取 prompt 真实冒烟 + 微参数定死 + 记忆体量对 B2 阈值影响复核）：① 抽取 prompt 真实 DeepSeek 冒烟（统一 key）——预置含事实/偏好/约定的多轮会话素材，实测抽取质量（记忆点准确度、一条一个独立记忆点、去重与冲突以最新为准的合并效果）与格式纪律（完整新列表输出稳定性）、单条 ≤200 字与总数 ≤50 约束达成度，定稿抽取 prompt 逐字登记（后端拥有、逐字断言面，沿 SUMMARY_PROMPT R2 定稿先例）② 微参数定死：轮数下限 N / 静默窗口 X 分钟 / 扫描间隔（CHG-011 定夺②精确值，实施期占位 4 / 10 分钟 / 60s）——以真实会话节奏取证合理性，定死写入 config 口径定稿 ③ 记忆体量影响复核：50 条 × 200 字满载记忆注入后的请求体体量与 B2 阈值 7000 的关系实测（CHG-011 内容 3.1「T0 冒烟复核，若实测体量异常收紧上限参数」授权口径）④ 注入文案定稿（`<user_memory>` 包裹形态与条目拼装格式逐字定死）⑤ 沿 iter-14/15/16 T0 取证模式留档 | REQ-042（描述「N/X T0 取证定死」「抽取 prompt 约束口径 T0 定稿」；内容 3.1 体量复核授权） | S | 类比：iter-14/15/16 T0 单点取证均定 S（路径明确、零产品代码改动）；本次为一次真实抽取冒烟 + 三个微参数定死 + 一次体量复核，性质同档取 S（若冒烟暴露缺陷按登记纪律另立 DEF，不预先吃余量） | 抽取 prompt 定稿文案逐字登记 + 注入文案逐字登记（逐字断言面）；N/X/扫描间隔精确值留档 `plans/iter-17-verify.md` T0 段并回填 changes.md CHG-011 定夺②精确值字段；体量复核结论留档（异常则按授权收紧上限参数并登记）；冒烟质量与格式纪律实测结论留档；零产品代码改动、无未登记变更 |
| T1 design-iter-17 设计基线：设置弹窗第六分区「AI 的记忆」+ API 口径——① 分区页形态：记忆条目列表（条目文本 + 来源/时间元信息）+ 条目编辑态 + 删除确认 + 整体停用开关（停用态页面明示语义文案）+ **注入文案预览块**（逐字 = 实际组装注入内容含 `<user_memory>` 包裹，定夺⑨）+ 空态（含「记忆在对话中自动沉淀」说明）+ 加载失败态（降级提示 + 重试）② 记忆管理 API 口径章节随稿定案（GET /api/memory 列表+停用状态+注入预览 / PUT /api/memory/{id} / DELETE /api/memory/{id} / PUT /api/memory/settings 开关，错误形态沿 sessions.py 404/409 detail 形状先例）③ 走查清单随稿（亮/暗双主题 + 空态 + 停用态 + 加载失败态 + 编辑/删除交互 + 注入预览逐字核对，几何断言面 v1.4.11 B、行为断言面适用项 v1.4.13 B；= REQ-043 验收 6 断言面）④ REQ-028 既有五分区口径零回退锚点逐条映射既有走查编号 ⑤ **卫生项：spec REQ-043「涉及页面」指针随基线零滞后回填（v1.4.10 A 防线）** | REQ-043（主）+ REQ-028（既有口径零回退锚点） | M | 类比：iter-15 design-iter-15 为 1 遥测视图触点 + API 口径章节定 M、iter-16 design-iter-16 为 2 触点 + API 口径章节定 M；本次为 1 个页级分区（七态：列表/编辑/删除/停用/预览/空态/失败态）+ API 口径章节——首个「页级设置分区」（CHG-011 顶格理由③），形态复杂度高于 iter-15/16 单卡单菜单，仍取 M（不另设上沿档） | CEO 批准基线（tag design-iter-17）；分区页七态形态与样件文案定死（逐字断言面——v1.4.10 B）；API 口径定稿；走查清单随稿；REQ-028 五分区零回退映射；spec「涉及页面」指针零滞后回填 |
| T2 后端记忆子系统核心 + 迁移 v10 + 记忆 API：① 迁移 v10——user_memories 表（user_id FK 级联、source_session_id/model 机器记录列）+ memory_jobs 表（PK (user_id, session_id)、status pending/done/error、watermark_msg_id、attempts）+ users.memory_enabled 加法列（默认 1）② memory.py 记忆读写 + 注入组装辅助——启用且非空时 `<user_memory>` 包裹为独立 system 消息，挂载 system[1] 动态尾区之后、摘要消息之前（五层注入序，定夺④）；停用/无记忆时组装与基线 v7 逐字段等价 ③ 常驻扫描任务（main.py lifespan 挂载，**七期首个常驻后台任务**）——周期扫描静默窗口（轮数 ≥ N + 静默 ≥ X + 有未覆盖增量 + memory_enabled=1 + 上游可解析）→ memory_jobs pending 行落库 → 执行；**重启恢复口径 = memory_jobs 持久化唯一权威（pending 行启动后拾起）**；在途调用随进程丢失、attempts 只计已完成失败（上限 3 留 error 行）④ 抽取执行器——沿 B2 call_summary 基建（非流式、独立超时护栏、SummaryOutcome 三终态）、跟随该用户当前模式（定夺⑧）、抽取 prompt 用 T0 定稿、模型输出完整新列表 → 后端同事务先删后插整体替换（限条数上限，定夺⑤）；失败降级恒为「无新记忆、存量照常注入」⑤ telemetry memory_extract 行（kind 加法扩展、endpoint='memory'、turn_id/step=NULL、session_id=触发来源、行不含记忆内容全文与 key）⑥ 记忆管理 API 路由——GET/PUT/DELETE + 停用开关（用户 token scope 归属隔离、跨用户 404）⑦ 微参数 config 字段（T0 定死值，.env.example 占位）⑧ 卫生：抽取不计回合、usage_daily 零写入（quota.py 零改动，定夺③）；注销级联（REQ-021 波及复验） | REQ-042（主）+ REQ-043（API 数据面）+ REQ-002/036 改写实现面 + REQ-039/021/034/037 波及复验 | L（取 4） | planning.md §2 L 定义「跨模块协作，或引入本项目未用过的新技术」双命中：七模块协作（db.py 迁移 v10 / memory.py 新模块[预估 200~250 行] / main.py lifespan 常驻任务 / telemetry.py 新 kind / proxy.py _assemble_pipeline 注入扩展 / memory 路由 / config.py）+ **常驻后台任务为七期路线首次引入（无先例可复用，CHG-011 顶格理由②）**；对比 iter-16 T2（五模块协作、全在既有框架上加法）取下沿 3，本期新增「首次常驻任务 + 重启恢复」不确定性面，取 4 不取下沿 | REQ-042 验收 1~7 逐条（①注入正确性：记忆 system 消息位置 = system[1] 之后（有摘要时记忆在前摘要在后）、内容 = 包裹条目逐字 / ②停用零回退：memory_enabled=0 组装与基线 v7 逐字段等价（REQ-033/036/039 既有验收复跑含压缩生效会话）/ ③抽取闭环：触发条件满足 → 假上游返回列表 → 整体替换 + job done + 恰 1 条 memory_extract 行（tokens = 假 usage 机器读数）/ ④失败降级：500/超时 → job error + attempts+1、记忆表不变、回合主路径正常 / ⑤重启恢复：预写 pending 行 → 新建 app 实例拾起执行 / ⑥存储隔离：会话 PUT 载荷形状零变化（vitest）+ 注销级联清零 / ⑦卫生断言：行与日志零记忆全文零 key，pytest + MockTransport）；REQ-043 API 面验收 5（归属隔离 404）；quota.py 与 usage_daily 数据面零改动验证；pytest 全绿 + ruff clean |
| T3 前端记忆分区 + 全局回归收口：① SettingsForm TABS 加法第六项「AI 的记忆」（分区导航/方向键/Esc/遮罩机制零变化）+ 记忆分区面板（列表/编辑/删除确认/停用开关/注入预览/空态/加载失败重试，按 design-iter-17 定稿，样件文案逐字）② 记忆 store + api 客户端加法扩展（client.ts/sessions.ts 回合数据面零改动——记忆注入为服务端行为）③ 编辑/删除/停用 toast 反馈与下一回合生效语义 ④ 全局回归基线 C 面收口走查 | REQ-043（主）+ REQ-028（六分区口径落地）+ REQ-038（定夺⑩零变化复核锚点） | M（取 3，M 上沿） | 类比：iter-16 T3「端点 + 菜单项 + 懒回填 + 聚合 + 卡」定 M2；本次为一个页级分区面板（七态）+ store + api 扩展 + 走查，交互面宽于菜单加法项（CHG-011 顶格理由③首个页级设置分区），取 M 上沿 3 | REQ-043 验收 1~6 逐条（①分区可达 + REQ-028 既有断言复跑零回退（vitest）/ ②查看/编辑/删除闭环：编辑保存 → 下一回合注入 = 新文本、删除 → 注入不含该条（pytest + vitest 联动）/ ③整体停用 → 下一回合请求体无记忆消息 + 页面停用态 / ④注入可见：预览与组装注入逐字一致 / ⑤归属隔离 / ⑥design-iter-17 走查清单留档：亮/暗双主题 + 空态 + 停用态 + 加载失败态 + 编辑/删除交互）；vitest 全绿 + guard:style + 生产构建；全局回归基线 C 面收口：pytest 282 + vitest 345 存量全绿（改写用例逐条登记映射口径），功能性删除为零 |

**携带项追溯**（planning.md §1「对应 REQ」映射规则：无独立 REQ 承载的子项填合法来源，来源编号已回查出处文档核对、不凭记忆——v1.4.8）：
- **T0 取证三项**：出处 changes.md CHG-011 内容 3.3 明文「注入/抽取文案后端拥有、T0 定稿后逐字断言面登记 verify 文档（沿 SUMMARY_PROMPT R2 定稿先例）」+ 内容 3.4「N/X/扫描间隔待 T0 定死，实施期占位」+ 内容 3.1「记忆体量上限对请求体总量影响有限，不冲击 B2 阈值口径（T0 冒烟复核，若实测体量异常收紧上限参数）」+ 内容 5 工作量段「T0 取证项为 T2 实施输入前置」。
- **迁移 v10 与记忆 API 并入 T2**（不另立任务）：迁移为 REQ-042 描述既有承载（「存储 = 迁移 v10 新建…」），记忆 API 为 REQ-043 描述既有承载（「数据面 = 新增记忆 API」），属 REQ 内任务归位拆分，非 REQ 外携带。
- **缺陷/观察项携带核查**：plans/defects.md 截至 DEF-036 全部已修复/已销账/已接受闭环；qa-audit-iter-16 的 1 NCR 当轮整改闭环、4 OBS 已处置（OBS-1 主会话代审计 CEO 接受，qa 员工 API 配额 429 重置 2026-08-26——**本期 QA 审计若配额未恢复，沿 iter-16 OBS-1 先例报 CEO 定夺处置方式**）——**本迭代无未闭环缺陷需携带**（v1.4.8）。

复杂度合计：**Σ10 = S1 + M2 + L4 + M3（容量 Σ≤10，顶格）**

- 口径对齐（沿 iter-13~16 定案数值口径：S=1 / M=2 / L=3~4，process/planning.md §2 只定义质性档位）；容量上限 Σ≤10 维持（iter-6~16 连续十一轮零偏差校准，见 retros/iter-16.md）。
- CHG-011 影响评估拆解预估 Σ9~10（T0 S + T1 S~M + T2 L + T3 M），审核稿原定级 **Σ10**；本计划取 **Σ10 顶格**（CEO 2026-08-20 定夺）。**顶格理由**（CHG-011 影响评估在案）：① 审核稿原将 C 定级 Σ10（七期路线仅 A1/D1 同级）② C 含七期路线首个「常驻后台任务 + 重启恢复」基建（无先例可复用，不确定性高于 B2 管道）③ UI 面为首个「页级设置分区」（B2 REQ-040 仅菜单一项）。顶格先例对照：iter-13（A1，Σ10）顶格成功收口零偏差（复盘在案），代价管理依赖余量为零时的纪律——本期以串行纪律（v1.4.15）与备砍序兜底。
- 膨胀处置：顶格无余量，任何新增范围即触发 planning.md §3 砍范围不延期。备砍序（任一回退属范围变更，报 CEO 批准后执行并登记）：a) 记忆页编辑功能降级为「查看 + 删除 + 停用」（编辑后移，Σ−1，CHG-011 备砍序②既有口径）；b) T0 微参数取证范围收窄为保守占位直接实施（不建议砍——取证驱动为 iter-14/15/16 连续验证实践）。**REQ-042 记忆管道核心（存储 + 抽取 + 注入）为底线不回退**（审核稿 C 主体 + 与套壳拉开差距最大的产品特性承诺）。

**全局回归基线（C 面收口）**（沿 CHG-007 定义、CHG-009/010/011 延展的验收门槛，RTM 独立行跟踪）：迭代关闭时前端 345 + 后端 282 存量用例全绿（计划时点口径引 iter-16 终态收口记录，启动时以 pytest collect / vitest list 实测核实）+ 本轮新增全绿；REQ-033/036「组装等价」类用例遇记忆在位会话按 REQ-042 验收 1/2 口径改写、逐条登记映射（旧断言 → 新断言，CHG-011 测试基线段既有口径）；功能性删除为零；度量数据全部机器采集（铁律 5）；RTM 全局回归基线行 C 面收口为全期达成。

## 本迭代不做

| 需求/事项 | 原因 |
|------|------|
| REQ-044 记忆度量与可观测（admin 聚合可见性） | CHG-011 定夺⑩定案不立项（2026-08-20 CEO 批准）：memory_extract 遥测落行并入 REQ-042 为必含项（成本可查），admin/用户侧聚合可见性入暂缓池，纳入走 CHG |
| 用户 systemPrompt 迁云端（五层第 4 层服务端持久化） | CHG-011 取证确认现状 = 前端 localStorage + 随回合上传、服务端零持久化（多设备不一致为现行行为与已知边界）；本期仅口径注记（REQ-008 改写），机制零改动——如需迁云端另立定夺/CHG（CEO 呈批时已知悉） |
| 移动端主界面适配 | CHG-007 定序：让位至 agent UI 形态稳定后（可与 D2 搭班）——CEO 2026-08-17 批准；B1 已交付排期锚点满足后择机评估，纳入走 CHG；C 顶格无搭班余量 |
| A2 体验深化主题包 ①引用上标/角标联动 ②搜索质量与时效调优 ③搜索过程可视化增强 | CHG-009 定夺⑦定案不整包搭班（2026-08-18 CEO 批准）：④⑤ 已由 REQ-037/038 收编；①②③ 留暂缓池；C 顶格无搭班余量 |
| 搜索供应商对比评估（Tavily vs 博查横向专项） | 暂缓池条目（CEO 2026-08-18 指示）：以 A2 上线后真实运行数据为主要输入，数据积累后择机排期，纳入走 CHG |
| 真实天气工具（和风天气类专用 API） | 暂缓池条目（iter-14/15/16 计划定案不纳入）：凭据 + 专属 Host 已验证备存服务端 .env，择机再评估 |
| D1 deep-research 子代理 / D2 hooks | 七期路线第 6/7 期（暂缓池，候选非承诺，纳入各走 CHG；D2 可与移动端搭班）——C 交付后为下一候选 |
| GLM 自填端点多 system 兼容随测 | DEF-002 口径：GLM 429 余额不足，CEO 不充值；充值恢复后随测（CHG-009 T0 遗留，非本迭代承诺） |
| v0.5.0 发布 | 独立卡服务器（VPS/域名/HTTPS）就绪，不属本迭代范围；发布决定（G3）由 CEO 批准（CHG-011 发布影响：不受本变更影响） |

## 风险清单

| 风险 | 应对 |
|------|------|
| ① 常驻后台任务为七期首次引入：asyncio 常驻任务与 pytest 测试隔离、扫描与回合主路径的共存面、进程重启的在途状态处理均无项目内先例 | 「落库 + 重启恢复」口径的唯一权威 = memory_jobs 持久化（CHG-011 内容 3.4 定死），pending 行拾起为独立验收（REQ-042 验收 5）；抽取与回合零交互语义（抽取读会话档快照、回合主路径不读 job 表）作为设计约束先行；扫描任务失败不阻塞主路径（写失败隔离沿 telemetry 先例）；T0 微参数取证先行（N/X/扫描间隔），实施期保守占位兜底不阻塞开发 |
| ② 抽取质量不确定：模型输出「完整新记忆列表」的格式纪律、去重/冲突合并效果直接决定记忆可用性（审核稿 §九「AI 记得用户是谁」的体验承诺） | T0 抽取 prompt 真实 DeepSeek 冒烟先行（质量与格式纪律实测为实现输入，不凭文档臆断，沿 SUMMARY_PROMPT R2 定稿先例）；整体替换是唯一无歧义可断言的落库语义（去重/冲突交模型、后端只限条数——CHG-011 定夺⑤）；失败/超时/空输出恒降级「无新记忆、存量照常注入」（降级方向恒为现状，用户无感）；attempts 上限 3 防无限重试 |
| ③ T2 为本期最重任务（L=4，七子步骤跨七模块协作，含首次常驻任务与注入组装扩展），且顶格 Σ10 无余量 | 拆七个子步骤逐项验收（迁移 v10 → 记忆读写 → 注入组装 → 常驻扫描 → 抽取执行器 → 遥测行 → 管理 API），**停用零回退（REQ-042 验收 2）= 逐步验收的恒等基准**（每步回归锚点）；备砍序 a 预备（REQ-042 管道核心为底线）；开发纪律沿制度 v1.4.15：设计基线未落不派开发（串行）+ 任何并行 agent 完成回报由主会话亲跑测试门槛核验后采信（v1.4.14 B） |

## 实际结果（复盘时回填）

- 完成任务：T0~T3 全部（S1+M2+L4+M3）；新增范围 = CHG-011 清单/卫生项随批执行与 OBS 顺手修正（零容量占用）
- 复杂度：计划 Σ10 vs 实际完成 Σ10（连续十二轮零偏差；顶格两战两胜）
- 砍掉的范围与原因：无（备砍序未触发）
- 回归：pytest 282→312 / vitest 345→364 / 走查 34 PASS/0 FAIL；DEF-037 当轮闭环；QA 有条件通过 2 NCR 当轮整改；G4 于 2026-08-21 关闭
