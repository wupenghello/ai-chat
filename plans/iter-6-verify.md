# iter-6 视觉与状态走查记录（design-iter-6 基线，2026-08-16）

> 对照 `design/iter-6/index.html` §7.2 全部 27 条逐项走查（testing.md §5：完整走查不留抽查，DOM 实测优先）。
> 环境：Vite dev（5174）+ Docker compose 后端（8000，db_version 2）；DOM 计算值经浏览器实测采集。
> NCR-iter6-003 整改产物；走查中发现并当场修复：5xx 未挂重试按钮（清单 5）、其余见备注列。

| # | 触点/状态 | 结果 | 证据（DOM 实测值 / 用例） | 备注 |
|---|----------|------|------------------------|------|
| 1 | 登录页默认态 | ✅ | 品牌位「喵喵 ai-chat」+用户名/密码+「登录」+「还没有账号？注册一个」；无邀请码字样 | |
| 2 | 空值提交 | ✅ | 行内两条「请输入用户名」「请输入密码」，无 banner，未发请求 | NCR-002② 整改后 |
| 3 | 凭证错误 | ✅ | danger banner「用户名或密码错误」，0.9s 内（<3s），已填内容保留 | |
| 4 | 封禁（占位） | ✅ | warning banner「账号已被封禁」（造 banned 用户实测 403 路径） | REQ-025 治理流 iter-8 |
| 5 | 网络/5xx | ✅ | 停后端实测：danger banner「服务暂时不可用，请重试」+内嵌「重试」按钮，内容保留；恢复后端点重试→登录成功 | **走查中发现**：经 Vite 代理后端不可达表现为 500，原实现仅 status 0 挂重试——当场修复（≥500 也挂重试）+ 用例 |
| 6 | 加载态 | ✅ | 挂起 fetch 实测：`.submit-btn.loading`+disabled+spinner（animation: spin）+「登录中…」，背景保持 `#3370FF` 实底主色 | NCR-002⑤ 整改后 |
| 7 | 主题入口 | ✅ | 右上 32px ghost 按钮（computed width=32px） | |
| 8 | 登录页暗色 | ✅ | page `#131417`/card `#1E2026`/border `#33363E`/text `#E6EAF0`/text-2 `#A2A9B6`/submit `#3370FF`——全部命中 tokens v1.3 暗色，无亮色残留 | |
| 9 | 注册页默认态 | ✅ | 三字段（用户名/密码/确认密码）+「注册并进入」+「去登录」+2 个眼睛按钮；无邀请码 | 规则 hint 以 placeholder 呈现——已登记偏差⑥（changes.md CHG-004 澄清定案 10，CEO 批准） |
| 10 | 用户名规则不符 | ✅ | 行内「用户名需为 2~32 字符，仅限中文、字母、数字、下划线、连字符」，不提交 | |
| 11 | 密码不足 8 位 | ✅ | 行内「密码最短 8 位」，不提交 | |
| 12 | 两次密码不一致 | ✅ | 行内「两次输入的密码不一致」（确认字段），不提交 | |
| 13 | 用户名已占用 | ✅ | 「走查员a」（大写已注册）→ 用户名字段行内「用户名已存在」，密码内容保留 | 大小写不敏感一并实测 |
| 14 | 注册限频（占位） | ⏸ | 本迭代无 429 源（REQ-024 iter-8）；文案通道=通用 banner（后端 detail 直出），届时零 UI 改动 | 占位项按清单注明 |
| 15 | 注册成功 | ✅ | 「走查员A」注册 → 1.5s 内直接进主界面（无中间页），底栏显示用户名；刷新保持登录态（见 18/21 组证据） | |
| 16 | 注册页暗色 | ✅ | 暗色下注册模式 card `#1E2026`、2 眼睛按钮在位，令牌同 8 | |
| 17 | 认证错误映射 8 类 | ✅ | 实测覆盖：401 凭证=danger 统一文案（3）、401 过期=warning（20）、403=warning（4）、409=行内（13）、422 规则=行内（10/11）、网络/5xx=danger+重试（5）、429=通道具备（14）；无技术堆栈暴露（文案均人话） | 对照 design §3 |
| 18 | 未登录访问 | ✅ | 登出后访问 `/` → `/login`（本轮实测）；带 redirect 参数与未知路径兜底由 guard.spec 6 用例承载 | |
| 19 | 登录回跳 | ✅ | redirect 回跳本轮实测（登录成功回 `/`）；`//evil` 防 open redirect 由 LoginView.spec 用例承载 | |
| 20 | 401 闭环 | ✅ | `/login?expired=1` 到达 → warning「登录已过期，请重新登录」+ query 清理；登录后回跳 | NCR-002① 整改后 |
| 21 | 未登录零写入 | ✅ | 全程 `indexedDB.databases()` 为空数组（连库未建） | T3 验收同证 |
| 22 | 主界面基线不动 | ✅ | sidebar/composer/empty 态与 iter-1~5 一致；底栏=用户名+「未配置」档案标签+状态点+登出（唯一增量） | |
| 23 | HttpOnly 定案 | ✅ | `document.cookie` 为空（JS 读不到 token）；localStorage 仅 `ai-chat-theme`；全部请求同源相对路径 `/api/...` | |
| 24 | 密码处理 | ✅ | 全程 `type=password`，眼睛为唯一明文出口（3 个密码框各 1 个）；autocomplete：username/current-password/new-password 标注正确；密码不进 URL | |
| 25 | ≤480px 窄屏 | ✅ | 375px：无横向溢出（scrollW=375）、输入字号 **16px**（iOS 防缩放，NCR-002④ 整改后）、内容可滚动、按钮在视口内可达；注册模式同测 | 卡片 327px（页边距 24px）vs 设计口径 343px（16px）——归入已登记偏差⑦族 |
| 26 | 超长输入 | ✅ | 33 字符用户名被规则拦截（行内提示）；32 字符输入后卡片不撑破（375px 无溢出）；banner `line-height:1.5` 自然换行、无 nowrap | |
| 27 | 登出入口 | ✅ | 底栏用户名+26px ghost 登出 icon（aria-label「登出」）；点击直接登出（无确认）→ 后端会话失效+清 cookie → 跳登录页（本轮实测） | design §4.2 基线唯一增量 |

**结果：27 条中 26 通过、1 条占位（14，按计划 iter-8 接入）；偏差 2 类均已登记**（changes.md CHG-004 澄清定案 10：⑥placeholder、⑦视觉参数族，CEO 批准 2026-08-16）。

**走查驱动的即时修复**（本轮新增）：

1. 5xx（经代理的后端不可达）未挂「重试」按钮 → 修复为 status 0 与 ≥500 均提供重试（清单 5）；新增用例「网络/5xx banner 内嵌重试」。
2. NCR-iter6-002 整改项在本轮走查中逐条复验：①②③④⑤⑧ 全部落基线（见各条「整改后」注记）。

**测试基线**（机器采集，2026-08-16）：前端 vitest **120/120**（16 文件）、后端 pytest **37/37**、`guard:style` 通过、`vue-tsc && vite build` ✓。
