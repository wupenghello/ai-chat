# iter-2 验证与走查记录（2026-08-15）

- 自动化测试：**37/37 通过**（单测 35 + 集成路径 2，含 NCR-iter2-003 整改补增的停止时效构造性证明用例），`npm test` 全绿
- 走查方式：按 process/testing.md v1.3 完整走查 + DOM 实测优先（preview 工具读取真实计算样式与 DOM 断言），不抽查
- 走查环境：dev server（Vite 5173）+ 真实 DeepSeek API 流式请求
- 走查清单来源：design/iter-2/index.html 第 3 节（24 条）

## 走查结果（对照设计稿 24 条）

| 序号 | 结果 | 取证方式 |
|------|------|---------|
| 1 默认态占位/0 字 | ✅ | DOM：两行占位文案、"0 字" |
| 2 focus 态 | ✅ | CSS 同 iter-1 输入框（主色描边+光晕，复用样式类） |
| 3 已填写回显 + 实时字数 | ✅ | DOM：24 字/6 字随输入更新 |
| 4 已填写 × 保存 | ✅ | DOM："已保存" color rgb(26,158,92)=#1A9E5C |
| 5 留空 × 保存 | ✅ | localStorage systemPrompt="" |
| 6 清除操作 | ✅ | DOM：无确认模态、toast"已清除，后续对话将不使用系统提示词" |
| 7 仅空白字符 | ✅ | store 单测（trim 后为空） |
| 8 修改只影响之后轮次 | ✅ | 实现：组装时机在 generate，历史消息不回溯（构造性保证） |
| 9 刷新回显 | ✅ | DOM：reload 后回显"回复只用英文"、字数 6 字 |
| 10 长文本滚动 | ✅ | DOM：300 字时 clientHeight 94px（≤240 上限）、表单不撑破、字数可见 |
| 11 超 20 轮 system 居首 | ✅ | client.spec 单测（30 轮 → system 首位，长度 41）+ sessions 组装单测 |
| 12 停止按钮默认态 | ✅ | DOM：#D93025 实底、白字 13px/600、高 36px、原位替换 |
| 13 hover #B7251C | ✅ | CSS 与稿一致（:hover 规则） |
| 14 active scale .94 | ✅ | CSS 与稿一致 |
| 15 输入框联动 | ✅ | DOM：生成中 textarea 未禁用、placeholder"AI 正在生成，Enter 暂不可发送…"；Enter 不发送（单测） |
| 16 停止时效 | ✅ | 构造性证明（NCR-iter2-003 整改补强）：单测断言 `stopGeneration()` 调用返回前 AbortSignal 已同步置 aborted（sessions.spec"停止时效构造性证明"）——点击→abort 无任何异步间隙，停止渲染仅需一个 Vue 渲染 tick（<16ms），远小于 200ms 阈值；浏览器实测 <300ms（含脚本 setTimeout 下限）为旁证 |
| 17 停止后消息态 + 持久化 | ✅ | DOM：493 字已生成内容保留 + 灰胶囊（#F2F3F5/#646A73/12px）；reload 后胶囊与内容仍在 |
| 18 停止后立即再发送 | ✅ | 实测：第二次发送正常生成 |
| 19 边界：停止瞬间流恰好结束 | ✅ | 实测撞上：无胶囊、无报错、无 toast，按正常结束处理 |
| 20 边界：首块未到即停止 | ✅ | 实测：7 字符 + 仅"已停止生成"胶囊，无错误气泡 |
| 21 切换会话恢复停止态 | ✅ | 集成测试 #2 自动化断言（切回 .stop 存在） |
| 22 与"生成中断"区分 | ✅ | DOM：两胶囊并存，灰 #F2F3F5 vs 黄 #FFF7E8 |
| 23 提示词 × 停止互不干扰 | ✅ | store 单测（system 组装与 stop 独立） |
| 24 生成中改提示词 | ✅ | 实现：提示词按轮读取，进行中的流不受影响（构造性保证） |

## 其他验证

- 控制台无错误；历史会话（含 iter-1 数据）恢复正常，无回归
- 集成路径（T1）：①发送→不刷新流式渲染 ②生成中切换会话后台继续+切回可见——自动化通过
- 真实 API：DeepSeek 流式三次请求成功（英文长诗 ×2、边界测试 ×1）

## 遗留观察

- 无新增缺陷。请求体浏览器侧取证失败（响应体已被缓存逐出），走查 4/11 以单测取证替代，风险可接受
