# 迭代计划 — iter-8（2026-08-16 ~ 2026-08-30）

> **已批准——2026-08-16 CEO 批准，Σ10 方案（T0~T3；REQ-021 与全链路 Compose/自部署文档/v0.5.0 发布挪 iter-9 收口）。**
>
> 容量依据：iter-7 复盘（retros/iter-7.md Q4）上限维持 **Σ ≤ 10**（S=1/M=2/L=3）。iter-8 全量收口清单（REQ-024/025/021 + 存量迁移 + Compose）合计 Σ14，超上限 40%——按 planning.md 砍范围保节奏。**范围取舍（CEO 拍板 2026-08-16）**：REQ-021 与本迭代主线零耦合（治理/迁移均不依赖改密注销），是唯一可独立后移项；Compose 与发布形态同批（CEO 2026-08-16 已绑定），v0.5.0 相应挪 iter-9。备选方案未采纳，留档备查：Σ12（推荐 + Compose，v0.5.0 仍缺 REQ-021，iter-7 曾否决同型 Σ12）/ Σ14（全量收口，需推翻 iter-7 校准结论上调容量）。
>
> **配额初始默认值（CHG-004 挂账「后续待定」，CEO 拍板 2026-08-16 随本计划定案）**：免费档（统一 key 模式）每用户每自然日 **30** 次对话请求；自填 key 档每用户每自然日 **500** 次；统一 key 每日总量全站 **2000** 次（触发熔断、次日恢复）；注册限频每 IP 每自然日 3 个（spec 既有默认）。限额维度 = 对话请求数；token 数随请求记录（供 REQ-025 用量列表），不作为限额维度；全部数值经配置注入、管理员可按用户覆盖（REQ-025）。
>
> v1.4.4 走查新规（设计带走查清单 → 实现自查 → 任务验收留档）与 v1.4.5 文档随任务收尾（RTM 行与周报条目随任务验收提交同步更新）本迭代全程生效。

## 迭代目标

公开注册的多用户服务具备滥用防护与管理员治理能力，v0.4.x 老用户登录后可一键把本地会话与供应商档案迁到云端。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-8` tag）才能开始开发，设计任务排在开发任务之前。
> 含 UI 的任务，验收标准必须包含「design-iter-8 走查清单留档」（v1.4.4：走查是任务级门槛，不是迭代末补做；无 UI 变更的任务须注明不适用）。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 design-iter-8 设计稿并基线 | REQ-025（管理后台原型）+ REQ-022（迁移入口）+ REQ-018（存量档案上云提示条） | M | 类比 iter-6/7 T0（多触点 M）：3 个触点均为既有模式——后台表格列表页、迁移提示条 ×2，复用 design-system 令牌与表单/列表组件模式；管理后台为新增页面类型但无复杂交互（列表 + 筛选排序 + 操作按钮） | 设计稿经 CEO 批准并打 `design-iter-8` tag，**含 §7.x 走查清单（v1.4.4 硬要求）**；T0 定夺项（随设计基线由 CEO 拍板）：① 配额档位与数值在后台的呈现口径（联动已定默认值 30/500/2000）② 迁移提示条触发规则与「暂不导入」再提示节奏 ③ 管理后台入口可见性口径（仅管理员可见）④ 用量列表的列与筛选排序最小集 |
| T1 REQ-024 用量配额与滥用防护 | REQ-024 + REQ-023（配额检查位销账）+ REQ-007（配额提示） | L | 拆解跨三模块：注册限频（auth 路由按 IP 计数）+ 配额检查接入 proxy 已预留检查位（每请求计数、档位与密钥模式联动、周期重置、统一 key 每日总量熔断）+ 用量计数落库（按用户按日，供 REQ-025 统计）；类比 iter-7 T1（跨栈 L 实测吻合）。含 SQLite 高频计数写（DEF-015 线程绑定前科，短事务/WAL 应对） | 同 IP 连续注册超阈值（默认每 IP 每日 3 个）后被拒且提示明确；测试账号配额设 5 次：第 6 次发消息收到配额提示且**服务端日志显示该请求未转发上游**（REQ-023 销账）；周期重置后同一账号可再调用；统一 key 每日上限（默认 2000）触发后全站对话请求收到暂停提示、次日恢复；统一 key 模式超配额提示可经高级设置自填 key 解锁；配额数值经配置注入、管理员按用户覆盖的接口随 T2（本任务先落默认值路径）；REQ-007 配额提示文案接入（design-iter-7 占位文案已定稿）；pytest 覆盖阈值边界、周期重置时刻、双模式档位切换联动；**无 UI 变更，走查不适用（注明）** |
| T2 REQ-025 管理员 Web 后台 | REQ-025（+ REQ-024 联动验收） | L | 拆解三半侧：后端 admin 层（管理员标记/封禁解封/按用户调配额/按用户按日用量查询 + 403 门禁）+ 前端 AdminView（用户列表 + 用量列表两个表格与操作）+ 权限测试；类比 iter-7 T2（四半侧 L 饱满吻合）。依赖 T1 配额体系先落（调配额才有对象） | 首个注册用户具管理员标识（存量库以最早注册用户补标记）；第二个注册用户为普通用户，其访问后台路由/接口返回 403、不暴露任何后台数据；封禁某用户后该用户登录被拒并见封禁提示、解封后恢复；管理员尝试封禁自己被阻止并提示；管理员将某用户配额调整为 N 后该用户实际可用量按 N 生效（与 REQ-024 验收联动）；后台可见按用户按日的请求数与 token 数列表，与后端调用日志计数一致（抽样比对 3 天）；普通用户的任何界面/接口均获取不到其他用户用量数据；**design-iter-8 走查清单留档（v1.4.4）**；前后端测试全绿 |
| T3 存量迁移上云收口 | REQ-022（迁移入口 + LWW 并发用例补验）+ REQ-018（档案上云）+ REQ-006/REQ-014（本地清除与复验） | M | 拆解前端主导：旧数据检测（IndexedDB 会话 + 旧 localStorage 档案字段）→ 提示条 + 一键导入（会话 PUT 新增不覆盖、档案新增不覆盖）→ 本地转只读 30 天后自动清除；后端复用既有 sessions PUT 与 profiles CRUD（iter-6/7 已验证）无新端点；类比 iter-7 T3（单模块 persistence 层 M 偏松） | 旧版本本地 20 个会话经迁移入口导入后云端会话数一致、内容逐字保留（REQ-022 验收）；存量本地 2 套档案（v0.4.x localStorage）一键导入后云端多出该 2 套且本地不再存有任何档案数据，「浏览器检索不到 key」按全量口径复验销账（REQ-014，tailoring 2026-08-16 口径收口）；「暂不导入」本次零上传、下次登录再提示；导入中断不产生半套数据（原子性）；导入后本地数据只读、30 天后被清除可观测（REQ-006/022）；两设备同时修改同一会话标题的后保存者生效用例补验（LWW，iter-6 挂账）；**design-iter-8 走查清单留档（迁移提示条触点）**；前端测试覆盖上述分支 |

**合计 Σ10**（M×2 + L×2），容量守恒（Σ ≤ 10）。范围取舍与备选方案见文件头说明。

## 本迭代不做

| 需求 | 原因 |
|------|------|
| REQ-021 账号管理（P1） | 容量不足：全量收口 Σ14 超上限 40%；REQ-021 与本迭代主线零耦合（治理/迁移均不依赖改密注销），是唯一可独立后移项；挪 iter-9（随 design-iter-9 设计先行），与 v0.5.0 收口同批 |
| 全链路 Compose 一键起 + 自部署文档 + dev 路由按环境裁剪（QA 观察项 6） | v0.5.0 相应挪 iter-9，Compose 与发布形态同批收口（CEO 2026-08-16 已绑定两者）；当前 compose 仅 backend 容器（iter-6 就绪），前端构建产物服务为增量 M；iter-8 开发联调沿用既有环境（Vite dev server + 后端容器） |
| v0.5.0 发布 | 范围未齐（缺 REQ-021），iter-9 末走 `/mm-release` |
| auth_sessions 过期行清理、useTheme 单测（iter-5 观察项 4） | 候补（iter-7 计划遗留候选顺延）：主线提前完成且余量充足时按计划外新增登记（复盘补账） |
| 云部署（VPS/域名/HTTPS） | CHG-004「后续待定」，本地跑通后再议 |

## 风险清单

| 风险 | 应对 |
|------|------|
| 配额体系横切注册/代理/存储三模块，误伤正常用户（限频过严、计数错漏、熔断误触发） | 默认值全部可配置 + 管理员可按用户调整（REQ-025）；pytest 覆盖阈值边界、周期重置时刻、双模式档位联动；计数写路径短事务 + WAL（DEF-015 SQLite 线程绑定前科）；「配额不足不抵达上游」以服务端日志取证 |
| 管理后台为新页面类型 + 权限门禁，设计-实现偏差前科（NCR-iter6-002 登录页 8 处） | v1.4.4 全链：T0 设计稿带 §7.x 走查清单 → 实现对照清单逐条自查再提交 → 任务验收留档；403 门禁前后端双侧测试 |
| 存量迁移含不可逆的本地清除 | 导入为新增不覆盖（先云端后清除）；清除仅在导入完成 30 天后发生；导入中断保持原子性（不产生半套档案/会话）；「暂不导入」零上传、下次登录再提示 |

## 实际结果（复盘时回填）

- 完成任务：T0~T3 全部完成（2026-08-16，提交 04986ca/e6a8772+3277086/2f91e45/ada2cf5 + 整改与收尾序列至 f6ca9d9）
- 复杂度：计划 Σ10 vs 实际完成 Σ10（偏差 0%；T0/T1/T3 吻合、T2 饱和吻合，详见 retros/iter-8.md Q4）
- 砍掉的范围与原因：无砍范围。过程缺陷：DEF-018（一般，设计稿暗帧白底，CEO 走查发现）/ DEF-019（一般，迁移条状态引用滞留，Code Review 发现）全部修复；LoginView.vue 漏随 T2 提交（审计补证发现，53e6b0a 补提交）；QA 审计 5 NCR 全整改；复盘产出制度 v1.4.6（提交防漏核对）+ 走查脚本沉淀 scripts/e2e-walkthrough.mjs；浏览器走查以 puppeteer 真实 Chrome 30/30 闭账（预览端口受阻的降级路径，tailoring 登记）
