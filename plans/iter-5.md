# 迭代计划 — iter-5（2026-08-15 ~ 2026-08-29）

> 容量依据：iter-2/3/4 复盘维持 Σ ≤ 10（S=1/M=2/L=3）；CEO 试用驱动的打磨走计划外快速通道（iter-4 复盘定案）。CEO 确认全排（2026-08-15，Σ8）。

## 迭代目标

用户可以切换暗色主题（全界面适配、刷新保持），并保存多套 API 供应商档案一键切换。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-5` tag）才能开始开发，设计任务排在开发任务之前。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 暗色令牌 + design-iter-5 设计稿并基线 | REQ-017 / REQ-018 | M | 类比 iter-4 T0（多触点 M）；另含 design-system 新增整套暗色令牌（组织级资产，浅↔暗对照表 CEO 一次定夺） | 令牌对照表 + 设计稿经 CEO 批准并打 `design-iter-5` tag |
| T1 颜色 token 化 + 令牌命名统一 | iter-4 复盘遗留（--c-error/--c-danger 漂移） | S | 单一横切改动：各组件硬编码色值（#F2F3F5、#E8EBF2、代码块深底等数十处）替换为令牌；REQ-017 前置 | 全仓库无裸色值（design-system 已有令牌覆盖）；亮色下外观不变（DOM 走查回归） |
| T2 暗色主题 | REQ-017 | L | 跨模块：全组件暗色适配 + 切换入口 + 持久化 + AA 对比度；顺带沉淀 design-system components.md（iter-3/4 复盘遗留） | 切换 200ms 内全界面生效、无亮色残留；刷新保持；暗色正文对比度 ≥4.5:1 |
| T3 多供应商档案一键切换 | REQ-018 | M | 一个模块内完整功能：settings 单套→多档案 + 设置页列表 UI + 生成中切换旧档案跑完下次生效（CHG-002 已定案）+ 测试 | DeepSeek+GLM 两套档案保存后一键切换各自完成流式对话；当前档案持久化；生成中切换当前回复用旧档案完整生成 |

**合计 Σ8**（M×2 + L×1 + S×1）。

## 本迭代不做

| 需求 | 原因 |
|------|------|
| interrupted/stopped 消息重试入口 | 未基线（技术债登记），纳入须走需求变更流程 |
| 窄视口响应式 | spec 明确 MVP 不承诺 |

## 风险清单

| 风险 | 应对 |
|------|------|
| 硬编码色值 token 化面广，漏改在暗色下残留亮色 | T1 第一步全局盘点裸色值清单逐一映射令牌；走查亮/暗双向核对（iter-5-verify 留档） |
| 暗色令牌为组织级资产（design-system），色值定稿需 CEO 批准 | T0 出浅↔暗对照表（含对比度计算）一次定夺，避免逐色返工 |
| 主题切换入口位置、档案删除/编辑交互 spec 未定 | T0 列「待澄清」集中定夺（沿用 iter-1~4 固化模式） |
| 多档案与 401 引导 / 未配置引导联动（REQ-007/014） | 沿用现有错误引导链路；切换不中断生成（CHG-002 已定案语义） |

## 实际结果（复盘时回填）

- 完成任务：
- 复杂度：计划 Σ8 vs 实际完成 Σ
- 砍掉的范围与原因：
