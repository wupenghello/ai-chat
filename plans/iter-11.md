# 迭代计划 — iter-11（2026-08-16 ~）

> 批准：2026-08-16 CEO「可以，全部批准」（CHG-006 基线 v4 + 方案 A 范围 + 优先级 + 主题按钮交设计提案，一并批准）。
> 变更依据：requirements/changes.md CHG-006（基线 req-baseline-v4，tag 已推送）。

## 迭代目标

用户日常对话的主界面——侧栏清爽可收起、操作收进「···」菜单、消息流无头像干扰、设置即点即弹——观感与交互对齐 DeepSeek（REQ-026/027/028 全量达成）。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-N` tag）才能开始开发，设计任务排在开发任务之前。
> 含 UI 的任务，验收标准必须包含「design-iter-N 走查清单留档」（v1.4.4：走查是任务级门槛，不是迭代末补做）。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 design-iter-11 设计稿：侧栏重构 / 消息流与顶栏 / 设置弹窗 / 通用下拉菜单交互规格（外点关闭、Esc、键盘可达） | REQ-026~028 | M | 类比 design-iter-8/9 同规模单页原型（各一次基线即过）；增量为本轮新增通用下拉菜单交互规格章节 | CEO 批准基线（tag design-iter-11）；主题按钮去留提案随稿提交 CEO 定夺（REQ-017 条款若改写随基线同步） |
| T1 侧栏重构：列表项单行 + hover「···」下拉（重命名/删除）+ 今天/昨天/近7天/更早分组 + 底部账户区「···」菜单（设置/管理后台/登出）+ 侧栏收起（状态持久化）+ 通用下拉组件落地 | REQ-026 | L | 拆解：5 个子改动（单行化/菜单化/分组/账户区/收起）× 跨 TheSidebar + SessionListItem + 新组件 + 持久化；类比 iter-9 T2 视觉改版（M）再加三个新交互形态；corrupted/搜索态/生成中断 pill 均有适配分支 | design-iter-11 走查清单留档（亮/暗双主题）；「···」菜单外点关闭/Esc/键盘可达；分组跨零点边界单测；收起状态刷新保持；REQ-003/004/005/012/016 spec 适配全绿 |
| T2 消息流与顶栏：去全部头像 + 用户气泡浅色轻量化（双主题）+ 导出收纳入菜单 + 「模型」显示去误导 + 主题按钮按 T0 定夺执行 | REQ-027 | M | 类比 iter-9 T2（视觉基调反转，M）：MessageBubble/App 局部视觉改动 + 操作栏布局适配，无新交互形态；导出入口迁移牵动 export.spec 少量用例 | design-iter-11 走查留档（用户/AI/错误气泡/中断/编辑态/版本切换 × 亮/暗双主题）；REQ-013 导出口径不回退（spec 适配全绿）；统一 key 无档案时顶栏无「未设置」误导显示 |
| T3 设置弹窗化：SettingsForm 整页视图迁模态弹窗 + 账户区「···」菜单打开 + 错误气泡「前往高级设置」定位适配 | REQ-028 | M | SettingsForm（989 行）整体迁容器、逻辑零改动，主要工作为容器/滚动/关闭交互与 locateAdvanced 定位复验；类比 iter-8 T2 组件化封装（M） | design-iter-11 走查留档（含 Esc/遮罩关闭）；settings/settings-form/账号全量 spec 弹窗容器下全绿；iter-2 走查 15 场景（错误气泡→高级设置定位）复跑留档 |

复杂度合计：**Σ10 = M + L + M + M**（大迭代容量上限 Σ ≤ 10，iter-6~10 连续五轮零偏差校准值）。

## 本迭代不做

| 需求/事项 | 原因 |
|------|------|
| REQ-029 管理后台体验重构 | 独立页面 + 需后端分页 API，塞入本迭代 Σ 超上限 40%（planning.md：砍范围保节奏）；CEO 批准排 iter-12（design-iter-12 承载） |
| 移动端主界面适配 | 非功能需求现状仅登录/注册页承诺 480px；CHG-006 已登记暂缓池，iter-12 候选，纳入须走变更 |
| 联网搜索 / 工具调用 | 暂缓池维持（iter-10 计划预告的第一候选，让位于 CHG-006 体验对齐） |
| ComposerBox 输入区改版 | CHG-003 已定案（发送按钮与首行文字对齐），本轮不翻案；若 T0 设计稿提出异议须 CEO 单独定夺，不默认纳入 |

## 风险清单

| 风险 | 应对 |
|------|------|
| 通用下拉菜单是新交互组件：外点关闭、Esc、键盘焦点、多实例（列表项逐项一个）易出边界情况 | T0 设计稿先定死交互规格（含键盘操作矩阵）；T1 实现后走查覆盖打开菜单后滚动/切会话/连续开两个菜单等边界 |
| SettingsForm 迁弹窗牵动大量既有用例（含 iter-9 账号 21 用例），定位逻辑（locateAdvanced）回归面广 | 迁移原则「只改容器不改逻辑」；全量 vitest + iter-2 走查 15 场景复跑作为验收门槛 |
| 时间分组依赖 updatedAt 判定「今天/昨天」，跨零点/时区边界易错 | 单测覆盖跨零点用例（23:59 → 00:01 分组切换）；实现用本地日期比对（沿用现 timeLabel 思路），不引入时区换算 |
| REQ-017 顶栏主题按钮若移除，属存量验收条款变更，易被 QA 判偏差 | CHG-006 影响评估已登记：随 design-iter-11 基线由 CEO 定夺并同步改写 spec，不作为实现偏差 |

## 实际结果（复盘时回填）

- 完成任务：{{}}
- 复杂度：计划 Σ10 vs 实际完成 Σ{{}}（新增范围注明）
- 砍掉的范围与原因：{{}}
