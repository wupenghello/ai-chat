# 迭代计划 — iter-2（2026-08-15 ~ 2026-08-28）

> 容量依据：iter-1 复盘建议初值 Σ ≤ 10（S=1/M=2/L=3），拍脑袋值，本迭代末复盘校准。
> 取舍记录：原草案含 REQ-009 自动命名（S），CEO 拍板砍掉排 iter-3，守住容量 10（2026-08-15）。

## 迭代目标

用户可以为会话设定系统提示词、在生成中随时停止，同时补上集成路径自动化测试——不再依赖 CEO 试用才发现真 bug；度量采集脚本恢复全自动运行。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-2` tag）才能开始开发，设计任务排在开发任务之前。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 iter-2 UI 设计稿并基线 | REQ-008 / REQ-010 | M | 拆解：2 个 UI 触点（设置页系统提示词输入区、生成中发送按钮→停止按钮的状态切换），复用 design-system 现有规范与 iter-1 已基线的设置页模式 | 设计稿经 CEO 批准并打 `design-iter-2` tag；无新设计令牌则走查记录即可 |
| T1 集成路径自动化用例 | 复盘改进项 C（tailoring.md 已承诺，2026-08-15） | L | 引入本项目未用过的组件挂载级测试（新技术档）：两条用例需真实组件树 + mock 流式 API + 断言 DOM 更新 | ①"发送消息→不刷新页面流式渲染"用例自动化通过；②"生成中切换会话后台继续生成、切回可见"用例自动化通过；纳入 `npm test` 常规执行 |
| T2 停止生成 | REQ-010 | M | 拆解：按钮态切换（T0 设计稿）+ 复用既有 AbortController（REQ-003 中断、DEF-009 删除终止已建基础）+ 中断内容持久化与"停止时流恰好结束"边界；类比 REQ-003 的中断逻辑（iter-1 已实现） | 点击停止后 200ms 内停止渲染新内容、输入框恢复可发送；已生成部分保留并随会话持久化；流恰好已结束时按正常结束处理不报错 |
| T3 系统提示词设置 | REQ-008 | M | 拆解：设置页 UI（T0）+ 本地持久化 + buildContext 组装（组装点已存在于 REQ-002 实现）+ 与 20 轮截断的交互验收；类比 iter-1 SettingsForm 持久化模式 | 设置"回复只用英文"后同一会话内 AI 回复为英文，清除后恢复；刷新后仍生效；会话超 20 轮时请求体中系统提示词仍在首位；留空按无提示词正常请求 |
| T4 DEF-001 度量脚本根因排查 | 铁律 5 流程基础设施（plans/defects.md DEF-001） | S | 单脚本环境问题定位（collect.sh 退出码 129，手动逐行复刻正常），无分支开发路径，排查类任务 | collect.sh 在当前执行环境正常退出并写入 csv，无需人工绕行；根因与修复记录更新进 defects.md |

- DEF-002（GLM 补验）不占复杂度：外部依赖 CEO 充值 key，充好后作为验证动作随时补做，不阻塞其他任务。

## 本迭代不做

| 需求 | 原因 |
|------|------|
| REQ-009 会话自动命名 | CEO 拍板砍掉以守容量初值 10（本为 S），排 iter-3 首选 |
| REQ-011 Markdown 渲染与代码块复制 | L（引入 Markdown 渲染库 + XSS 防护，新技术），容量不足，排 iter-3 评估 |
| REQ-012 会话重命名 / REQ-013 导出会话 | P2，排 iter-3+ |

## 风险清单

| 风险 | 应对 |
|------|------|
| T1 测试环境与现有 vitest 配置冲突或搭建超预期 | 时间盒控制：优先 @vue/test-utils 组件挂载方案；不可行则降级为 Playwright 冒烟脚本并记入 tailoring.md |
| GLM key 未充值，DEF-002 继续挂起 | 外部依赖只能等 CEO 充值；发布记录继续如实声明，不阻塞其他任务 |
| 系统提示词与 20 轮截断交互出回归 | T3 验收明确含"第 30 轮请求体系统提示词居首"，并在 client.spec 补截断组合用例 |

## 实际结果（复盘时回填）

- 完成任务：{{}}
- 复杂度：计划 Σ10（S=1/M=2/L=3）vs 实际完成 Σ{{}}（新增范围注明）
- 砍掉的范围与原因：{{}}
