# 迭代计划 — iter-1（2026-08-17 ~ 2026-08-28）

> CEO 已确认（2026-08-15）。补充决定：前端框架 **Vue 3**（Composition API + `<script setup>`），架构要求**模块化**（见 T1 验收）。开发用真实密钥为 GLM（模型 glm-5.3），存于本地 `.env`，**绝不入库**（仓库仅含 `.env.example` 占位）。

## 迭代目标

用户配置好 API 密钥后，能完成多轮流式对话，会话可新建/切换/删除并本地保存恢复——MVP 完整闭环可用。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-1` tag）才能开始开发，设计任务排在开发任务之前。

| 任务 | 对应需求 | 预估 | 估算依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 设计师产出 iter-1 设计稿（基于原型细化）并基线 `design-iter-1` | 全部 UI | 0.5d | 拍脑袋值 | 覆盖 8 条 P0 的页面状态，CEO 评审通过，tag 打在项目仓库 |
| T1 工程脚手架 + 测试框架（Vue 3 + Vite + TS，含 npm test） | - | 1d | 拍脑袋值 | `npm test` 可跑、hook 门禁生效；模块化结构：`src/api/`（供应商客户端）、`src/stores/`（会话/设置状态）、`src/components/`（UI 组件）、`src/db/`（持久化）、`src/composables/`（复用逻辑），职责单向依赖，不出现跨层直连 |
| T2 设置页：base URL/模型名/密钥 三项 + 校验 + 清除密钥 | REQ-014 | 1d | 拍脑袋值 | 不完整配置不写入；清除后本地无残留；git 全量历史检索不到真实 key |
| T3 API 客户端：OpenAI 兼容流式调用 + 上下文组装（20 轮截断） | REQ-001/002 | 2d | 拍脑袋值 | mock 下流式解析正确；第 30 轮请求仅带最近 20 轮 |
| T4 对话界面：流式渲染 + 错误气泡/重试/401 引导设置页 | REQ-001/007 | 2.5d | 拍脑袋值 | 401/429/500/网络失败四态区分；重试复用原消息 |
| T5 会话管理：新建/历史/切换/删除 + 生成中断标注 | REQ-003/004/005 | 1.5d | 拍脑袋值 | 生成中新建/切换 → 中断并标注；删除有二次确认 |
| T6 IndexedDB 持久化与恢复 | REQ-006 | 1d | 拍脑袋值 | 刷新后全部恢复；流式中断刷新 → 恢复时标"生成中断" |
| T7 集成验证 + 视觉走查（对照设计基线）+ 修复 | 全部 | 1d | 拍脑袋值 | GLM（glm-5.3）真实冒烟通过；走查清单出档 |

**合计 10.5d / 容量 10 工作日**。偏差超 30% 时砍范围保节奏，首选砍 T6 挪 iter-2（会话在内存中仍可用，MVP 主闭环不受损）。

## 本迭代不做

| 需求 | 原因 |
|------|------|
| REQ-008 系统提示词设置 | P1，CEO 已确认降级，排 iter-2 |
| REQ-009 会话自动命名 | P1，排后续迭代 |
| REQ-010 停止生成 | P1，排后续迭代 |
| REQ-011 Markdown 渲染与代码块复制 | P1，排后续迭代 |
| REQ-012 会话重命名 | P2 |
| REQ-013 导出会话 | P2 |

## 风险清单

| 风险 | 应对 |
|------|------|
| DeepSeek/GLM 流式协议细节差异 | T3 全程 mock 开发，T7 真实 API 冒烟兜底 |
| IndexedDB 复杂度超预期 | 降级方案：localStorage 分片存储，走变更记录 |
| 首次估算无校准（全拍脑袋值） | 偏差 >30% 砍范围不延期；复盘积累校准数据 |

## 实际结果（复盘时回填）

- 完成任务：
- 工作量：预估 10.5d vs 实际
- 砍掉的范围与原因：
