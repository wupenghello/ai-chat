# 迭代计划 — iter-16（2026-08-19 ~ 2026-09-02）

> 状态：**已批准（2026-08-19，CEO 批准「批准，按草案落盘」，按草案原文落盘）**。
> 变更依据：requirements/changes.md **CHG-010**（已批准 2026-08-19，CEO 原话「全部按推荐批准」，基线 req-baseline-v7）——本迭代 = 七期路线第四期 **B2「三级上下文压缩」**，排入 REQ-039（P0）/ REQ-040（P1）/ REQ-041（P1）+ REQ-002/033/036 改写实现面 + 波及 5 项（REQ-015/030/034/037/038）复验；A1/A2/B1（REQ-030~038）已于 iter-13/14/15 全交付并关闭 G4，B1 已提供三项 B2 前置（REQ-036 分区挂载点 / REQ-037 遥测数据底座 / REQ-038 admin 承载面）。
> 迭代时长：名义标准 2 周（2026-08-19 ~ 2026-09-02，项目 tailoring.md「迭代长度：2 周」适用，无偏离需登记）；沿近期单日迭代节奏（iter-13/14/15 均 1~2 天内收口），实际周期以开发 → 测试 → QA 审计 → Code Review → 复盘全部收口为准。
> 定夺状态：CHG-010 九项定夺已**全部定案**（②摘要模型跟随回合当前模式不引入独立配置 / ③阈值「T0 取证后定死」0.75Y、实施期 32768 占位 / ④手动压缩做、入口 = 侧栏「···」菜单项 / ⑤独立表 context_summary 不写回会话档 / ⑥原文永久在库不做恢复 UI / ⑦30 轮验收 X 值随 T0 定死 + 假上游 usage 机器读数 / ⑧回合内摘要调用计回合 token、手动压缩不计回合仅落遥测 / ⑨微参数 K=2 / R=5 / 30s / ≤800 字）——本计划**无开放定夺项**；定夺③⑦的精确值由 T0 取证回填（非定夺项，取证即定死）。

## 迭代目标

三十轮长对话不再失忆：中段历史被自动摘要替代参与组装、第 1 轮关键信息仍答得对，请求体被阈值管住不随轮数膨胀（库内原文零删除、界面口径全部不变）；用户可在侧栏「···」菜单主动压缩某个会话并看到结果；部署者可在管理后台看到压缩次数、降幅与摘要调用成本（全部机器采集，缺失如实标缺失、不造数）；未超阈值或压缩降级时基线 v6 口径零回退（前端 324 + 后端 239 存量用例回归全绿）。

## 任务表

> 涉及 UI 的任务：先出设计稿并基线（`design-iter-N` tag）才能开始开发，设计任务排在开发任务之前。
> 含 UI 的任务，验收标准必须包含「design-iter-N 走查清单留档」（v1.4.4：走查是任务级门槛，不是迭代末补做）。

| 任务 | 对应需求 | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T0 前置取证（阈值与 30 轮验收定死 + 摘要 prompt 真实冒烟）：① 真实 DeepSeek（统一 key）30 轮混合会话（穿插搜索工具回合）跑测，取 step=1 prompt_tokens 基线 Y（机器读数，铁律 5，不手数 token）② 阈值定死 **0.75Y**（取整到千位）写入 config 新 settings 字段（.env 可覆盖）口径定稿；30 轮验收 X 值同源定死（定夺③⑦精确值，出处 CHG-010 内容 3.4）③ 摘要 prompt 真实冒烟——质量（开头事实/要求/偏好与工具结论保留度）与字数（≤800 字达成度）实测留档，定稿文案逐字登记（后端拥有、逐字断言面）④ 沿 iter-14/15 T0 取证模式留档，真实 30 轮冒烟走查记录同段落档（REQ-039 验收 5 的 T0 取证段） | REQ-039（验收 5「真实 DeepSeek 30 轮冒烟走查留档 T0 取证段」；描述「T0 取证后定死」阈值条款）+ REQ-041（tokens_before 触发依据口径同源） | S | 类比：iter-14/15 T0 单点取证均定 S（路径明确、零产品代码改动）；本次为一次真实 30 轮会话取证 + 两个参数定死 + 一份 prompt 冒烟，性质同档取 S（若冒烟暴露缺陷按登记纪律另立 DEF，不预先吃余量） | Y / 阈值（0.75Y 取整）/ X 精确值与测量法留档 `plans/iter-16-verify.md` T0 段，并回填 changes.md CHG-010 定夺③⑦精确值字段；摘要 prompt 定稿文案逐字登记（逐字断言面）；冒烟质量与字数实测结论留档；零产品代码改动、无未登记变更 |
| T1 design-iter-16 设计基线：两触点 + API 口径——① REQ-040 手动压缩入口 = 侧栏会话列表项「···」菜单「压缩上下文」项（复用 REQ-026 通用下拉菜单组件，零新增交互模式；执行中态/失败态/无需压缩态 + toast 三态文案定稿）② REQ-041 admin 遥测视图压缩卡（压缩次数 + 平均降幅，缺失态缺失标注）；API 口径章节随稿定案（`POST /api/chat/compact` 端点形状、成功/无需压缩/失败/409 生成中语义）；**卫生项：spec REQ-040/041「涉及页面」指针随基线零滞后回填（v1.4.10 A 防线）** | REQ-040（主）+ REQ-041（admin 面，同稿承载）+ REQ-026/038（既有口径零回退锚点） | M | 类比：iter-15 design-iter-15 为 1 遥测视图触点 + API 口径章节 + 三态定 M；本次 2 触点（主界面菜单项 + admin 压缩卡）+ API 口径章节 + 四态，但菜单项复用既有组件、admin 面为 iter-15 遥测视图加法扩展，规模同档取 M | CEO 批准基线（tag design-iter-16）；手动压缩入口形态与 toast 三态文案定死（样件文案逐字断言面——v1.4.10 B）；admin 压缩卡形态与缺失标注语义定稿；走查清单随稿（亮/暗双主题 + 执行中态 + 失败态 + 无需压缩态 + 缺失态，几何断言面——v1.4.11 B、行为断言面适用项——v1.4.13 B；= REQ-040 验收 5 + REQ-041 验收 5 断言面）；REQ-003/004/005/012/016/026 零回退组逐条映射既有走查编号；spec「涉及页面」指针零滞后回填 |
| T2 后端三级压缩管道核心 + 迁移 v9：① 一级 snip——wire 层确定性裁剪（仅最近 K=2 条 tool 消息保留结果全文，更早替换占位文案 `[旧工具结果已裁剪：{工具名} · {状态}]`，逐字断言面登记 verify 文档）② 二级 compact 摘要调用基建——非流式、30s 独立护栏、跟随回合当前模式（统一 key → .env 三变量 / 自填 → 生效档案三要素，模型名随模式默认、零新增配置面）、摘要 prompt 用 T0 定稿、注入前 `<conversation_summary>…</conversation_summary>` 字面包裹防注入、摘要作为独立 system 消息挂载 system[1] 之后历史之前 ③ 三级阈值判定——读上一回合 step=1 telemetry tokens_prompt 机器实测值，无记录按未超阈值处理（保守不造数）；实施期占位 32768，T0 定死值同批写入 config ④ 失败降级——error/timeout/空摘要/4xx/5xx 恒回退基线 v6 组装（20 轮窗口 + snip），回合不阻塞、warning 日志 ⑤ 迁移 v9——context_summary 表（PK (user_id, session_id)、水位 watermark_msg_id、ON DELETE CASCADE）+ telemetry tokens_before/tokens_after 加法列（存量 NULL 不回填）+ compress 行创建与落库（turn 端点自动回合关联、tokens_after=NULL 待懒回填）⑥ 摘要 tokens 计入回合累计（quota.py 零改动）+ 水位失效判定（编辑重建删水位 → 行删除失效；版本切换分支水位不存在 → 视同失效）+ 阈值与微参数（K=2/R=5/30s/800 字）config 字段（.env.example 占位）⑦ 卫生：库内消息全文零删除、表与日志零 key 零消息内容全文；与 LWW/409 守卫/整档透传零交互 | REQ-039（主）+ REQ-041（迁移 v9 与 compress 行数据面）+ REQ-002/033/036 改写实现面 + REQ-015/030/034/037/038 波及复验 | L（取下沿 3） | planning.md §2 L 定义「跨模块协作」命中：agent.py 组装内嵌管道 / 新增压缩模块（摘要调用器 + 产物读写 + 水位失效，CHG-010 预估 150~200 行新模块）/ db.py 迁移 v9 / telemetry.py compress 行 / config.py 五处协作；类比 iter-15 T2 五模块协作取下沿 3 定级（实际相符）；取 3 不取 4 的理由：全部改造在既有 agent 运行时与遥测框架上（组装器已单点收敛、迁移体系 v8 先例、采集点挂既有调用路径），无本项目未用过的新技术；迁移 v9 与 compress 行（REQ-041 数据面）为任务内携带子项——沿 iter-13/15 单任务携带多 REQ 先例，不推高 Σ | REQ-039 验收 1~7 逐条（①snip 确定性逐字断言 / ②超阈值本回合请求体含摘要 system 消息且挂载 system[1] 之后、历史仅最近 R=5 轮 / ③阈值下零回退与基线 v6 逐字段等价、含旧工具会话按验收 1 口径断言、改写映射登记 / ④假摘要调用 500/超时回退不压缩、turn.end 正常、compress 行 status=error/timeout / ⑤30 轮第 31 次请求体 ≤ X token（假上游 usage 机器读数）且关键事实答对、真实 DeepSeek 冒烟走查引用 T0 留档 / ⑥压缩前后会话档 messages 数量与全文逐字不变、GET 输出不变 / ⑦产物仅存 context_summary、PUT 载荷形状零变化不含摘要字段，pytest + MockTransport；PUT 载荷断言 vitest）；REQ-041 验收 1~2（compress 行完整性：恰 1 条、tokens_before=触发依据值、tokens_after 懒回填后与下一回合 step=1 llm 行 tokens_prompt 一致；test_quota 全套零改动复跑全绿 + test_telemetry 全绿、行形状仅加法列）；REQ-002 验收增补与 REQ-033 验收 1/2 新口径复跑；quota.py 与 usage_daily 数据面零改动验证；pytest 全绿 + ruff clean |
| T3 手动压缩 + admin 压缩卡与全局回归收口：① 后端 `POST /api/chat/compact` 端点——鉴权与会话归属校验（沿复合主键隔离）、同步执行完整管道（snip + 全量 compact，不受阈值约束）、成功/无需压缩/失败/409（该会话生成中拒绝）语义；不计回合、usage_daily 零写入（定夺⑧）② 前端侧栏「···」菜单项 + toast 反馈（按 design-iter-16；复用 DropdownMenu，数据面 client.ts/sessions.ts 零改动）③ tokens_after 懒回填——该会话下一次 step=1 上游调用返回 usage 后独立短连接回填（失败不阻塞、不补造）④ admin 聚合扩展——`GET /api/admin/telemetry` 加法扩展压缩次数与平均降幅（1 − Σtokens_after/Σtokens_before，任一侧缺失显缺失标注）、成本聚合计入 compress 行 tokens；403 门禁沿 get_admin_user；AdminView 压缩卡（按 design-iter-16）⑤ 全局回归基线 B2 面收口走查 | REQ-040（主）+ REQ-041（懒回填 + 聚合 + admin 面）+ REQ-038（口径零回退锚点） | M | 类比：iter-15 T3「一个聚合端点 + 一个视图」定 M；本次为一个 compact 端点 + 一个菜单项 + 懒回填短连接 + 聚合加法扩展 + 压缩卡，但全部建立在 T2 已交付的管道与产物表上、无新数据模型；拆解：主对话面零触达（REQ-001~019 触点不变），前端仅菜单加法项与 toast | REQ-040 验收 1~5 逐条（①compact 端点落 compress 行 endpoint='compact' turn_id=NULL、context_summary 更新、usage_daily turns 零变化、test_quota 零改动复跑 / ②手动压缩后下一回合请求体含摘要 / ③无需压缩分支零上游调用（假传输层零调用断言）+ 前端提示 / ④他人 session_id → 404、普通用户操作无 admin 门槛 / ⑤design-iter-16 走查清单留档：亮/暗双主题 + 执行中态 + 失败态 + 无需压缩态）；REQ-041 验收 3~5 逐条（造数聚合数值精确断言：次数/降幅、缺失 → 缺失标注、成本聚合含 compress 行 tokens / 普通用户访问扩展聚合端点 403 零泄露 / design-iter-16 走查清单 admin 面留档：压缩卡 + 缺失态亮暗双主题）；vitest 全绿 + guard:style + 生产构建；全局回归基线 B2 面收口：pytest 239 + vitest 324 存量全绿（改写用例逐条登记映射口径），功能性删除为零 |

**携带项追溯**（planning.md §1「对应 REQ」映射规则：无独立 REQ 承载的子项填合法来源，来源编号已回查出处文档核对、不凭记忆——v1.4.8）：
- **T0 取证三项**：出处 changes.md CHG-010 内容 3.4 明文「B2 T0 以真实 DeepSeek 取证 30 轮混合会话（含搜索工具回合）的 step=1 prompt_tokens 基线 Y，阈值定死为 0.75Y（取整到千位）写入 config；30 轮验收的 X 值同源定死（定夺⑦）」+ 内容 5 工作量与排期段「T0 取证项（③阈值 Y 值与⑦ X 值实测、摘要 prompt 真实冒烟、30 轮会话走查）为 T1 实施输入前置」+ REQ-039 验收 5 明文「真实 DeepSeek 30 轮冒烟走查留档 T0 取证段」。
- **迁移 v9 与 compress 行并入 T2、懒回填并入 T3**（不另立任务）：两处均为 REQ-041 描述/主流程既有承载（「与 v9 同批落库」「压缩执行 → compress 行创建（tokens_after=NULL）→ … 独立短连接懒回填」），属 REQ 内任务归位拆分，非 REQ 外携带。
- **缺陷/观察项携带核查**：plans/defects.md DEF-001~036 全部已修复/已销账/已接受闭环；qa-audit-iter-15 三 NCR 当轮整改闭环、OBS-1~4 已随复盘与整改勾销（OBS-3 随 CHG-009 落地核对清单第 8/9 项勾验）——**本迭代无未闭环缺陷或观察项需携带**（v1.4.8）。

复杂度合计：**Σ8 = S1 + M2 + L3 + M2（容量 Σ≤10，余量 2，不顶格）**

- 口径对齐（沿 iter-13/14/15 定案数值口径：S=1 / M=2 / L=3~4，process/planning.md §2 只定义质性档位）；容量上限 Σ≤10 维持（iter-6~15 连续十轮零偏差校准，见 retros/iter-15.md）。
- CHG-010 影响评估审核稿定级 **Σ8**、拆解预估 Σ7~9（管道核心 L~XL + 手动压缩 M + telemetry/admin S~M + design S），本计划取 **Σ8**——与审核稿定级一致、取中位且留 20% 余量。**不顶格的理由**：iter-13 顶格（Σ10）代价是当轮缺陷修复 + QA 整改吃掉全部余量（复盘在案）；本期含真实 DeepSeek 30 轮取证与摘要调用质量两项外部不确定性，宁可留余量。**携带说明**：迁移 v9 与 compress 行（REQ-041 数据面）并入 T2、懒回填并入 T3，为 REQ 内归位拆分，不重复计入 Σ。
- 膨胀处置：新增范围 ≤2 可由余量吸收；触顶后继续膨胀按 planning.md §3 砍范围不延期。备砍序（任一回退属范围变更，报 CEO 批准后执行并登记）：a) REQ-041 admin 压缩卡简化——仅保留压缩次数卡，降幅卡与懒回填延后（降幅聚合缺数据面自然显缺失标注，不回退）；b) REQ-040 手动压缩整体顺延——design-iter-16 范围收窄为仅 admin 面（CHG-010 影响评估既有口径），端点与菜单项留下期；**REQ-039 三级管道核心为底线不回退**（审核稿 B2 主体 + REQ-002 终态演进承诺）。

**全局回归基线（B2 面收口）**（沿 CHG-007 定义、CHG-009/010 延展的验收门槛，RTM 独立行跟踪）：迭代关闭时前端 324 + 后端 239 存量用例全绿（计划时点口径引 iter-15 终态收口记录，启动时以 pytest collect / vitest list 实测核实）+ 本轮新增全绿；REQ-033 验收 1「组装等价」含旧工具回合的用例按裁剪占位口径改写、逐条登记映射（旧断言 → 新断言，CHG-010 测试基线段既有口径）；功能性删除为零；度量数据全部机器采集（铁律 5）；RTM 全局回归基线行 B2 面收口为全期达成。

## 本迭代不做

| 需求/事项 | 原因 |
|------|------|
| 移动端主界面适配 | CHG-007 定序：让位至 agent UI 形态稳定后（约 B1 后，可与 D2 搭班）——CEO 2026-08-17 批准；B2 已交付，排期锚点满足后择机评估，纳入走 CHG |
| A2 体验深化主题包 ①引用上标/角标联动 ②搜索质量与时效调优 ③搜索过程可视化增强 | CHG-009 定夺⑦定案不整包搭班（2026-08-18 CEO 批准）：④⑤ 已由 REQ-037/038 收编；①②③ 为纯体验层留暂缓池（②建议与「搜索供应商对比评估」迭代合并评估）；B2 容量 Σ8 无搭班余量 |
| 搜索供应商对比评估（Tavily vs 博查横向专项） | 暂缓池条目（CEO 2026-08-18 指示「后期专门弄个迭代测试」）：以 A2 上线后真实运行数据为主要输入，数据积累后择机排期，纳入走 CHG |
| 真实天气工具（和风天气类专用 API） | 暂缓池条目（iter-14/15 计划定案不纳入）：和风凭据 + 专属 Host 已验证备存服务端 .env，B2 后排期时再评估 |
| C 五层记忆体系 | 七期路线第 5 期（暂缓池，候选非承诺，纳入走 CHG）；本期恰好为其交付复用面——摘要调用基建（调用器 + context_summary 存储 + 水位语义）随 REQ-039 落地（CHG-010 暂缓池联动注记） |
| D1 deep-research 子代理 / D2 hooks | 七期路线第 6/7 期（暂缓池，候选非承诺，纳入各走 CHG；D2 可与移动端搭班） |
| v0.5.0 发布 | 独立卡服务器（VPS/域名/HTTPS）就绪，不属本迭代范围；发布决定（G3）由 CEO 批准（CHG-010 发布影响：不受本变更影响） |

## 风险清单

| 风险 | 应对 |
|------|------|
| ① 摘要调用质量与成本不确定：摘要质量直接决定 30 轮连贯性验收（开头关键信息保留度不可预先保证）；摘要调用 tokens 计入回合（定夺⑧），用户感知配额内消耗略增；自填端点对非流式摘要调用的兼容性各家不一 | T0 摘要 prompt 真实冒烟先行（质量与字数实测为实现输入，不凭文档臆断）；摘要调用跟随回合当前模式、失败/超时/空摘要**恒降级**不压缩组装（降级方向恒为现状，用户无感）；tokens 如实入账 compress 行与回合累计（铁律 5），admin 压缩卡可观测效果与成本；自填端点拒绝摘要调用同失败分支降级，不区分错误形态 |
| ② 阈值参数依赖 T0 实测：Y 值若异常（过大/过小）则 0.75Y 阈值与 30 轮验收 X 值需重定，占位值与定死值存在切换窗口 | 取证先行（T0 为 T2 实施输入前置，沿 iter-14/15 T0 模式）；实施期以保守占位 32768 兜底不阻塞开发（CHG-010 定夺③定案口径）；T0 定死值随 T2 同批写入 config 并留档回填 changes.md 定夺③⑦；若 Y 实测异常（如搜索回合 32 KiB 结果叠加致基线远超预期），阈值按 0.75Y 口径重定并登记 T0 取证段，不另走变更（参数定死口径 CHG-010 已授权） |
| ③ T2 为本期最重任务（L，五模块跨协作），且含摘要调用器新模块与水位失效语义等首次引入的逻辑面 | 拆六个子步骤逐项验收（snip → 摘要调用基建 → 阈值判定 → 失败降级 → 迁移 v9 → 计账与卫生），管道降级方向恒为基线 v6 组装作为每步回归锚点（阈值下零回退 = 逐步验收的恒等基准）；备砍序 a/b 预备（REQ-039 管道核心为底线）；开发纪律沿制度 v1.4.14 B 条：任何并行 agent 完成回报由主会话亲跑测试门槛核验后采信 |

## 实际结果（复盘时回填）

- 完成任务：{{}}
- 复杂度：计划 Σ8 vs 实际完成 Σ{{}}（新增范围注明）
- 砍掉的范围与原因：{{}}
