# 迭代计划 — iter-10（2026-08-16 ~ 2026-08-30）

> **已批准——2026-08-16 CEO 批准，Σ4 方案（T1~T3，清理收口小迭代）。**
>
> 容量依据：iter-9 复盘（retros/iter-9.md Q4）上限维持 **Σ ≤ 10**（S=1/M=2/L=3）。本迭代 Σ4，小迭代，容量宽裕。
>
> 范围来源：CEO 2026-08-16 拍板「单独做 C（清理小迭代）」——基线 v3 需求（REQ-001~025）已全部达成，本迭代**无需求类任务**，全部为技术债/工程改进，按 planning.md「对应 REQ」映射规则用合法来源标注（复盘改进项 / QA 审计观察项 / Code Review 观察项），追溯链完整。
>
> 方向留档：云部署 + v0.5.0 发布暂缓（CEO 待服务器配置，发布记录 releases/v0.5.0.md 已就绪）；新需求迭代暂缓（spec 暂缓池预告「联网搜索/工具调用——待后端就绪后走下一次需求变更」，后端已就绪，为 **iter-11 第一候选**）。
>
> v1.4.7 全程生效：提交防漏核对（含 RTM + 周报条目随任务提交）+ 变更登记即时化 + 文档随任务收尾 + 走查清单（本迭代无 UI 变更，走查不适用须注明）。

## 迭代目标

清完三项技术债与全部观察项挂账，工程面与文档一致无遗留，为 v0.5.0 上线（待服务器）与新需求迭代（iter-11 候选：联网搜索/工具调用）扫清底账。

## 任务表

> 本迭代无 UI 变更任务，设计稿/走查清单不适用（各任务验收标准内注明）。

| 任务 | 对应来源（合法追溯） | 复杂度 | 判定依据 | 验收标准 |
|------|---------|------|---------|---------|
| T1 技术债三项清理 | iter-7 Code Review 观察项（settings.boot 重试）+ iter-5 QA 审计观察项 4（auth_sessions 清理、useTheme 单测） | M | 三个完整小项各含测试（3×S 打包）；类比 iter-9 T3 多 S 打包 M（偏满），本次偏松取 M 保守 | ① settings.boot() 失败后本会话可重试（不再「档案列表空至刷新页面」），前端 spec 覆盖重试路径；② auth_sessions 过期行惰性清理（登录/校验时顺带删 `expires_at < now`），pytest 双断言：过期行被清、有效会话不受影响；③ useTheme 单测覆盖（切换/持久化/初始读取）。**无 UI 变更，走查不适用（注明）** |
| T2 spec「涉及页面」hygiene（14 条） | iter-9 QA 审计观察项 5（retros/qa-audit-iter-9.md） | S | 14 处机械文档更新 + 逐条核对设计稿基线状态，路径明确无分支 | spec 中 14 条「待设计师产出」全部更新为对应 design-iter-N 已基线口径；QA 复核 0 残留。**无代码/UI 变更，走查不适用（注明）** |
| T3 Compose 形态流式首块延迟复测 | iter-8 Code Review 观察项（首块延迟取证依赖共享连接池，部署形态变化需复测） | S | 单一实测取证动作（统一 key 已配置，脚本/网络面板可观测），无开发分支 | docker compose 全链路形态下实测流式首块额外延迟 ≤500ms（spec 非功能口径），数据留档（verify 文档）；若超标登记 DEF 并定位（nginx 反代 vs backend），不隐瞒。**无 UI 变更，走查不适用（注明）** |

**合计 Σ4**（M + S + S），容量守恒（Σ ≤ 10）。

## 本迭代不做

| 项 | 原因 |
|----|------|
| 云部署 + v0.5.0 发布 | CEO 暂缓待服务器（VPS/域名/HTTPS）配置；发布记录 releases/v0.5.0.md 已就绪，随时可走 |
| 新需求（联网搜索/工具调用等暂缓池项） | 方向 B 暂缓；联网搜索/工具调用为 iter-11 第一候选（spec 暂缓池预告「待后端就绪后走下一次需求变更」，后端已就绪） |
| RAG / 移动端 / 普通用户用量面板 / 同步冲突精细合并 | 暂缓池维持暂缓 |

## 风险清单

| 风险 | 应对 |
|------|------|
| T3 复测超标（nginx 反代多一跳影响首块延迟） | proxy_buffering off 已配置（SSE 直通）；若超标属复测发现而非新缺陷——登记 DEF 定位（nginx vs backend），留档数据不隐瞒 |
| auth_sessions 清理误删有效会话 | 惰性删除只清 `expires_at < now` 行 + pytest 双断言（过期被清、有效不受影响） |
| 清理迭代无 REQ 承载被审计质疑范围合法性 | planning.md「对应 REQ」映射规则明确允许流程基础设施类任务用合法来源（复盘改进项/缺陷/QA 观察项编号）；本计划表逐任务标注来源编号 |

## 实际结果（复盘时回填）

- 完成任务：{{}}
- 复杂度：计划 Σ4 vs 实际完成 Σ{{}}（新增范围注明）
- 砍掉的范围与原因：{{}}
