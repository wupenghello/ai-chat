# 发布记录 / Checklist — ai-chat v0.2.0（2026-08-15）

> 发布记录在发布动作**之前**写好，不是事后补（见 process/release.md）。

## 本版包含

自 v0.1.0（MVP，8 条 P0）以来，iter-2 + iter-3 交付 6 项新特性（REQ-008~013），RTM 全部「已验证」：

| 需求 | 说明 |
|------|------|
| REQ-008 | 系统提示词设置（全局单一，CEO 拍板；组装恒居首位不受 20 轮截断） |
| REQ-009 | 会话自动命名（首条消息前 20 字 + 省略号；手动改名后不被覆盖） |
| REQ-010 | 停止生成（主动停止 = 中性胶囊，与「生成中断」警告胶囊区分） |
| REQ-011 | Markdown 渲染与代码块复制（markdown-it + DOMPurify 双保险，代码块语言标签 + 复制按钮） |
| REQ-012 | 会话重命名（双击/铅笔行内编辑，Enter/Esc/失焦保存，空标题恢复） |
| REQ-013 | 会话导出（Markdown 文件，`## 用户 / ## AI` 区分，空会话 toast） |

质量证据：62/62 测试、走查 28/28、QA 审计 0 NCR（符合）、生产构建通过（vue-tsc + vite build）。

## 已知问题

| 问题 | 处置决定 |
|------|---------|
| GLM 供应商未真实实测（key 余额不足） | CEO 决策（2026-08-15）不充值、不补验，REQ-014 接受部分达成（DEF-002 关闭）；DeepSeek 实测通过，协议同 OpenAI 兼容 |
| 密钥存浏览器 localStorage（无加密） | spec 已接受（单用户本机使用） |
| 窄视口（<768px）无响应式 | spec 明确 MVP 不承诺 |
| interrupted/stopped（中断/停止）消息无重试入口 | 与基线一致（重试仅绑定错误气泡），技术债登记 |

## Checklist（逐项执行并留痕）

- [x] 全部测试通过，无跳过用例（62/62，2026-08-15 12:36 执行记录）
- [x] P0 需求全部「已验证」（RTM 为证，8/8）
- [x] 版本号已更新（package.json 0.2.0 = tag v0.2.0）
- [x] 部署步骤已列出并执行，输出已记录（gh-pages 分支 e5ec292 已推送，Pages 生效）
- [x] 部署后冒烟验证点全部通过（见下）
- [x] 回滚方案明确（见下）
- [x] **CEO 发布批准**：2026-08-15，CEO 明确选择"批准发布 v0.2.0"

部署后冒烟验证结果（线上 wupenghello.github.io/ai-chat，与部署产物同一份）：

1. ✅ 页面打开：HTML 200，资产 JS/CSS/.nojekyll/favicon 全 200，应用挂载完整
2. ✅ 设置页配置：settings store 代码未变（v0.1.0 已发布验证）
3. ✅ 发送消息流式：streamChat 未变（v0.1.0 已发布 + iter-2 真实 DeepSeek 实测）
4. ✅ Markdown 渲染：线上 bundle 含 markdown/DOMPurify/code-block/code-copy + 新令牌 4cc38a（iter-3 dev 交互实测：代码块语言标签+复制按钮+"已复制"反馈）
5. ✅ 会话重命名 + 自动命名：线上 bundle 含"重命名"（iter-3 dev 交互实测：双击改标题 + 首条消息自动命名省略号）
6. ✅ 导出会话：线上 bundle 含"导出"（iter-3 dev 交互实测：顶栏导出按钮 + export.ts）
7. ✅ 刷新页面会话恢复：IndexedDB 持久化未变（v0.1.0 已发布验证）

> 说明：交互冒烟在 iter-3 dev（同一份源码）完整走过 28 条走查 + 62 用例；本次部署冒烟以线上 bundle 内容校验 + 资产 200 确认部署产物为 v0.2.0 代码，未在线上重复点按（线上为 gh-pages 静态托管，交互路径与 dev 构建一致）。

## 部署步骤（已执行）

1. `npm run build:pages`（vite build，`--base=/ai-chat/`）→ dist/（index.html 0.49KB / css 17.87KB / js 255.87KB，gzip 3.77KB / 102.74KB）
2. dist/（独立 git 仓库，gh-pages 分支）提交 e5ec292 并推送
3. GitHub Pages 从 gh-pages 分支生效（线上 200，新资产 hash 已引用）
4. 线上验证：`https://wupenghello.github.io/ai-chat/` HTML 200，JS/CSS/.nojekyll/favicon 均 200

## 回滚方案

- **怎么退**：`git -C dist push origin a4e1bdd:gh-pages --force`（回退到 v0.1.0 的 gh-pages commit）；或 `git push origin --delete gh-pages`（Pages 即 404）
- **退到哪**：v0.1.0 线上状态（gh-pages commit a4e1bdd）；本地与 tag `v0.1.0` 代码不受影响
- **预计耗时**：< 2 分钟；线上无服务端数据需迁移（用户数据全在本机浏览器 IndexedDB）
