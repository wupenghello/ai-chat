# v1.1.0 发布记录（2026-08-23）

> CEO 批准「你先发布部署吧」（2026-08-23）——v1.0.0 后首个功能版。semver：功能性新增（REQ-053）→ minor 位。

## 本版包含（v1.0.0〔a7c6c88〕以来全部）

| 批次 | 内容 | 提交 |
|------|------|------|
| CHG-016 / iter-22 REQ-053 真实天气工具 | 和风天气接入（148 城坐标表 + 坐标兜底 + 出网白名单动态化）；demo_weather 移除（echo 保留）；基线 req-baseline-v13 | 70f5392 / caac620 / 02c4377 / 5aad421 |
| DEF-041 流式滚动劫持 | 输入意图先行（上滚轮/触屏下滑立即脱离跟随，先于位置判定——DEF-034 残留微任务竞态） | 30255d0 |
| DEF-042 设置分区样式不统一 | MemoryPane 复用 class 显式对齐（scoped 边界）+ 跨分区一致性契约断言 | 624b9e7 |
| 版本号三处落位 | package.json / pyproject.toml / main.py → 1.1.0 | 6a9a8a3 |

## 部署实录（沿 v1.0.0 同形）

1. 版本落位 6a9a8a3 推 GitHub；本地 `npm run build`（dist 800K，assets `index-DqrTkuRK.js` / `index-BVbsWC2k.css`）
2. dist tar 打包 scp 上传服务器解压至 /opt/ai-chat/dist；服务器 `git pull`（a7c6c88 → 6a9a8a3）
3. `backend/.env` 增 `AI_CHAT_WEATHER_KEY` / `AI_CHAT_WEATHER_HOST`（和风凭据，CHG-016 后续动作面——key∧Host 均配置即启用）
4. `docker compose up --build -d`：ai-chat-backend / ai-chat-frontend 双容器重建 healthy；backend 自报 version 1.1.0；db_version 10（零迁移）

## 冒烟验证（全部通过，2026-08-23 00:2x）

- [x] HTTP 301 → HTTPS 200；首页资产 hash 与本地构建逐字一致（DEF-041/042 修复随产物上线）
- [x] 注册冒烟账号 smoke-v110（普通用户，id=3）+ /api/quota 200（research_available=true——Tavily 沿用）
- [x] **天气回合端到端（真 DeepSeek + 真和风）**：POST /api/chat/turn「用 weather 工具查北京天气」→ tool.call weather → 真实数据「北京实时天气（观测于 2026-08-23T00:26+08:00）：多云 26°C（体感 28°C），南风 3 级，湿度 87%」→ turn.end reason=done
- [x] DEF-042 契约规则在线上 CSS 产物：`.section-label.pane-label[...]{...font-size:14px;font-weight:600...}`
- [x] demo_weather 随 iter-22 移除（线上工具面仅 weather）

## 注记

- 冒烟账号 smoke-v110 留库（CEO 可经 admin 后台删除；消耗统一 key 1 回合 + 和风 2 次调用）
- GeoAPI 未启用（定夺⑥回退路径上线：城市表 + 坐标兜底）；启用后如需切两步链走变更
- iter-22 流程收口（QA 审计 → Code Review → G4 复盘）独立于本发布，另行执行

## 回退方案

- 服务回退：`docker compose down`（分钟级；数据保留 ./data/ai-chat.db）
- 版本回退：服务器 `git checkout a7c6c88` + 恢复 v1.0.0 dist（需另存）+ `backend/.env` 移除和风两行 + `docker compose up --build -d`；v1.1.0 零迁移零数据面变更，回退无数据风险
